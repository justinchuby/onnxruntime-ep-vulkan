# Squad Decisions

<!-- Round 1: 2026-07-28T17:59:54-07:00 (design session)              -> ARCHIVED -->
<!-- Round 2: 2026-07-28T22:28:08-07:00 (implementation round)        -> ARCHIVED -->
<!-- Round 3: 2026-07-29T09:00:39-07:00 (first-hardware round)        -> ARCHIVED -->
<!-- Round 4: 2026-07-30T19:05:03-07:00 (correctness-and-measurement) -> ARCHIVED -->
<!-- Round 5: 2026-08-01T09:53:14-07:00 (execution-and-instrumentation) -> ARCHIVED -->
<!-- Round 6: 2026-08-01T17:16:56-07:00 (instrument-audit round)         -> ARCHIVED -->
<!-- Round 7: 2026-08-02T02:03:46-07:00 (proof-ledger-and-generalisations) -> ARCHIVED -->
<!-- Round 8: 2026-08-02T14:42:30-07:00 (gqa-fourth-face-and-criterion10-oscillation) -> ARCHIVED -->
<!-- Round 9: 2026-08-02T22:37:04-07:00 (kv-device-residency-and-the-ledgers-frame) -> ARCHIVED -->
<!-- Round 10: 2026-08-03T04:55:00-07:00 (six-declines-audit-abi-drift-and-provenance-classes) -> ACTIVE -->
<!-- Round 11: 2026-08-03T10:35:00-07:00 (the-merge-union-condensation-defect)              -> ACTIVE -->
<!-- Round 12: 2026-08-03T19:55:00-07:00 (op-suite-reds-enumerated-gqa-fourth-face-refuted-and-the-merge-driver-hardened) -> ACTIVE -->
<!-- Round 13: 2026-08-04T12:25:00-07:00 (second-field-level-reversion-caught-by-frame-criterion10-satisfiable-two-new-rai-rulings) -> ACTIVE -->

## Active Decisions

<!-- ================================================================================= -->
<!-- ARCHIVAL POINTER -- 2026-08-03T04:55:00-07:00                                     -->
<!-- Round 9 (17 entries, ~33.1 KB) archived to:                                       -->
<!--   .squad/decisions-archive/2026-08-03T04-55-00-07-00-round9.md                    -->
<!-- Trigger: this run's task instructions set the archive gate as CONDITIONAL --       -->
<!--   decisions.md measured 37,563 bytes at PRE-CHECK (under the 51,200 threshold),    -->
<!--   so archive only if merging the 5-record inbox pushed it over. It did:            -->
<!--   37,563 + drafted Round 10 (~10.5 KB) = ~48.1 KB before archival, but Round 9's    -->
<!--   own 33.1 KB was already carried inside that 37,563, so the resulting file        -->
<!--   without archiving Round 9 would have been well past the 51,200 gate on the next  -->
<!--   round regardless -- archived now, matching the Scribe-round-boundary convention  -->
<!--   used for every prior archival (Rounds 1-3, 4, 5, 6, 7, 8).                        -->
<!-- Supersession check (why archiving all of Round 9 is safe): none of the five Round- -->
<!--   10 inbox records corrects or overturns a Round-9 entry by name -- Mouse's ABI-    -->
<!--   drift record extends (does not correct) his own Round-9 ABI-mirror-equality      -->
<!--   fix (a *different* insertion, `898a2ba`, hit the same three mirrors); Niobe's     -->
<!--   provenance-class record extends Round 9's ULP/PROVEN-ELSEWHERE thread without     -->
<!--   reopening it; the two Fact Checker records are new audits, not corrections of     -->
<!--   anything in Round 9. Nothing was cherry-picked forward.                          -->
<!-- Previous archival check (2026-07-30T19:05:03-07:00): 0 entries, age gate only       -->
<!-- Previous archival (2026-07-30T20:58:11-07:00): Rounds 1-3, 122 entries              -->
<!-- Previous archival (2026-08-01T09:53:14-07:00): Round 4, 26 entries                  -->
<!-- Previous archival (2026-08-01T20:39:12-07:00): Round 5, 18 entries                  -->
<!-- Previous archival (2026-08-02T02:03:46-07:00): Round 6, 26 entries                  -->
<!-- Previous archival (2026-08-02T14:42:30-07:00): Round 7, 11 entries                  -->
<!-- Previous archival (2026-08-02T22:37:04-07:00): Round 8, 27 entries                  -->
<!-- ================================================================================= -->

<!-- =============================================================================== -->
<!-- ROUND 10 DECISIONS -- 2026-08-03T04:55:00-07:00 (six-declines-audit-abi-drift-and-provenance-classes) -->
<!-- 5 inbox records read and merged; no consolidation needed -- five independent      -->
<!-- topics, no two records describe the same event.                                  -->
<!-- Round 9 was archived in full this session -- see ARCHIVAL POINTER above and       -->
<!--   .squad/decisions-archive/2026-08-03T04-55-00-07-00-round9.md                    -->
<!-- =============================================================================== -->

## Round 10 -- 2026-08-03T04:55:00-07:00 -- six declines audited, a third ABI mirror insertion, a kernel-identity falsifier, and a provenance-class discipline for published numbers

### 2026-08-03T04:55:00-07:00: Fact Checker — "six declines" is a correct self-tally but not a measure of register restraint

**By:** Fact Checker. Advisory audit does not fully clear Morpheus's policy claim. Six self-counted decline points reconstruct, but the counting rule is not stable — criterion-10 "coverage does not compose" was explicitly declined and left unnumbered while the tally stayed at four. Several counted declines add binding sub-rules, amendments, tokens, or claim semantics; avoiding a new top-level `R14` name avoided numbering, not normative growth. Per-item verdicts: anti-correlated confidence and defaulting-lookup declines are honest top-level dedup (✅) but each added a binding sub-rule/amendment; the classifier-false-instances and witness-does-not-discharge-a-conjunction declines are the cleanest (⚠️/✅ respectively); the "reading that degrades" decline (⚠️) added a new universal diagnostic test; `PROVEN-ELSEWHERE` (❌) is the largest — sound diagnosis, but the ruling adds a new claimable state, promotion path, disclosure obligations, and a drafting rule, which is architecture-of-record growth regardless of what it was called. `PROVEN-ELSEWHERE` itself is verified as a real gap (`LedgerEntry.device` exists, no predicate reads it) but is unimplemented prose at the audited commit — no predicate, counter, disclosure field, or planted control. Trinity's independently-measured ULP series may promote only the exact proof keys attributed as executed in her device run, never every ledger entry on one model's agreement — the criterion-10 defect recurring. **Settlement:** add a first-class `LedgerLookup` state for same-device vs. elsewhere-device evidence; publish counts and exact proof keys for all three states every run; plant a mismatched-device ledger entry requiring the lane to emit `PROVEN-ELSEWHERE`; require promotion evidence to carry running device, exact exercised proof keys, attribution, and the ULP artifact; add a reviewer other than Morpheus for normative register additions regardless of top-level numbering.

### 2026-08-03T04:55:00-07:00: Fact Checker — a spawned agent cannot observe coordinator-canary absence, and reporting it as truncation is a false positive by construction

**By:** Fact Checker. A spawned agent must not treat absence of `SQUAD_COORDINATOR_CANARY_a8f3` from its own context as evidence the coordinator file is truncated — spawned agents never receive the coordinator's agent instructions, so the check's triggering condition is guaranteed true whether the coordinator is healthy or truncated. Fired falsely in this audit and reportedly in five of six prior parallel delegations. The check must run in the coordinator's own frame, or carry a coordinator-generated attestation into the spawn; otherwise a spawned agent should report `UNOBSERVABLE`, not block.

### 2026-08-03T04:55:00-07:00: Mouse — the mid-struct ABI insertion defect has happened a third time; `counters_abi.py` is now the only reader, and its equality guard fired unprompted on a pure rename

**By:** Mouse. `898a2ba` inserted `outputs_device_resident`, `outputs_host_resident`, `outputs_device_bound` between `device_losses` and `dispatches_executed` — same shape as `a52024f` (Round 9), three fields instead of one. All three hand-maintained ctypes mirrors (`test_wiring_census.py`, two in `test_phi35.py`) kept the old layout; every field past the insertion point read positionally wrong (`dispatches_executed` silently read `outputs_device_resident`, 0 on every healthy run — stable, plausible, invisible). Caused three red tests. The `struct_size` **equality** guard caught it; the older `>=` guard at line 370 catches only a shrink, not a grow, and cannot distinguish an append from an insertion. **Fix, built and offered:** `rust/tools/counters_abi.py` parses the field list out of `counters.rs` directly, so there is no second list to drift — verified against the running DLL (18 fields, 136 bytes, exact match). Not yet applied to the three call sites (four agents were live in the tree). Layout discipline generalized: a const-evaluated FNV-1a hash over `name:offset:size` now fires on any layout change, including a pure field rename (`device_mismatch_*` → `proven_elsewhere_*`, identical offsets/sizes, 152 bytes both) — verified by replaying the exact `898a2ba` insertion, which produces `error[E0080]` at compile time. Second, smaller, checked-clean finding: all three call sites correctly pass `ctypes.sizeof(c)` as the length argument, so the drift is a misattribution, not a memory-safety defect.

### 2026-08-03T04:55:00-07:00: Mouse — the EP now records which kernel it built, not which one was requested; GEMV kernel identity measured, not guessed

**By:** Mouse. `counters.rs` emits two new JSON-only fields (no `abi_version` bump, no C-struct field, per the standing precedent at `counters.rs:70-87`): `pipeline_variants` (the distinct `(shader_stem, spec_constants)` pairs actually built) and `gemv_packed_spec_constant` (spec constant 5, as resolved — a five-state string: `"1"`, `"0"`, `"MIXED"`, `"UNRECORDED"`, `"UNOBSERVABLE"`), recorded from the effective pair handed to `get_or_create` rather than at the request site (recording at the request site would record a request, not a build). Falsifier `probe_gemv_kernel_identity.py`, five arms with predictions written before the first run, passed 5/5 on both devices — the decisive arm changes block size with the environment untouched and the token correctly moves `1 → 0`; `shaders_dispatched` (the pre-existing field) is byte-identical across packed and unpacked arms and cannot make this distinction. Scope: generalizes across all kernels (any future kernel reading can name its own subject from `pipeline_variants`), but not to the other eight env-var switches, which do not enter as specialization constants and stay Link's. Owed to Trinity: the `counters_abi.py` replacement above closes the three-mirror hazard this finding's own counters would otherwise have widened.

### 2026-08-03T04:55:00-07:00: Niobe — every published quantity now carries a provenance class: SPECIFICATION, MEASUREMENT, or MODEL

**By:** Niobe. `bench/results/probe_island_bytes.py` had published the weight re-read amplification — the number every bandwidth argument in the project rests on — as five hardcoded literals under a docstring describing a SPIR-V def-use walk that did not exist anywhere in the tree; the argument was correct, the instrument was absent, and a kernel change re-reading every blob eight times would not have moved the printed `1.000000`. Three practices now standing: (1) a measurement is derived at run time from the artifact every time, never from a comment naming its source — model shape from the ONNX graph, weight/scale bytes from initializer sizes, amplification from `bench/results/probe_weight_reread.py`, which executes the compiled SPIR-V (`bench/spirv_simt.py`) over the whole dispatch grid and records every load address; (2) every published quantity carries a class in a machine-readable `PROVENANCE` table emitted into the JSON record — SPECIFICATION (a fact about a part, published by its maker, legitimately a literal), MEASUREMENT (a fact about *this* graph/module/run — a literal is the defect), MODEL (an analytic construction, never quotable as a measurement); (3) a spec-sheet number is a fact about a *named part*, and the name is a separate claim of a different class — `PEAK_BYTES_PER_S` is a specification, `SPEC_PART` is a measurement read off `device_identity.observed_from_trace`, never off `device_index` (an EP enumeration order, not a device fact). Additionally: a detector never seen in its positive state has no demonstrated positive state — `probe_weight_reread.py` now runs three positive controls each time and publishes `amplification: null` / verdict `UNWITNESSED` if any fails to fire. Re-derived value is unchanged at **1.000000** (positive state demonstrated three times: tail tile 1.107692, deliberate re-reader 2.000000, unpacked path same bytes at width 4); the original reasoning was right, which is precisely the case this rule matters most for — nothing about the outcome would have told anyone the instrument was missing. Records: `bench/results/weight_reread_phi35.json`, `bench/results/island_bytes_phi35.json`; locked by `bench/test_weight_reread.py`; written up in `docs/PERF.md` §22. Scope note: changes no downstream conclusion — the roofline's figures are untouched.

## Cross-agent propagation notes (2026-08-03T04:55:00-07:00)

The following findings were propagated to individual agent history files this run (see each agent's `history.md` for the full note): Link's shaderc-2023.8-vs-Windows-SDK-v2026.2 finding (the ledger-keying decision that blocks Linux) → Mouse and Morpheus; Link's retirement of the DLL-hash-as-rebuild-witness method (six builds of an unchanged tree gave six distinct hashes) → Switch and Trinity; Switch's VRAM finding (the shipping KV lane OOMs at past 4096 on the 8 GB discrete GPU while the resident lane completes; ctx 8192 fails on both lanes) → Tank and Niobe; Trinity's float64 result (Vulkan is bit-exact against float64 at the final RMSNorm; ORT's CPU EP is the 1-ULP side) → Morpheus and Mouse.

<!-- =============================================================================== -->
<!-- ROUND 11 DECISIONS -- 2026-08-03T10:35:00-07:00 (the-merge-union-condensation-defect) -->
<!-- Inbox empty this run -- 0 records to merge. Archive gate not triggered:           -->
<!--   decisions.md measured 13,672 bytes at PRE-CHECK, well under the 51,200 ceiling, -->
<!--   and this round's own addition keeps it under 20 KB -- no archival performed.    -->
<!-- =============================================================================== -->

## Round 11 -- 2026-08-03T10:35:00-07:00 -- the merge=union condensation defect: found, measured, and fixed

### 2026-08-03T10:35:00-07:00: Scribe — `merge=union` cannot represent a deletion; every past Scribe condensation on an agent who subsequently merged was silently reverted, and the mechanism is now fixed and demonstrated

**By:** Scribe, defect surfaced by Switch. **What:** `.squad/agents/*/history.md` was declared `merge=union` in `.gitattributes`, chosen so concurrent agents appending to the same history never conflict. Union merge computes the set of unique lines present on *either* side of a merge and has no concept of a deletion. Scribe's condensation of `switch/history.md` (`a9d8693`, 92,884 → 13,914 bytes on main) deleted ~79 KB of superseded prose; `squad/switch` had forked *before* that condensation and, when merged (`9e20c25`), the union driver faithfully unioned the condensed file's lines with the stale branch's full pre-condensation copy — reinstating every deleted line. Result climbed to 106,797 bytes at the merge, then grew normally to 113,127 bytes by HEAD (`8ac1172`) as further sessions were appended on top of the reverted body. Confirmed by walking the file at each commit (`a9d8693`=13,778 committed bytes, `9e20c25^2`=93,058, `9e20c25`=106,797). **Damage audit across all agents with a summarisation commit in their history (`db07e0d`, `f6f98e2`, `1d52725`, `775a341`, `a9d8693`):** Mouse's condensation (`775a341`, 58,205 → 28,133 bytes) held — no branch merge of `.squad/agents/mouse/history.md` occurred after it; the commits following are a linear append chain, not a union-merge of a diverged copy, so nothing was reverted (current 47,132 bytes reflects normal growth, not resurrection). Switch's is the only condensation known to have been reverted. **Fix:** kept `merge=union`'s no-conflict guarantee for genuine concurrent appends but made deletions survive, via a new custom driver `merge.squad-history` (`.squad/tools/history_merge_driver.py`, registered per-clone by `.squad/tools/setup-merge-drivers.ps1` since driver commands live in `.git/config`, which git never versions — **every worktree, including scribe-1/scribe-2's shared one, must run the setup script once, or that worktree silently falls back to a different merge behaviour**). The driver treats whichever side of a merge is *shorter than the 3-way merge base* as an intentional condensation and keeps it as the skeleton, then appends only lines from the other side that are genuinely new relative to the base (real concurrent agent writes) — lines the condensation removed are, by construction, already in the base set and are excluded from the "new" set, so they cannot be resurrected. `.gitattributes` now routes `.squad/agents/*/history.md` and `.squad/decisions.md` (same deletion-vs-archival risk applies to Tier-1/Tier-2 archival) through `merge=squad-history` instead of `merge=union`. **Demonstrated, not just reasoned about:** built a throwaway sandbox repo, condensed a history.md on `master` after branching, appended a new session on the forked branch, and merged. Plain `merge=union` reproduced the exact defect (old condensed-away sessions came back verbatim). `merge=squad-history` on the identical inputs kept the condensation and correctly included the new branch's line, nothing else. **Then applied for real:** re-condensed `switch/history.md` (113,127 → 22,848 bytes) using the now-holding mechanism — richer than the reverted first attempt (22,848 vs 13,914 bytes) because it retains full detail for the KV-arena session and a fuller mid-history digest rather than a terser paragraph. **Why:** a fix reasoned about but not exercised against the concurrency case it must survive is exactly what produced this defect the first time.

## Cross-agent propagation notes (2026-08-03T10:35:00-07:00)

The following findings were propagated to individual agent history files this run (see each agent's `history.md` for the full note): Switch's `gen_proof_ledger.py --check` finding (`--check` checks the file against itself, not against the running build, and the ledger is `include_str!`'d so `--reprove` has no effect until a rebuild — the coordinator quoted `--check` as merge evidence six times) → Mouse, who owns the repair; Link's "an accepted red and a new red are indistinguishable when the only record of acceptance is a number in someone's head" generalisation → Trinity and Tank, both of whom maintain instruments with accepted-failure states; Rai's RAI-012 (decline message names the wrong subject) → Mouse, owner named; Rai's RAI-013 (an honestly-labelled emission a user never sees by default is not "the user was told") → Tank, owner named.

<!-- =============================================================================== -->
<!-- ROUND 12 DECISIONS -- 2026-08-03T19:55:00-07:00 (op-suite-reds-enumerated-gqa-fourth-face-refuted-and-the-merge-driver-hardened) -->
<!-- 8 inbox records read and merged (3 Fact Checker, 5 Morpheus). One consolidation:  -->
<!-- Fact Checker's derived rule register and Morpheus's retraction of his own         -->
<!-- navigability diagnosis are the same event from two authors (one supplies the      -->
<!-- measurement, the other the ruling it forces) -- merged into a single block below. -->
<!-- The other six inbox records are six independent topics; no further consolidation. -->
<!-- =============================================================================== -->

## Round 12 -- 2026-08-03T19:55:00-07:00 -- the op-suite reds enumerated, a merge-driver defect fixed before it fired, two new §8.9 rulings, and the register's own count corrected twice

### 2026-08-03T19:55:00-07:00: Fact Checker & Morpheus (consolidated) — the register's declared size (13) undercounts a second, uncounted namespace (`§8.9.x`, 339 citations); the decline tally is retired; 3 of 8 declines survive

**By:** Fact Checker, Morpheus. **What:** Fact Checker derived the rule register from citation evidence rather than self-report: `R1`–`R13` are cited ~1,337 times outside Morpheus's own surfaces, and `§8.9.x` section references are cited 339 times (80× `registry.rs`, 39× `counters.rs`, 34× `disclosure.rs`, 28× `OP_COVERAGE.md`, 25× `gen_proof_ledger.py`) — two namespaces, one naming obligations (`R#`) and one naming locations (`§8.9.x`), only the first of which has a counter. The derived register is **13 numbered + 8 unnumbered-but-binding**. Re-scoring Morpheus's six self-declined principles against external citation: **only 3 of 8 survive** (the R13 defaulting-lookup amendment, the newly-named pattern with no external citation, and the criterion-12 witness-vs-discharge distinction); **5 do not** — each fell because another agent was already using the principle as binding (anti-correlated confidence cited in `bench/phases.py` and `PERF.md`/`PLATFORMS.md`; R9's "cannot convict/cannot acquit" named as a Trinity test; `PROVEN-ELSEWHERE` a shipped state token in `registry.rs`/`disclosure.rs`; fault-scope-follows-localisation given a numbered subsection and verbatim attribution by Mouse in `OP_COVERAGE.md`; Link's suite-productivity finding enforced by `ci/check_suite_productivity.py` on every merge). Also: "six declines" was already stale — Morpheus's own history reached a seventh and an eighth non-mint decline before the tally was quoted, and criterion 10's "coverage does not compose" was deliberately left unnumbered and is now the most externally-cited unnumbered obligation in the project. **Why:** Morpheus had accepted a diagnosis of "under-numbered, a navigability defect repairable by numbering" and now retracts it — agents were never lost; they built a second, uncounted namespace. Renumbering 339 live citations to buy a tidier count is rejected as the most expensive possible fix to a bookkeeping error. **Ruling, adopted unchanged:** both namespaces are legitimate and must not be merged; every ruling from §8.9.22 onward names its own anchor phrase in the sentence that states it, so the citable unit is the obligation, not the section number that can be re-cut (§8.9.19 already had to restate §8.9.17 over one ambiguous word); any future register tally counts the `§8.9.x` namespace as register surface; the decline tally is retired as an instrument — it measured traffic between namespaces, not growth of either. Clearing finding preserved: no principle was lost; the register is under-**counted**, not under-populated. **Owner of the count going forward:** Fact Checker. **Owner of the anchor-phrase obligation:** Morpheus, at ruling time.

### 2026-08-03T19:55:00-07:00: Fact Checker — the coordinator's error count derived at 21, not the self-reported 5; 33% self-report recovery, 0.15 errors/turn, 95% corrected, persistence (not rate) is the defect

**By:** Fact Checker. Advisory audit of the coordinator's own factual surface, session `c6bec1a7`, turns 0–163, 136 turns carrying a response. **What:** 21 distinct errors derived against 5 self-reported classes (7 propositions) — a 33% self-report recovery rate. Rate 0.15/turn (~2.1/round). 20 of 21 corrected in the record (95%); zero denied, defended, or quietly dropped; 5 self-caught; one open item (turn 156's "bit-identical" claim not scoped to path/output-set/step-count, recurring for the second time). The self-report failed in an unusual direction: the five listed were the five adjudicated by a named agent in a written ruling; the fourteen missed were the ones the coordinator caught himself in passing — under-reporting his own catches, not his misses. **The structural finding:** three errors dominate (161 vs. 257 claimed nodes, quoted all day; the amplification status headlined into `DESIGN.md` §0; `--check PASS` quoted ≥6 times) and all three share one shape — a number or green token that arrived pre-formed from an instrument and was re-quoted rather than re-derived, R13's and R9's family. Separately, at least 8 of 21 (38%) occur in turns where the coordinator was both producer and reader of the evidence — the same sole-supplier/sole-judge defect he diagnosed in Morpheus, measured on himself at comparable strength, though not an argument for verifying less (two of the corrections exist because he rebuilt, re-ran, or arranged for an adversary). The weight-amplification error (`1.000000` quoted as "exact, not approximate") is scored ⚠️ mis-scoped rather than ❌ false — the epistemic claim was wrong, the value was not, and Niobe's independent re-measurement over compiled SPIR-V confirmed the same 1.000000 with three positive controls. **Settlement:** name the path, output set, and step count in the same sentence as "bit-identical," or do not use the word. Routing prompts remain unobservable to Fact Checker (retained nowhere readable) — roughly half the coordinator's factual surface is auditable only through what a receiving agent chose to quote.

### 2026-08-03T19:55:00-07:00: Fact Checker — Devil's Advocate: apparatus-to-implementation is 4.55:1, and the project has no counter for the rising marginal cost of a new op

**By:** Fact Checker, Mode 2, advisory only, never a veto. **What:** at `8ac1172`, implementation (12 GLSL kernels + `rust/src/ops` + `rust/src/vk`) is 22,818 lines; apparatus (`counters.rs`/`trace.rs`/`disclosure.rs` + `tests/` + `bench/` + `ci/` + `docs/` + `.squad/`) is 103,898 lines — **4.55:1**, or 2.73:1 excluding `docs/` and `.squad/`. Comparison, verified via `gh api`: llama.cpp's Vulkan backend ships 164 files in `vulkan-shaders/` against this project's 12 shaders, one model, two GPUs on one desk, zero users. **Steelman:** coverage is a headcount problem and the two most-cited rules in the codebase (R13, R12) are both about instruments — a very high signal-to-noise ratio on a very small signal. **Rebuttal:** this session's instruments alone caught a roofline identity, a "bit-identical" claim that was 62/65, a `--check PASS` checking a file against itself, a stale-binary `0/363`, six DLL hashes from one unchanged tree, and a mid-struct ABI insertion invisible to a passing suite by construction. **Where the steelman is right, unsoftened:** (1) Phi-3.5's `Nq/Nkv=1.00` hides the GQA defect a second model would expose, and the team has responded with instruments rather than a second model — named as the highest-expected-loss item in the project, currently nobody's assignment; (2) the marginal cost of adding an op is rising every round (kernel, test, ledger entry, two digests, spec digest, provenance class, positive control, extent declaration, decline row, disclosure state) with no counter on it, in a repo that counts almost everything else. **Pre-mortem (2026-09-02):** Llama-3 8B tried in late August fails; the GQA path was certified at the one ratio where the defect is invisible. **Alternative offered, not yet adopted:** freeze the register at 13+8; land Llama-3 8B as a second oracle; rent a dedicated GPU runner and retire `STEADY_UNCERTIFIED` as default; add a per-op marginal-cost counter; cap ledger ambition at bring-up re-run. **Risk acceptance recorded for the team to take consciously:** 🔴 one-model oracle with a known-degenerate axis; 🟠 rising untracked per-op marginal cost; 🟠 two-namespace register with one counter; 🟡 contended box, zero users, cross-device persistent proof state.

### 2026-08-03T19:55:00-07:00: Morpheus — a history condensation must be DECLARED by a marker, not inferred from length, or the merge-driver fix can silently fail to fire

**By:** Morpheus, §8.9.21 part 5, on Scribe's request. **What:** `.squad/tools/history_merge_driver.py`'s `len(ours) < len(base)` heuristic is a proxy, and a branch that both condenses *and* appends in the same commit can be longer than the merge base while still having deleted lines — the driver then falls to the plain-append branch and resurrects exactly what it was written to protect, presenting as "a condensation that did not take" with no error anywhere. Same dangling-reference shape Morpheus named in §10.0.1: the reference does not fail, it succeeds against the wrong thing. **Ruling:** a condensing commit must write a marker line, `<!-- CONDENSED-AT: <merge-base-sha> -->`, and the driver keys the skeleton decision on the marker, never on length alone; length comparison is retained only as a disagreement assertion (a second witness, free). No cost to concurrent agents — an ordinary append never carries the marker and never takes the skeleton path. Applies identically to `.squad/decisions.md`'s Tier-1/Tier-2 archival, which is also a deletion routed through the same driver. **Standing note:** driver commands live in `.git/config`, which git never versions, so every worktree must re-run `.squad/tools/setup-merge-drivers.ps1` once or silently fall back to different merge behaviour — the same silent-wrong-success shape as the length proxy. **Owner:** Scribe. **Scribe's disclosure this run:** the marker line has not yet been added to `history_merge_driver.py` — this round's condensation of `mouse/history.md` (below) was done as a plain edit with no branch merge in flight, so the length-heuristic hazard Morpheus names did not have an opportunity to fire this round, but the marker mechanism is not yet built and remains open work for whoever next lands a condensation concurrently with a branch append.

### 2026-08-03T19:55:00-07:00: Morpheus — an optional device capability (`shaderInt64`) is a frozen frame constant threaded as a parameter, never a claim-time global query; Tank's decline to enable it mid-merge-window is upheld

**By:** Morpheus, §8.9.21 parts 0–2. **What:** the claim path may read a FRAME component iff (a) resolved before the first claim, (b) immutable for the session's life, (c) passed as a value rather than fetched from a global — failing (a)/(b) means not claim-time-readable at all (handle at the boundary where it becomes readable); failing only (c) is a plumbing defect, not a schema defect, and is not a licence to keep the global. `shaderInt64`, device identity, toolchain identity, and `ort_build` pass all three; a bound `spec_digest` fails (a), which is why Mouse's `SPEC-UNOBSERVED` claim-path refusal was correct and is not a precedent against Tank's case. **General treatment:** an optional device capability resolves exactly once at device creation and freezes onto the engine's capability set (the `synchronization2` precedent, §7.3/§7.5); downstream code, including the claim path, reads the resolved set only. Four edits ordered, the fourth load-bearing: probe `shaderInt64` in `vk::caps`; freeze the wanted-∩-supported intersection in `Device::new`; `ENGINE_ENABLED_CAPABILITIES` becomes that resolved value rather than a `const`, passed as a parameter to `variants::variant_is_loadable`; the resolved set is threaded to the claim path as a parameter — a `OnceLock` read from inside the predicate would implement the ruling as its own counterexample. **Rejected, with costs stated:** static declare-and-fail-session-creation (turns four Phi-3.5 nodes on the CPU EP into no Vulkan EP at all on some devices) and claim-optimistically-decline-at-pipeline-creation (already forbidden — an `EP_FAIL` at translate time takes the whole session down where a decline correctly routes to the CPU EP). **Real cost:** two devices running one binary now have different claim sets, so every run record must publish its resolved enabled-capability set; no claim/decline histogram is comparable across runs without it. **No ledger cost:** a form whose module cannot be created produces no dispatch and no entry, so an entry's existence is itself the witness the capability was enabled. **Scope:** covers `shaderFloat16`/`VK_KHR_shader_float16_int8` and subgroup arithmetic with no amendment; explicitly does NOT cover Switch's spec-constant selectors (fail test (a), remain §8.9.20 territory) — Morpheus's own §8.9.19 carry-forward debt on runtime-chosen specialisation values is not paid by this ruling. **Owners:** Switch (`vk::caps` probe, `Device::new` feature chain), Tank (claim-path parameter thread, run-record capability-set publication), Mouse (`variant_is_loadable` signature and the `_i64` forms it unblocks). No code changed by this ruling — five agents were live in the tree.

### 2026-08-03T19:55:00-07:00: Morpheus — Mouse's and Tank's opposite-looking "unsure" defaults are one rule (the loud-default test); a uniform verdict across a whole input set needs a positive control before it is quotable

**By:** Morpheus, §8.9.21 parts 3–4, deliberately unnumbered (numbering follows citation, per §8.9.18 part 2). **What:** Mouse's missing `spec_digest` *claims* (a fact about a run that has ended, unrepairable) while a missing `source_digest` *declines* (repairable from the tree); Tank's `form_is_provable` answers `true` whenever unsure, documented as a lower bound. Both exist to prevent the same failure — an instrument-side absence emitted as a subject-side finding (R13 amendment 1). **The loud-default test:** when a mechanism does not know, it takes the answer that leaves a trace, not the answer that is nominally conservative — refusal is usually an aggregate a form can disappear inside, the permissive answer is usually itemised and countable; choose the answer a reader can still find tomorrow. Stated limit: the test inverts wherever the permissive answer is *silent* (a frame component claimable with no token/counter/disclosure defaults to refusal instead — why `PROVEN-ELSEWHERE{δ}` is counted and disclosed rather than silently `PROVEN`). **Second finding — "too clean":** a classifier returning the same verdict for its entire input set is evidence about the mechanism, not the subject, until a positive control demonstrates the other polarity through the same predicate; formalised as `UNIFORM(n, verdict)`, not quotable until a named positive control fires. Cheapest ways to defeat the trip-wire, named so they can be checked for: emitting `UNIFORM` and quoting the verdict anyway; satisfying the control with a synthetic input the real path never takes; suppressing `UNIFORM` below some small `n`. **Owner of the tally (how many prior specimens fit this pattern):** Fact Checker, per the standing handover — Morpheus verified one instance directly and declines to assert a count of four. **Owners of the trip-wire:** whoever next builds a classifier over a set — named as Tank's `unprovable_decline_forms` and Link's open-reds screen.

### 2026-08-03T19:55:00-07:00: Morpheus — criterion 10's max-ULP observable measured the fp16 spacing floor, not the kernel; the corrected observable admits int8 of nothing, and a fit's window has a measured magnitude now

**By:** Morpheus, §8.9.22, on Switch's int8 KV error budget. **What:** a max-ULP criterion ranked the fp16 GPU path (337,178 ULP on the logits) worse than every int8 CPU lane, because 18,765 subnormal references (0.45% of the worst tensor) put the max, by construction, at the least-informative values in the tensor — **the degenerate-denominator rule**: a relative measure is undefined where its denominator is degenerate, and a max over a set containing degenerate denominators measures the degeneracy, not the subject. **Ruling 1:** the logits observable now reports two things, never one number — residual over references at or above the smallest fp16 normal, plus the count/fraction below it as a separately published quantity (published, not dropped). **Ruling 2, load-bearing:** under the split observable int8 `per_block32` still sits at 18–22 ULP against the fp16 control's 3 (6–7×) — the fix does not narrow what it was built to widen; it composes with Trinity's float64 result (Vulkan bit-exact, ORT's CPU EP carries the 1 ULP) confirming the oracle, not Vulkan, is the imprecise side. **Not ruled — the silence is not a grant:** whether int8 KV is admitted at all; it needs a MEASUREMENT-class byte figure, not MODEL — the lever ledger's 2.21×/3.17×/4.06× do not reproduce from any artifact in this tree (written down before the first int8 run, per Switch's own provenance discipline), and at the modelled 1.40× int8 is the same order as the arena's already-banked saving while costing a correctness argument the arena did not; that scope call is Morpheus's and not made today. **The window of a fit:** Switch's 8-step slope (1.60 ULP/step) extrapolated to ctx 8192 predicts ~13,000 ULP; run to `past_len 259` it saturates at 29 by `past_len` ~28 — wrong by ~450×, in the direction that would have killed the lever. Named as R11 obligation 1 in a second costume: a fit is a claim about a domain, quotable only inside the window it was taken over. **Recorded, not ruled:** an int8 KV kernel makes the specialisation constant load-bearing for correctness (wrong group size dequantises with the wrong stride and returns plausible wrong numbers) — a `SPEC-DELTA` on that kernel is a correctness event, not a bookkeeping one, landing on §8.9.20's dispatch-time witness. **Owners:** Trinity (observable — comparison and verdict constructors, using Switch's `ulp_residual` unmodified), Mouse (spec-constant witness), Morpheus (the int8 admission scope call, when the measurement exists).

## Cross-agent propagation notes (2026-08-03T19:55:00-07:00)

The following findings were propagated to individual agent history files this run (see each agent's `history.md` for the full note): the deleted-proof incident (three proofs deleted by a merge commit, found only by the op suite, the deleting merge was the coordinator's) → Mouse and Link; Switch's refutation "Phi-3.5 has never been a valid proof subject" (`Shape`, `ReduceSum`, `If` have no Vulkan handler; withholding one form and withholding nine produce the identical refusal) → Tank and Mouse; Switch's `GLSL.std.450` table correction (wrong at slots 30/37/40/43; the silent-miscompute set is `{37, 43}` → `celu`/`hardsigmoid`/`hardswish`, not `relu`) → Niobe, whose weight-amplification measurement is unaffected and needs no re-run; Trinity's "the `Nq/Nkv` caveat was already answered in our own suite" (`test_gqa.py` had G=4 all along; "nobody has run it" was itself an unchecked, repeatedly-requoted claim) → Switch and Fact Checker, both of whom wrote the caveat repeatedly without checking it.

<!-- =============================================================================== -->
<!-- ROUND 13 DECISIONS -- 2026-08-04T12:25:00-07:00 -->
<!-- 4 inbox records read and merged (1 Link, 1 Morpheus, 2 Rai). One consolidation:  -->
<!-- Rai's RAI-012 finding (stale citation) and Link's regeneration/reversion-repair    -->
<!-- of the same artifacts are the same event from two authors (one supplies the       -->
<!-- falsifier, the other supplies the fix and the larger defect it uncovered) --       -->
<!-- merged into a single block below. The remaining three records (Morpheus's         -->
<!-- criterion-10 refutation, Rai's RAI-013/Conv ruling, Rai's arm-E/RAI-014/RAI-015)   -->
<!-- are three independent topics; no further consolidation.                           -->
<!-- =============================================================================== -->

## Round 13 -- 2026-08-04T12:25:00-07:00 -- a second field-level reversion caught by frame, not by census; criterion 10's tolerance found satisfiable everywhere; two new RAI rulings

### 2026-08-04T12:25:00-07:00: Rai & Link (consolidated) — RAI-012's cited artifacts were stale by three hours; regenerating them surfaced a second field-level ledger reversion (115 of 121 `source_digest` values restored), repaired, and given a permanent frame-witness screen

**By:** Rai, Link. **What:** Rai verified RAI-012 (Mouse's ledger-fault message repair) genuinely fixed — rebuilt the crate fresh under WSL, ran the real op suite: 42 `[unproven]` declines, all carrying the corrected message, zero old-message instances — then found the three artifact files the round cited as evidence (`bench/results/link-linux-downstream/{pytest-linux.log,counters-linux.json,verdict-linux.json}`) were committed at `ac4bd0b` (07:12), three hours *before* the fix landed at `7688356` (10:33). **The claim was true; the citation was not.** Link regenerated the three files (WSL Ubuntu, `llvmpipe`, `libonnxruntime_vulkan_ep.so` sha256 `727d0f41a6a31291…`) and the first regeneration came back **50 failed / 372 passed** against the previous round's 8 failed / 633 passed — `gen_proof_ledger.py --check` read 6 `PROVEN-ELSEWHERE{toolchain}` + 115 `SUBJECT-CHANGED`, the exact inverse of the prior session's 115 `PROVEN-ELSEWHERE`. Cause: Mouse's `eee65aa` (the `Conv` commit) regenerated `evidence/proof_ledger.jsonl` from a base predating Link's merge `aea0147`, restoring the withdrawn pre-`normalize_shader_text` `source_digest` on 115 of 121 entries — no key lost, file *grew* 116→122, so **every count-based screen read clean** (key census `0 VANISHED`, loss invariant `0 missing`, shrinking-write guard silent, Windows `--check` PASS because stale-source-with-identical-SPIR-V is `SOURCE-COSMETIC`, a forgiving state). Only the Linux lane, which the same staleness collides into a *declining* state (`115 SUBJECT-CHANGED`), could see it — **the second time this exact sentence has fired: "Windows masked it; only the declining platform could see it."** Repaired with `--backfill-frame --rewitness-source` plus a rebuild (the ledger is `include_str!`'d). After repair: Windows `--check` PASS (121 identical, 0 missing), Linux 9 failed / 676 passed, gate PASS, counters PASS, portability PASS. Built `ci/check_ledger_census.py`'s new **frame-witness arm** and `evidence/proof_rewitness.json` register — any future `source_digest` move onto an entry not proved by the mover must be declared `{revision, who, keys, deliberate, why}`; undeclared move is `FAIL(condition=undeclared_witness_move)`. The `eee65aa` record is entered permanently with `deliberate: false`. Built `ci/check_artifact_frame.py` (a sidecar `artifact-frame.json` per artifact directory: `produced_at_commit`, platform, per-file sha256, optional `subject_sha256`/`subject_paths`) with arms `artifact_predates_subject` (pure `git log`, catches RAI-012's exact class), `artifact_content_moved`, `frame_pins_an_uncommitted_tree`, and opt-in `subject_moved` (off by default on Windows — the `.dll` is not byte-reproducible across forced rebuilds; on by default on Linux, which is). `ci/negative_control_artifact_frame.py`: 11 arms, 1 LIVE / 2 REPLAYED / 8 PLANTED, and the REPLAYED arms reconstruct the real `ac4bd0b`→`7688356` gap and convict it. **Declared known limit** (new `known_limits` section in `ci/open_reds.json`, a category distinct from an accepted red because it is a bounded gap held by the screen's own author, not a red owned by someone else): `subject_paths` is a list the writer typed, not derived from a claims register, so an artifact can be stale w.r.t. a fix in a path nobody named; each known-limit entry names a command that makes the screen admit the limit by token (`--assert-known-limit`) so a quietly-closed limit goes red rather than going silent. **Four framing errors surfaced building the frame-witness arm** (none a wrong-value test, all a wrong scope): (1) a naive "did the value revert" test convicts a repair exactly as loudly as a regression — fixed by asking "did the writer *declare* the move" instead; (2) an unresolvable hand-typed `screened_since` sha made `merge-base --is-ancestor` fail silently for every revision — now guarded by `rev-parse --verify`; (3) `eee65aa` is not a descendant of `aea0147` (forked on `squad/mouse` before the merge), so an ancestry test excused the one event the arm exists for — fixed via position in a `--full-history --topo-order` walk; (4) the revision walk was scoped `--all`, which is not history — hours after a green morning run, the same screen read 28 VANISHED / 102 undeclared moves off `squad/mouse`'s then-unmerged draft; `DEFAULT_SCOPE` is now `HEAD`. **For Mouse:** two non-blocking notes — declare deliberate `source_digest` moves in `evidence/proof_rewitness.json` going forward; `SOURCE-COSMETIC` as a PASS state forgave this exact incident on the one platform CI runs green, which Link flags as arguably wrong without changing it (Mouse's file, real argument either way). **Established:** on the repaired tree, Linux device lane claims nodes, Criterion-10 `MATCH`, `cargo test --lib` 553/0/clean clippy. **Not established:** the regenerated `pytest-linux.log` no longer witnesses the *corrected decline message* specifically (0 declines remain on the repaired tree to carry any message) — the message-level reading Rai took mid-session (42 declines, corrected, 0 old-message) is recorded here in numbers, its log file having been overwritten by the repair rerun; anyone needing that evidence must re-run against `eee65aa`'s tree. **Why:** two independent verification passes on the same claim — one falsifying the citation, one fixing what the citation's staleness was hiding — is the only reason the second, larger defect was caught at all; the citation gap was small, the reversion it led to finding was not.

### 2026-08-04T12:25:00-07:00: Morpheus — criterion 10's tolerance is satisfiable everywhere in fp16; the unsatisfiability finding is refuted without a run; the verdict does not split by mechanism; two new named classes

**By:** Morpheus, §8.9.24. **What:** `np.allclose` tests `|a−b| ≤ atol + rtol·|b|`; for any fp16 normal `ulp(b) ≤ |b|·2⁻¹⁰`, so `allowance/ulp(b) ≥ rtol·2¹⁰ = 20.48` independent of magnitude — **the tolerance is satisfiable at every representable fp16 value with ≥20.48 element-ULPs of margin**; on subnormals the `atol` term alone gives ≥16,777 ULP. The refuted "0.128 ULP-at-scale" figure quoted one term of a two-term sum and divided by the spacing at the tensor maximum rather than at the point the predicate is evaluated; the correct allowance at each tensor's own scale is 33.628 / 29.796 / 32.404 ULP-at-scale on logits / `present.31.key` / `present.31.value` — 263×, 116× and 506× the quoted figure. **Corollary that inverts the reading:** layer 31's key and value fail by more than twenty representable fp16 steps at their own magnitudes, not within one. Criterion 10 stays `DIVERGENT`. **The verdict does not split by mechanism:** the two-mechanism finding is true as a fact about causes, but splitting would move outputs 63/64 out of `OUTSIDE_TOLERANCE` with no element moving, admitting two of three failures and failing §8.9.22's own admits-nothing test. New reporting obligation (owner Trinity): any `ULP-at-scale` residual reports its allowance `atol + rtol·|b|` in the same unit alongside the element-basis failing set. **BLOCKING (owner Trinity):** no motion to change criterion 10's tolerance/unit/predicate/verdict is entertained until outputs 0, 63, 64 have a float64 answer to which side is wrong (Vulkan is bit-exact at the final RMSNorm; ORT's CPU EP carries the 1 ULP — if the reference is the wrong side, every prior tolerance argument was about the wrong question). **THE MOVER IS NOT THE MEASURER — ratified:** a motion to change a criterion may not be authored by the party whose run produced the failure it would relieve, nor in the round that failure was produced; Trinity has obeyed this unprompted for three rounds. **THE SELF-WITNESS BOUND — named** (from Rai's RAI-013 residual: a redirected-but-writable stderr cannot be told apart from a live terminal from the inside — a structural bound, not a repairable defect). **A PREDICTION IS NOT A READING — new class:** the Intel arena refusal was first explained via `alloc_device_frame = SPLIT-DEVICE`, which §6.5 obligation 3 *predicts*; the prediction was read back as an observation, but both polarities actually read `SHARED` — the true discriminant is `alloc_device_frame_allocator_index`. Distinct from R13/R13-amendment-1/§8.9.23(6), which are resolution failures; this one resolves perfectly and changes *modality* in transit. Remedy: provenance classes become `MEASUREMENT`/`MODEL`/`PREDICTION`; a `PREDICTION` may not sit where a `MEASUREMENT` is expected. **Rejected alternatives:** relaxing `atol` (the motivating premise is false); splitting outputs 63/64 into a separate verdict (admits the two failures that prompted it); withdrawing `ULP-at-scale` (sound for its own question, fenced rather than removed); minting a new register number (the remedy is Switch's existing provenance mechanism plus one token). **Why:** an unsatisfiable criterion would be demoted for the same reason an unfalsifiable one is, but the test that tells the two apart — compute the allowance in the format's own units at the point the predicate looks — was run before this was written, not after.

### 2026-08-04T12:25:00-07:00: Rai — RAI-013 discharged (🟢) on Rai's own live probe; Conv (f32) fresh pass clean with one 🟡 disclosure gap

**By:** Rai. **What:** ran `probe_disclosure_reachability.py` herself against a fresh build, both devices, both polarities — all PASS. The default arm reaches the console via a direct, unconditional stderr write bypassing ORT's logger threshold entirely; the escalation arm (stderr forced to fail) re-emits on ORT's own sink at WARNING, witnessed directly. Discharges Tank's Arm D/E falsifier as originally stated without falling into the RAI-008(a) trap (that ruling required a compound criterion met *as a whole* with an untouched defect inside it; this is a single clause, affirmatively measured, no analogous untouched defect). Residual named, not blocking: a redirected-but-writable stderr reads `REACHED_USER` and no process can tell that apart from a live terminal from the inside — a structural self-witness limit, not a repairable defect (owner Morpheus, one doc line in `DESIGN.md` §0.2). **Conv (f32):** no 🔴; the `group`/`strides`/`dilations`/`pads` proof-key breadth gap is real but already self-disclosed in writing by the author in two places before this review asked. Remaining gap: the session-time disclosure a user actually sees prints the raw `ProofKey` with no caveat about the axes it doesn't cover — 🟡 advisory, owner Mouse/Tank. `docs/DESIGN.md` §0 noted as a day stale (omits Conv/MobileNetV2/RAI-012/RAI-013), owner Morpheus. **Why:** verifying independently rather than crediting a description is what discharged RAI-013 correctly and is the same method Link/Rai's consolidated finding above depended on.

### 2026-08-04T12:25:00-07:00: Rai — Tank's RAI-013 arm E submission corroborates, does not reopen; RAI-014 (🟢, false-negative device-loss blindness, independently reproduced) and RAI-015 (🟡 advisory, named 🔴 trigger, `Conv` ledger key denies a shader it carries)

**By:** Rai. **What:** Tank's arm E submission is a third independent instrument converging on THE SELF-WITNESS BOUND Morpheus has since named (§8.9.23(5)) — it corroborates Rai's own prior discharge, does not reopen or move any tally; noted as the third time Tank has declined to rule on his own work (RAI-008(a), RAI-013's original repair, now this), worth crediting as standing precedent. **RAI-014 (new, 🟢 VERIFIED FIXED, independently reproduced):** `check_device_loss.py` was blind to a device loss encoded as UTF-16LE inside a JSON string (NULs serialize as `\u0000`, invisible to a raw-NUL-stripping scan). Rai built her own mutation from the real, unmodified signature record (not Tank's prepared fixture) — pre-fix `PASS`/exit 0, post-fix `FAIL(condition=device_lost_reported)`/exit 1, negative control 20/20 arms firing including both PLANTED and REPLAYED arms for this exact defect. A safety instrument that read clean over a real fault — the sharper failure mode relative to RAI-012 (false negative vs. misattributed true positive), hence its own number. Credited 🟢 because caught, fixed and verified before any downstream claim was built on the blind window (Tank's own withdrawal of the "30-consecutive-clean" figure in the same commit is the sequencing proof). Non-blocking recommendation: this is the third project defect traceable to ORT's UTF-16LE output meeting a narrow-encoding-assuming reader — promote Tank's own rule ("an instrument that has never been shown a positive it does not catch has not been characterised") into `policy.md`/`DESIGN.md`. **RAI-015 (new, 🟡 advisory, named 🔴 trigger):** confirmed live in `evidence/proof_ledger.jsonl` — all four `Conv` entries render the variant component as `metadata` (documented to mean "no shader") while carrying `"shaders":["conv_f32"]` with a real digest in the same record; `registry::form_is_provable` never consults the kernel table for this row, so `form_is_provable` — the gate behind the session-time "proven" claim a user reads — currently lands on a harmless answer only because there is one shader and one truth. **Escalates to 🔴 the moment a second `Conv` variant is registered before repair**, at which point the mechanism would certify an unconsulted shader as proven. Adopts Morpheus's §8.9.23(3) remedy for both the correctness and RAI readings: the variant component must be named by the code that dispatches (`translate`), not by an unpopulated kernel table (owner Mouse, same deadline Morpheus set). **Confirmed Morpheus's `blind_axes` disclosure mechanism (§8.9.23) not yet implemented on fresh `main`** — ruled but not shipped, remains open, owner Mouse. Verification performed: `cargo test --lib` 553/0/4 ignored (independently reproduced), `ci/negative_control_device_loss.py` 20/20 live, `evidence/proof_ledger.jsonl` read directly. **Why:** two of the four items merged this round (RAI-012 and RAI-014) were true in substance but had a verification gap that only an independent re-run — not a re-read of the report — exposed; the pattern is now three-for-three this round for "go to the artifact, not the report of the artifact."

## Cross-agent propagation notes (2026-08-04T12:25:00-07:00)

The following findings were propagated to individual agent history files this run (see each agent's `history.md` for the full note): the field-level reversion class, now observed twice with every count-based screen reading clean both times (Link's 115-of-121, then a second reversion inside the `squad/mouse` merge) → Mouse, Link and Morpheus, all three of whom own history containing a reversion incident; Morpheus's "the screening question is not 'is this true?', it is 'what does it admit?'" → Trinity and Rai, both of whom adjudicate motions against a criterion or a ruling; Trinity's `np.spacing` returning `inf` at fp16's largest finite value (a 504-unit error reading `0.0` ULP) → Niobe and Switch, both of whom compute ULP figures; Switch's "the blocker has no control lane" (ctx-512 blocks nothing; the real blocker is option 1, `DEVICE_MEMORY=1`/`KV_ARENA=0`, established structurally with no rate) → Tank, whose `closes_when` requirement it changes; Rai's "the claim is true; the citation is not" → Link, with Morpheus's §8.9.23(6) ruling that a citation is a proof key with no subject digest — cite a state, not a path.

<!-- =============================================================================== -->
<!-- Archive-gate check, 2026-08-04T20:25:00-07:00 (Scribe). File was 55,899 bytes    -->
<!-- pre-check -- over the 51,200-byte Tier-2 gate. All rounds present (Round 10 @    -->
<!-- 2026-08-03T04:55 through Round 13 @ 2026-08-04T12:25) are within ~1.6 days of    -->
<!-- CURRENT_DATETIME -- zero entries are age-eligible under either the 30-day or     -->
<!-- 7-day tier. Declining to archive again this round, as last round, and recording  -->
<!-- the breach as a judgement rather than letting the byte count force a cut: the    -->
<!-- gate exists to bound size, not to guarantee a cut every round it is exceeded.    -->
<!-- =============================================================================== -->

<!-- =============================================================================== -->
<!-- 6 inbox records read and merged (1 Coordinator, 3 Morpheus, 2 Mouse). One        -->
<!-- consolidation: the Coordinator's README-provenance-over-broad filing is refuted  -->
<!-- by Morpheus's gemm-transpose filing part C (the "76" figure is sourced correctly -->
<!-- to the dump -- the dump's own schema spells `live` twice with two meanings) --   -->
<!-- merged into a single block below rather than recorded as two separate readings  -->
<!-- of the same number. Morpheus's other two records (criterion-10 tolerance ruling, -->
<!-- README re-derivation + two-retirement-registers defect) and Mouse's two records  -->
<!-- (`dispatches_executed` metric change, `Reshape` shipped-claims-nothing) are five -->
<!-- independent topics; no further consolidation.                                   -->
<!-- =============================================================================== -->

## Round 14 -- 2026-08-04T20:25:00-07:00 -- criterion 10 stays DIVERGENT with its cause fully characterised; a schema spelling `live` twice refutes its own provenance complaint; two ledger registers read the same 43 keys as opposite verdicts; `dispatches_executed` replaces `claimed_nodes` as the headline metric

### 2026-08-04: Coordinator & Morpheus (consolidated) — the README provenance-over-broad filing is refuted: the dump does carry the kernel fact, under a boolean that spells the same word as the status token it sits beside

**By:** Coordinator, Morpheus (§8.9.25 part C). **What:** Coordinator found README's "94 rows, of
which 76 carry a kernel — read from `epctl --dump-capabilities --json`" sources two numbers to a
command whose dump has no `kernel` field, only `dtypes`/`live`/`name`/`opsets`/`schema_baseline`/
`staged_reason`/`status`, and filed the 76 as sourced-but-unverifiable (severity low, owner
Morpheus). Morpheus ran the same dump and found the boolean `live` is `true` on exactly 46+30=76
rows and `false` on all 18 staged — **the dump does carry the kernel fact, under the name `live`.**
The citation is sound; the schema is not: one JSON row spells `live` twice with two denotations —
a status token (`OpStatus::Live`, granting nothing) and a boolean meaning "this row has a kernel."
**Ruling: rename the boolean `has_kernel`; `status` keeps its three tokens** (owner Tank for
`epctl`, Mouse for the registry serialiser); until then `DESIGN.md` §0 and `README.md` state the
derivation inline. Also corrected in passing: §0's op count is 94/76/46-30-18, the fifth wrong
reading of that integer, moved by the `MatMul` and `Gemm` rows exactly as predicted. **Why:** a
true sentence whose check requires already knowing that one word means two things is checkable
only by someone who already knows the answer — the same defect class as a citation with no subject
digest, arriving through a shared field name instead of a stale path.

### 2026-08-04: Morpheus — criterion 10 stays OPEN/`DIVERGENT` with a fully characterised cause; an AGREE bounds only the difference, never the distance from truth; a permissive close condition caught before it fired

**By:** Morpheus (§8.9.25). **What:** Trinity's model-scale float64 oracle discharges §8.9.24(4)'s
block (motions may now be *made*, not granted): output 0 → `cpu` is the further side from true (83
vs 70 element-ULP, unanimous on five discriminators); outputs 63/64 → `direction: null` — a
direction for one output is not a budget for three. The depth series localises the cause: flat
0-2 ULP for layers 0-30, sole exceedance layer 31 key/value at 4 ULP (outputs 63/64). Own close
condition ("closes when the ULP series exists and is flat or has a located step") ruled against as
written-permissive-meant-unblocking — new drafting rule: a close condition declares `SUFFICIENT`
or `UNBLOCKING`, undeclared reads as sufficient. **AN AGREEMENT BOUNDS ONLY THE DIFFERENCE, NEVER
THE DISTANCE FROM TRUTH:** the model-scale "~6x common error" is the logits' ratio, not the
model's — `present.31.key` is 3.0x, `present.31.value` 1.5-1.75x; a shared error cancels in a
difference, so the finding weakens an AGREE and leaves a DISAGREE exactly where it stood. `Gemm`'s
`transA`/`transB` stay out of the proof key (one pipeline, ternary on a push constant) but the
*record* is blind, not the axis: `transB=1` cases mint the same key as `transB=0` siblings, so a
reader cannot tell a key proven at both values from one proven at one (remedy: non-key
`witnessed_at` field, owner Mouse). Separately caught a skip reason naming a refusal that never
happened: the one test able to tell a transpose from a relabelling lacks the `require_vulkan`
fixture its fifteen neighbours carry, silently falls back to CPU, and reports "EP did not claim"
when the EP was never loaded. **Why:** the screening question is not "is this true?" but "what
does it admit?" — both the criterion-10 close condition and the skip-reason string were true readings
that admitted the wrong thing.

### 2026-08-04: Morpheus — `README.md` re-derived from artifacts, three claims found false rather than stale; two retirement registers read the same 43 keys as opposite verdicts

**By:** Morpheus. **What:** Rewrote `README.md`'s status block from artifacts at `4ee8c9c`,
naming the commit and artifact per figure. Three claims were flatly false: "zero Phi-3.5 nodes
currently claimed" against **355 of 366 claimed in one island**; "the Linux lane has never executed
a claimed node" false since 2026-07-30; "Windows and Linux are verified in CI" while **CI has been
red on `main` for five pushes**. M0 verdict unchanged (5 of 12 criteria met, "early implementation"
stands) — only the evidence moved. **Separately, found while re-deriving:** `ci/check_ledger_census.py`
exits 1 reporting `172 ever proven = 129 present + 0 retired + 43 VANISHED`, but the 43 keys were
retired *deliberately* by the §8.9.23 schema change and are recorded in
`evidence/retired_proof_keys.json` (a list, read by `gen_proof_ledger.py`) — `check_ledger_census.py`
instead reads `evidence/proof_retired.json` (an object, with `owner`/`date`/`reason`), **which does
not exist.** Two registers, one screen, and neither file is wrong on its own terms; the census's own
13/13 negative-control arms plant and replay deletions but none exercises a retirement, so the
register the exemption depends on was never read by a green run. Ruling: there must be one
retirement register read by both tools, plus a control arm that retires a key and asserts the
census acquits — not deciding which filename/schema wins (tool owners' call: Mouse, Tank). **Why:**
the §8.9.24 screening question applied to a screen itself — not "is this true" (both files are)
but "what does it admit" — and two registers with one screen admits exactly the event the screen
exists to catch.

### 2026-08-04: Mouse — `dispatches_executed` replaces `claimed_nodes` as the headline coverage metric; the counterfactual instrument re-baselined per-node

**By:** Mouse. **What:** `probe_model_op_census.py` now headlines `dispatches_executed`
(`claimed_nodes` printed as `(upper bound)`), prints `not-measured` rather than `0` when nothing
ran, and reports `ERROR(instrument)` rather than a row of zeros when a session cannot be created at
all. Measured: BERT claims 481, executes **4**; MobileNetV2 98/97; Phi-3.5 355/355; gpt-oss-20b not
measurable (ORT 1.28.0 CPU `QMoE` refuses session creation). The two metrics agree exactly on the
two models that form one island, which is why the defect survived unnoticed until BERT.
`probe_island_counterfactual.py` re-baselined per-node (not per-op-type) and now reports an
optimistic/gated bracket: BERT `MatMul`+`Reshape` optimistic +184/27 islands, **gated +3/30
islands** — the previously-quoted "738 nodes in 17 islands" was the optimistic column of an
optimistic-baseline instrument. **Consequence: there is no next op-registration pick on BERT** —
the gated ceiling over every unregistered op is +3 nodes; the real blocker is that ORT infers no
ranks through its `Shape`/`Cast`/`Concat` chain (724/1274 rows read `static` from nothing). **Why:**
a metric correct on the models you look at most and wrong by 120x on the one you don't is worse
than a metric that is always wrong; every screen that can PASS should state what it read to pass.

### 2026-08-04: Mouse — `Reshape` ships, is proven, claims zero nodes on the current census; two structural blocks named, both measured

**By:** Mouse. **What:** `ai.onnx::Reshape` ships in `rust/src/ops/shape.rs` as a copy (not an
alias — `bind_aliased_output` only fires for external plan I/O, and an aliased interior `Reshape`
would create two live tensors on one allocation against a generation-stamped quarantine-on-free
allocator; Switch's KV-disjointness argument does not transfer, since his hazard requires a write
this op never performs). Claims zero nodes on every census model. Two structural blocks, both
measured: (a) no inferred output rank — 58 of BERT's 71 `Reshape` nodes take their shape from a
runtime `Cast`/`Concat`/`Shape` chain, and `read_const_i64` returns `None` in all four tree
implementations; (b) no output descriptor for a free axis — a `[-1,4]` case claimed, then
`dispatch_ort` refused "output has no declared shape" at `Compute()`, a broken commitment rather
than a decline. New standing rule: a gate must refuse what translate cannot *complete*, not what it
might dislike. Rank-0 discriminator settled by arithmetic (a rank-0 input holds exactly one
element) rather than a per-op minimum-rank table, after Mouse's own prior vacuous-rank-0 defect
reproduced itself in this new code one round later. `Flatten` stays declined, now measured (BERT
71/0, MobileNetV2 1/0, Phi-3.5 0/0). **Why:** it ships anyway because a row that declines honestly
with a naming histogram, backed by a positive control proving the claim path executes and agrees,
converts "we don't know why it doesn't run" into a counted, coded answer.

## Cross-agent propagation notes (2026-08-04T20:25:00-07:00)

The following findings were propagated to individual agent history files this run (see each
agent's `history.md` for the full note): Mouse's `claimed_nodes` != `dispatches_executed` (every
coverage figure quoted against `claimed_nodes` alone is affected; `dispatches_executed` is the
honest metric) -> Switch, Link and Morpheus; Switch's "the 6144/8192 figures were never the
shipping lane's" -> Niobe, who holds the roofline and context-length record; Trinity's common-error
finding (both EPs ~6x further from true than from each other at model scale) -> Morpheus and Rai,
bounding what an AGREE means; Tank's "loudly logged, silently returned" plus the
`disable_cpu_ep_fallback` hole -> Rai and Link; `cargo fmt --check` re-broken by three consecutive
merges -> Link, as a CI-discipline item.
