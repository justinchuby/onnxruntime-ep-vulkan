name: CI

# Triggers: every push to main and every pull request.
on:
  push:
    branches: [main]
  pull_request:

# ------------------------------------------------------------------
# Version pins — change here, nowhere else.
# ------------------------------------------------------------------
# ORT_VERSION: compile and test against ORT 1.28 (released 2026-07-24).
#   DO NOT use 1.27: it contains a null-allocator PrePack bug and a
#   deleter lifetime issue in the plugin-EP path, fixed in 1.28.
#   Source: .squad/decisions/inbox/fact-checker-ort-1.28-api.md
#
# MESA_VERSION (Windows only): lavapipe via mesa-dist-win.
#   mesa3d-{ver}-release-msvc.7z ships lavapipe (Vulkan 1.3 CPU
#   software rasterizer) as a self-contained Windows DLL + ICD JSON.
#   Source: https://github.com/pal1000/mesa-dist-win (26.1.3 = latest
#   stable as of 2026-07-28).
# ------------------------------------------------------------------
env:
  ORT_VERSION: "1.28.0"
  MESA_VERSION: "26.1.3"
  # LunarG SDK version used on both Linux (apt repo) and Windows (installer).
  # The Linux apt repo uses a three-part version (strip the trailing .0).
  VULKAN_SDK_VERSION: "1.3.296.0"
  # ONNX minimum version — must match the floor in tests/requirements.txt.
  # onnx 1.22 contains a WRONG Attention-24 reference implementation (Justin Chu,
  # ONNX owner, confirmed: fix correct semantics, no opset bump). The fixed semantics
  # will ship in a forthcoming onnx release; this pin prevents silent oracle drift.
  # TODO(Fact Checker): update to the fixed-semantics release once confirmed.
  ONNX_MIN_VERSION: "1.22.0"

# ------------------------------------------------------------------
# CI lanes:
#   format          : rustfmt check (ubuntu, fast, no build)
#   lane-checks     : two-polarity self-test of the gate readers in
#                     ci/ — no Vulkan, no GPU, no ORT session
#   build-test-linux: cargo build + clippy + op tests (lavapipe)
#                     + validation layers (zero errors enforced)
#                     + layering lint + CRITERION 10 GATE
#   build-test-windows: cargo build + clippy + op tests (lavapipe
#                     via mesa-dist-win) + no-ICD fallback test
#                     + validation layers via LunarG SDK
#                     + CRITERION 10 GATE
#
# WHAT A GREEN LANE MEANS HERE, AND WHAT IT MEANT BEFORE 2026-07-31:
#   Before: the pytest process exited zero. That is all it meant, and
#   on 2026-07-30 it was true for most of a day while this EP executed
#   ZERO nodes and ORT silently re-ran everything on CPU.
#   Now: the lane additionally carries a gate artifact (gate_chain_fp32,
#   docs/PLATFORMS.md §7.8) whose pass condition includes the
#   model_output_equivalence VERDICT — so a run that did not measure
#   reports UNMEASURED, a run in which this EP executed nothing reports
#   UNATTRIBUTED, and neither of those is a pass. Each lane also runs a
#   NEGATIVE CONTROL that removes the ICD and requires the gate to go
#   red, because a check observed only to pass is not known to be a check.
#
#   `continue-on-error` appears NOWHERE in this file, and no verdict-bypass
#   flag does either (one exists for local debugging; it is deliberately not
#   spelled out here so that a grep for it in this file finds nothing).
#   Absence of a check is a refusal, not a default green — the same
#   principle bench/admissible.py carries for Niobe.
# ------------------------------------------------------------------

jobs:

  # ----------------------------------------------------------------
  # 1. Format check (fast, no build, no Vulkan SDK needed)
  # ----------------------------------------------------------------
  format:
    name: rustfmt check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust toolchain (rustup, no third-party action)
        run: |
          rustup toolchain install stable --profile minimal --component rustfmt --no-self-update
          rustup default stable
          cargo fmt --version

      - name: Check formatting
        # The rust/ crate may not exist yet if Tank hasn't landed it.
        # Exit 0 with a warning rather than breaking CI on a missing crate.
        run: |
          if [ -f rust/Cargo.toml ]; then
            cargo fmt --manifest-path rust/Cargo.toml --all -- --check
          else
            echo "::warning::rust/Cargo.toml not found — format check skipped (crate not yet landed)"
          fi

  # ----------------------------------------------------------------
  # 2. Lane-check self-test (no Vulkan, no GPU, no ORT session)
  # ----------------------------------------------------------------
  # The lanes below gate on ci/check_verdict.py and ci/check_fatal_log.py.
  # Those are instruments, and R9 says evidence scales only with
  # falsifying instruments — so the instruments themselves need a
  # falsifier, and it must run where the thing they check is broken
  # or absent. Hence: synthesised verdict records and synthesised
  # logs, both polarities, asserting the printed TOKEN and not only
  # the exit code (R13: two different things spelled the same way
  # are not a signal).
  #
  # This job needs no Vulkan and no GPU on purpose. If it needed the
  # subject under test to be healthy, it could not tell us anything
  # on the day the subject is sick, which is the only day it matters.
  # ----------------------------------------------------------------
  lane-checks:
    name: Lane-check self-test (two polarities, no GPU)
    runs-on: ubuntu-latest
    # Narrowest scope, declared HERE rather than at workflow level: this is the only job
    # in this workflow that asks GitHub anything (the `main_is_green` register entry and
    # its own negative control, both of which shell out to `gh run list`). Adding this
    # block resets the job's default token to exactly these two scopes and no others —
    # `contents: read` for actions/checkout, `actions: read` for `gh run list` (workflow
    # run history, not code, not issues, not packages). The other three jobs in this file
    # (format, build-test-linux, build-test-windows) never call `gh` and are deliberately
    # left OFF this block so they keep whatever this repository's default token policy is,
    # rather than being broadened to match a need that is local to this one job. See
    # ci/check_gh_auth.py, written for issue #16.
    permissions:
      contents: read
      actions: read
    steps:
      # fetch-depth: 0 (full history), not the default depth-1 shallow clone.
      # This job runs every REPLAYED negative-control arm that reads a historical
      # commit with `git show <ref>:<path>` — 607056a (negative_control_build_
      # precondition.py), 133b9fe (negative_control_open_reds.py), 8a851f8
      # (negative_control_readme_usage.py), eb84364/26fd93f (negative_control_
      # ledger_census.py). A depth-1 checkout makes every one of those commits
      # unreachable, so `git show` fails, the REPLAYED arm reports UNOBSERVABLE
      # rather than firing, and the negative control counts that as a failed arm
      # (ci/negative_control_build_precondition.py:historical_workflow: "a control
      # that cannot read its subject must say UNOBSERVABLE rather than quietly
      # dropping an arm") — red for a reason that has nothing to do with the
      # product. Full history costs nothing here: this job builds no code and
      # needs no Vulkan SDK.
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install test dependencies
        # `pytest onnx numpy` alone is not enough: `harness_census_drift`'s registered
        # command (tests/ops/test_harness_census.py::test_census_baseline_has_no_drift,
        # ci/open_reds.json) collects through tests/ops/conftest.py, which does an
        # unconditional `import onnx_ir as ir` / `import onnxruntime as ort` at module
        # scope for every test under tests/ops/ -- including this one, which itself uses
        # neither. Without them collection fails with `ModuleNotFoundError: No module
        # named 'onnx_ir'` before a single assertion runs, and check_open_reds.py reports
        # `FAIL(condition=unaccounted_red)` for a census that is not actually drifting
        # (rust/tools/audit_instruments.py --check is green).
        #
        # Install from tests/requirements.txt -- the one authoritative floor list --
        # the same way the Windows build-test job's "Install Python test dependencies"
        # step does, instead of hand-copying a second (or third) package list here.
        # This job creates no Vulkan device and no ORT session, so it needs no
        # ORT_VERSION-exact pin (unlike the Linux build-test job, which links against a
        # provisioned ORT_HOME and does pin exactly) -- the requirements.txt floor is
        # sufficient for "does this import resolve at collection time".
        run: pip install -r tests/requirements.txt

      - name: Lane inventory against the COMPOSED workflow, not this branch's view
        # On 2026-08-02 the same shape occurred five times in one day, in four subsystems
        # and three languages: two correct branches composing into a broken whole. Mine
        # was this file -- my inventory was complete for the workflow I could see, and
        # Switch's step was complete on the branch he could see. No command either of us
        # could have run on our own branch would have shown it, because each branch was
        # genuinely fine. Our discipline verifies branches; the defects live in unions.
        #
        # This classifies the UNION of step names on both sides. On a pull_request event
        # the checkout is already the merge commit and this is belt-and-braces; on a push
        # to a branch it is the only thing that looks at the other side at all.
        #
        # --union-required, deliberately: if the reference cannot be read, the check
        # silently degrades to the branch-only view it exists to replace, which would be
        # green for exactly the reason it was written to stop being green for.
        #
        # NO `--depth` HERE — ISSUE #28. This used to be `git fetch --no-tags --depth=1
        # origin main`. That single flag reads as "keep this one lookup cheap", and on a
        # job that already declares `fetch-depth: 0` above it should be a no-op — but a
        # depth-limited fetch marks its OWN graft boundary regardless of what history the
        # repository already holds, so it shallow-marked `origin/main`'s tip anyway. On a
        # `pull_request` event the checkout IS `refs/remotes/pull/N/merge`, GitHub's
        # synthetic two-parent merge of the PR head into the CURRENT tip of `main` — so
        # that graft point was always exactly one of HEAD's own two parents, and
        # `git rev-parse --is-shallow-repository` went true for the WHOLE checkout.
        # `ci/check_ledger_census.py`'s own history-completeness guard (correctly, and
        # deliberately not weakened here) then refused to answer at all, and the register
        # in `ci/open_reds.json` reported it `FAIL(condition=unaccounted_red)` on every
        # PR. The lookup this step needs is one already-known ref, so a plain fetch (no
        # depth) costs nothing new to download and grafts nothing.
        run: |
          git fetch --no-tags origin main
          python ci/check_lane_inventory.py \
            --workflow .github/workflows/ci.yml \
            --union-with FETCH_HEAD \
            --union-required \
            --summary "$GITHUB_STEP_SUMMARY"

      - name: Verdict vocabulary preflight (which kind of instrument outage?)
        # This job's tests fail COLLECTION when tests/ops/_verdict.py is missing, which
        # is correct (a skip reports the same green as a pass) and unreadable on its own:
        # a collection error looks the same whether the vocabulary is legitimately absent
        # from this checkout or the job is broken. This step answers that first, by
        # token, and puts the answer in the job summary.
        run: python ci/check_vocabulary.py --github-summary

      - name: Two-polarity tests for ci/ lane checks
        # The command is ASKED FOR, not written here. It was written here once, and that
        # copy did not carry the three `--deselect`s that `lane_checks_suite` carries in
        # ci/open_reds.json — so the three census-extent tests, which the register accepts
        # as red with named owners (Mouse, Switch, Tank) and a closes_when, failed this
        # step on every push to main for ten consecutive pushes. The register had done its
        # job; a second, register-blind spelling of the same command undid it. `--emit-cmd`
        # makes the register the only place that command exists.
        run: |
          set -o pipefail
          mkdir -p bench/results/ci-lane
          cmd="$(python ci/check_open_reds.py --emit-cmd lane_checks_suite)"
          echo "register-declared command: $cmd"
          eval "$cmd" -p no:randomly 2>&1 | tee bench/results/ci-lane/lane-checks.log

      - name: Two-polarity suite asserted something (productivity floor)
        # always(): the value of this step is highest exactly when the step above went
        # red, because "113 failed" and "0 collected" are the same red on the step above
        # and completely different findings. This job certifies every other check in ci/;
        # if it ever reports success for collecting nothing, every green downstream of it
        # is uninterpretable.
        if: always()
        run: python ci/check_suite_productivity.py
          --suite ci/test_lane_checks.py --lane lane-checks
          bench/results/ci-lane/lane-checks.log

      - name: Suite-productivity negative control (demand red on a suite that did nothing)
        # R9: a guard never seen failing has no demonstrated positive state. Every
        # condition and every instrument path of check_suite_productivity.py fires here,
        # including the two LIVE arms from a scratch virtualenv really built without the
        # optional onnx-shape-inference package -- red before the collection-time import
        # was made lazy, green after, same environment both times.
        run: python ci/negative_control_suite_productivity.py

      - name: Build-precondition screen (a lane that could not build must not report)
        # The general case of the BUILD_SKIPPED defect, screened statically so it fires
        # on the pull request that introduces the next one rather than on the incident
        # that reveals it. BP1: no step may write a gating env name and exit 0 in the
        # same script. BP2: no `if: env.NAME` may read a name nothing writes -- a dormant
        # guard is re-armable by one line. BP3: a `Build *` step must assert its own
        # artifact exists.
        run: |
          python ci/check_build_precondition.py \
            --summary "$GITHUB_STEP_SUMMARY" \
            .github/workflows/ci.yml .github/workflows/conformance.yml

      - name: Build-precondition negative control (demand red on each rule)
        # Includes a REPLAYED arm: the real `BUILD_SKIPPED=1; exit 0` text read out of
        # this repository's own history at 607056a, not a plant.
        run: python ci/negative_control_build_precondition.py

      - name: PowerShell exit-status screen (a verdict step must consume its own code)
        # Issue #49: both Windows "Gate negative control" steps captured
        # `ci/gate_chain_fp32.py`'s intentionally non-zero exit into `$code`, printed the
        # verdict with `Write-Host` (which never touches $LASTEXITCODE), and fell
        # through with no `exit`. GitHub's generated pwsh wrapper then appended
        # `if ((Test-Path variable:\LASTEXITCODE)) { exit $LASTEXITCODE }`, so the step's
        # real exit code stayed pinned to the gate's deliberately non-zero $code even
        # when the step's own log said PASSED. This is a static screen over the workflow
        # text for the general shape -- a step that captures $LASTEXITCODE into a
        # variable and does not end its script by explicitly consuming it -- so it fires
        # on the pull request that introduces the next instance rather than on the
        # incident that reveals it.
        run: |
          python ci/check_powershell_exit_status.py \
            --summary "$GITHUB_STEP_SUMMARY" \
            .github/workflows/ci.yml .github/workflows/conformance.yml

      - name: PowerShell exit-status negative control (demand red on the real shape)
        # Includes a REPLAYED arm: the real stale-exit-code text read out of this
        # repository's own history at the commit issue #49 was filed against, not a
        # plant, and it asserts BOTH known offending steps are named -- the one issue
        # #49 was filed against and its sibling, which carries the identical shape and
        # had simply never been exercised because Windows' ICD suppression had never
        # taken until PR #45.
        run: python ci/negative_control_powershell_exit_status.py

      - name: Flake-witness negative control (demand red on an intermittent)
        # A flake witness on a repo with no reproducing flake is silent, and silence from
        # a check is indistinguishable from silence from no check. This control makes it
        # go red on demand and — just as importantly — makes it STAY GREEN on the three
        # shapes that look like a flake and are not: a regression across commits, a
        # portability difference across lanes, and a test that stopped running.
        run: python ci/negative_control_flake_witness.py

      - name: Test-lock auditor (process-global families, static, no GPU)
        # Which #[test] fns touch a process-global without holding ledger::test_lock():
        # the counters statics, the ORT logger pointers, and — since Round 37 — contended
        # environment variables. This is the cheap static half of the contention gate that
        # the build lanes run: it names the offending test and file:line in milliseconds,
        # where the repeat gate needs 20 cargo invocations to make the same test go red.
        #
        # --selftest first, and it is not decoration: an auditor never shown to fire is
        # indistinguishable from a blind one, and this one's first version WAS blind twice
        # (a hand-written two-file list, then a whole-file test module it never scanned).
        run: python rust/tools/audit_counter_test_lock.py --selftest --check --pairs
      - name: Contention-gate failure extractor (replay a real red, demand names)
        # The 2026-08-03 incident had two halves and this is the second one: the red was
        # real and its test name was lost to a `Select-Object -Last 2`. This replays a
        # committed real red capture through the gate's extractor and requires the two
        # failing test names and a panic site back out — and requires the tail-2 slice of
        # that same capture to yield nothing, which is the incident, reproduced.
        run: python rust/tools/contention_gate.py --selftest --list

      - name: Open-reds negative control (demand each rule red on purpose)
        # 39 arms, and the REPLAYED one is not a plant: it takes the real ci.yml bytes at
        # 133b9fe -- the merge that reintroduced four dormant `BUILD_SKIPPED` guards --
        # and requires the register to call them an UNACCOUNTED red. The same rule over
        # today's bytes is green, so it is not a constant.
        #
        # Its LIVE arm runs the real register end to end, which includes `main_is_green`
        # -- and that entry shells out to `gh run list` (ci/check_main_is_green.py). PR
        # #13 run 31052604259 hit exactly this: this step had no GH_TOKEN, so `gh` refused
        # with "set the GH_TOKEN environment variable", check_main_is_green.py correctly
        # reported ERROR(instrument=github_unreachable), and that did not match the
        # register's accepted signature for `main_is_green` (FAIL(condition=main_is_red)),
        # which failed the LIVE arm for a reason that has nothing to do with any rule
        # under test here -- an instrument-configuration failure wearing a product-verdict
        # coat. Same token as the "Open reds" step below, minted for this job only (see
        # the job-level `permissions:` block): `actions: read` is all `gh run list` needs.
        env:
          GH_TOKEN: ${{ github.token }}
        run: python ci/negative_control_open_reds.py

      - name: Proof-ledger census (has a proof ever gone missing?)
        # `gen_proof_ledger.py --check` asks whether every entry in the ledger agrees with
        # the build. It cannot ask about an entry that is no longer there. On 2026-08-03
        # Tank found three Cast proofs committed by 26fd93f and absent from main: the
        # deletion happened inside a merge conflict resolution (eb84364), and `git log -S`
        # on the ledger found the addition and no removal, because history simplification
        # hides the proving commit from the file's own log -- 13 revisions simplified vs
        # 55 with --full-history. --check stayed green, the shrinking-write guard covers
        # writes by the TOOL and a merge is not one, and the only instrument that noticed
        # was the op suite, red and unaccounted since that merge.
        #
        # This is `N ever declared = M ruled on + K retired` (check_open_reds.py's frame)
        # applied to proofs, with the denominator taken from git history rather than a
        # tracked file -- because the failure being detected is a merge rewriting tracked
        # files, and a denominator the merge can edit is not a denominator.
        run: python ci/check_ledger_census.py --json bench/results/ledger-census.json

      - name: Proof-ledger census negative control
        # 13 arms, 2 LIVE / 5 REPLAYED / 6 PLANTED, ratio printed. The REPLAYED arms are
        # not plants: they run the screen against eb84364 and 26fd93f as they really are
        # and require conviction of the first and acquittal of the second. One arm exists
        # only to assert that --full-history is load-bearing, by the revision counts,
        # because without it the replay reported PASS on the merge it was written to
        # convict.
        run: python ci/negative_control_ledger_census.py

      - name: Verification-subject screen (does a check verify itself?)
        # Mouse's sweep, wired here because it arrived in ci/ at 2832526 in NO workflow
        # step. ci/check_lane_inventory.py could not see that: it asks whether every
        # workflow step has a Check, not whether every check has a step, so an unwired
        # tool is invisible to the coverage census by construction. CHECKED 22,
        # classified 22, FOUND 0 SELF (13 ARTIFACT, 9 EXTERNAL) — after
        # gen_proof_ledger.py --check stopped comparing the ledger against itself.
        run: python ci/check_verification_subjects.py

      - name: Open reds (every guard is the colour the register declares)
        # THE POINT OF THIS STEP. Three checks were handed to me that fail in main's own
        # checkout -- audit_instruments --check, ci/test_lane_checks.py, tests/union_check
        # --run -- and every one of them was already printing its failure in full. None
        # was wired to anything that reads a colour. They were not silently passing; they
        # were loudly failing into a void, which is the same defect wearing a louder coat.
        #
        # This does NOT turn them green. Two of the five accepted reds are TRUE and
        # closing them would mean baselining an open item. It makes an accepted red
        # structurally different from a new one: a red with no entry fails the lane, an
        # entry whose check went green fails the lane (good news, delete it), and an
        # accepted red is ANNOTATED into the merge UI with its owner and the event that
        # would close it -- where a reviewer sees it and a truncated log cannot eat it.
        #
        # There is no flag that suppresses a failure. Adding one would recreate the void.
        env:
          # `main_is_green` asks github.com for the colour of the default branch, so this
          # step needs a token. Without one it returns ERROR(instrument=github_unreachable),
          # exit 4, which is honest but useless -- and "the instrument could not reach its
          # subject" is exactly the state that let ten red pushes go unremarked.
          GH_TOKEN: ${{ github.token }}
        run: python ci/check_open_reds.py --summary "$GITHUB_STEP_SUMMARY"

      - name: GH-invocation auth screen (every API-using `gh` call has a token path)
        # Issue #16's static half. The two steps above needed a human to notice that
        # `gh run list` has no token in scope; this exists so the NEXT one does not need
        # a human. It reads .github/workflows/*.yml for every step whose script calls the
        # `gh` CLI with a subcommand that talks to the API (`run`, `api`, `pr`, `issue`,
        # `release`, `workflow`, `repo`, ... -- not `--version`/`help`/`completion`), plus
        # every step that invokes a ci/*.py script which itself shells out to `gh`,
        # directly or by way of ci/open_reds.json's registered commands (`negative_control
        # _open_reds.py` -> `check_open_reds.py` -> the `main_is_green` entry's cmd ->
        # `check_main_is_green.py` -> `gh run list`, which is the exact chain that had no
        # token). For each one it requires GH_TOKEN or GITHUB_TOKEN to be declared in an
        # `env:` block this step can actually see -- workflow, job, or the step itself,
        # recognising both the block and inline YAML forms of `env:` semantically rather
        # than by line shape (issue #21: the inline form was being falsely convicted) --
        # and fails naming the step when none is. It only reads env KEY names, never a
        # value, so it cannot print a token even by accident.
        #
        # Issue #21: pointed at the WHOLE .github/workflows directory, not two files
        # named on the command line, so a workflow added later is screened from the day
        # it exists rather than the day someone remembers to extend this line -- and a
        # zero-`gh`-reaching-subject frame (this screen pointed at the wrong scope, a
        # subdirectory, or a stale path) is a loud ERROR(instrument=
        # zero_gh_reaching_subjects) by default, never a silent PASS.
        run: python ci/check_gh_auth.py .github/workflows

      - name: GH-invocation auth negative control (demand red without a token, never a skip)
        # LIVE arm proves the thing issue #16 actually asked for: with GH_TOKEN and
        # GITHUB_TOKEN stripped from its environment, ci/check_main_is_green.py -- the
        # exact process this job runs that calls `gh run list` -- must exit
        # ERROR(instrument=github_unreachable), exit 4, and must never print PASS and
        # never exit 0. "gh absent, unauthenticated or offline" collapses to one polarity
        # here on purpose: whichever of the three is true on a given runner, the answer a
        # reader gets must be "unread", never "clean". REPLAYED arm points the screen
        # above at this repository's own ci.yml as it stood at b1886d9 -- one commit
        # before this fix -- and requires FAIL(condition=missing_token_path) naming the
        # Open-reds negative control step; the same screen over today's bytes is PASS, so
        # it is not a constant. Issue #21 added: the whole workflows directory still
        # passes via directory expansion; screening only conformance.yml (which has zero
        # real gh-reaching steps) is ERROR, never a silent PASS; the inline `env: {...}`
        # form satisfies the token check exactly like the block form; a wrongly-named
        # env key does not; a workflow with steps but zero gh-reaching subjects is ERROR
        # by default and PASS only with --allow-empty-frame; an empty or mis-scoped
        # directory is ERROR; and directory expansion recurses into subdirectories so a
        # nested workflow cannot hide from a broader invocation. The rest are the
        # original PLANTED arms: a synthetic `gh api` call with no token anywhere, a
        # token declared in the wrong job, a non-API `gh --version` call that must NOT be
        # flagged, and a step that reaches `gh` only through another script.
        run: python ci/negative_control_gh_auth.py

      - name: Tautological-assertion screen (no GPU, whole tree)
        # Scans Rust and Python sources for assertions that pass without reading their
        # subject. It reports 0 detections over the tree today and exists as a regression
        # barrier, not a discovery tool -- the two assertion defects that actually
        # occurred here are outside its reach and its own output says so. Its evidence
        # that it works is entirely planted, in the step above.
        run: python ci/check_tautological_assertions.py
      - name: Tick-conversion screen (static, no GPU, no vocabulary)
        # The 52x defect class: a code path that reads a device tick as a nanosecond.
        # Neither of our other instrument families can see it. Unit tests prove the
        # conversion correct WHERE CALLED and say nothing about whether every path calls
        # it; and no device lane on earth that this CI can reach will show it, because
        # timestampPeriod is 1.0 on lavapipe, on SwiftShader and on NVIDIA, which makes
        # the conversion the identity and a skipped conversion byte-identical to a
        # performed one. It shows up only on parts like Intel Iris Xe (52.0833 ns/tick),
        # which we do not have a runner for. So this is decided from the source text,
        # where the period's value is irrelevant.
        #
        # It imports nothing from tests/ops/_verdict.py — it emits no verdict about the
        # EP, only a finding about source — so it still runs during a vocabulary outage.
        run: python ci/check_tick_conversions.py --github-summary --json tick-screen-inventory.json

      - name: Tick-conversion screen negative control (inject the defect, demand red)        # R10, applied to the screen itself: a screen that has only ever been observed
        # passing is one step from a screen that cannot fail. This injects each defect
        # the screen claims to detect into a scratch COPY of rust/src and fails the job
        # if the screen stays green. It never writes to the checked-out source.
        run: python ci/negative_control_tick_conversions.py

      - name: Census extent and independent whole (criterion 12 evidence, no GPU)
        # DESIGN.md §10 row 12 asks for a census PLUS three things the census cannot
        # supply about itself: the EXTENT of each observation, the DECOMPOSITION IDENTITY
        # against a whole the census does not control, and a NAME-against-CONTENT check.
        # The whole is enumerated from production Rust — counter fields, trace phases, env
        # switches — so the count can be wrong, which is the only reason it is evidence.
        # This step does NOT close row 12: Trinity owns that tally.
        run: >
          python ci/check_census_completeness.py
          --json-out bench/results/census-completeness.json

      - name: Census extent negative control (plant a mechanism, demand red)
        # A completeness check observed only passing reads as coverage and asserts
        # nothing — the exact defect the step above exists to find.
        run: python ci/negative_control_census_completeness.py

      - name: Device-loss screen (artifacts, no GPU)
        # A run that lost the Vulkan device, fell back to the CPU EP and exited 0. The
        # exit code is deliberately NOT an input: the exit code IS the defect. A lost
        # device that exits 0 does not look like a failure, it looks like a smaller
        # number — Tank's two ctx-512 KV points were truncated by one and differencing
        # them produced an apparent 6.7% saving that was an observation ending early.
        # Two signals, neither a status code: the EP's own device-lost text (Vulkan
        # specification language, stable across vendors) and a structural rule comparing
        # what an artifact declared it would observe against what it observed.
        run: python ci/check_device_loss.py --lane

      - name: Device-loss screen negative control (demand red)
        # 14 arms, and it counts LIVE / REPLAYED / PLANTED separately rather than
        # reporting "14 passed", because those three are worth very different amounts.
        run: python ci/negative_control_device_loss.py

      - name: README usage screen (does the documented import exist?)
        # The README told every reader to `import onnxruntime_ep_vulkan` for months while
        # that package did not exist anywhere in the tree. Nothing could have noticed: the
        # test suite imports what exists, so documentation drift is invisible to it by
        # construction. This reads the README's fenced python blocks as claims and
        # requires every imported module to resolve — either installed, or actually in
        # this tree. It does not execute the blocks; executing them needs a GPU, a built
        # artifact and a model, and a check that can only run on one desk is not a check.
        run: python ci/check_readme_usage.py

      - name: README usage screen negative control (replay the shipped defect)
        # The strongest arm is not planted: it runs the screen against the real README at
        # the commit before the shim landed and demands red. The rest are planted, and the
        # count of planted arms is printed so nobody reads them as if they were replays.
        run: python ci/negative_control_readme_usage.py

      - name: Upload tick-screen inventory
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: tick-screen-inventory
          path: |
            tick-screen-inventory.json
            bench/results/tick-screen-negative-control.json
            bench/results/census-completeness.json
          if-no-files-found: warn

  # ----------------------------------------------------------------
  # 3. Linux: build + clippy + op tests (lavapipe GPU-less lane)
  #    + criterion 10 gate artifact + negative control
  # ----------------------------------------------------------------
  build-test-linux:
    name: Build + op tests (Linux, lavapipe)
    runs-on: ubuntu-22.04
    env:
      # Force Vulkan loader to use lavapipe exclusively.
      VK_ICD_FILENAMES: /usr/share/vulkan/icd.d/lvp_icd.x86_64.json
      ONNXRUNTIME_EP_VULKAN_VALIDATE: "1"
      # VK_INSTANCE_LAYERS moved to the pytest step so the lavapipe smoke-check
      # gets a clean baseline enumeration (per docs/PLATFORMS.md §7.4.2 Fix 2:
      # setting it at job level can cause vkCreateInstance to fail if the layer
      # path is not yet configured, masking lavapipe as the real problem).
      VK_LOADER_DEBUG: warn

    steps:
      - uses: actions/checkout@v4

      - name: Install Rust toolchain (rustup, no third-party action)
        run: |
          rustup toolchain install stable --profile minimal \
            --component rustfmt,clippy --no-self-update
          rustup default stable
          rustc --version && cargo --version

      # -------------------------------------------------------
      # LunarG Vulkan SDK apt repository — provides glslc (shaderc).
      #
      # Ubuntu 22.04's own repos do NOT contain shaderc or glslc.
      # The LunarG apt repository for Jammy does, via the 'shaderc'
      # package which installs /usr/bin/glslc.
      #
      # Version pin: the list file URL includes the SDK minor version
      # (e.g. 1.3.296) so this does not silently pick up a new SDK.
      # To bump: update VULKAN_SDK_VERSION at the workflow level.
      # -------------------------------------------------------
      - name: Add LunarG Vulkan SDK apt repository
        run: |
          set -euo pipefail
          # Strip trailing .0 for the versioned repo URL (1.3.296.0 → 1.3.296)
          SDK_SHORT="${VULKAN_SDK_VERSION%.*}"
          # Fetch the LunarG signing key into the modern keyring location.
          wget -qO- https://packages.lunarg.com/lunarg-signing-key-pub.asc \
            | sudo gpg --dearmor -o /usr/share/keyrings/lunarg-signing-key.gpg
          # Write our OWN .list entry referencing that keyring.
          # We do NOT use LunarG's versioned .list file verbatim because it
          # does not contain a signed-by= clause, so apt can't verify it —
          # that is the root cause of the NO_PUBKEY AA8452080E383F7E error.
          echo "deb [signed-by=/usr/share/keyrings/lunarg-signing-key.gpg] https://packages.lunarg.com/vulkan/${SDK_SHORT} jammy main" \
            | sudo tee /etc/apt/sources.list.d/lunarg-vulkan-jammy.list > /dev/null
          sudo apt-get update -q
          echo "LunarG apt repo added (SDK ${SDK_SHORT}, signed-by= set)"

      # -------------------------------------------------------
      # Vulkan SDK: lavapipe (Mesa CPU rasterizer), validation
      # layers, GLSL compiler (glslc), spirv-tools.
      #
      # glslc comes from the LunarG repo (added above) via the
      # 'shaderc' package — NOT from Ubuntu's own repos, which
      # do not ship shaderc. Previous rounds tried 'glslang-tools'
      # (provides glslangValidator only) and then 'shaderc' from
      # Ubuntu repos (package does not exist). The LunarG repo
      # is the definitive source for glslc on Ubuntu 22.04.
      #
      # 'shaderc' (LunarG repo) → /usr/bin/glslc
      # 'glslang-tools' (Ubuntu) → /usr/bin/glslangValidator (kept)
      # Ubuntu 22.04 ships Mesa 22.0, Vulkan 1.3.
      # -------------------------------------------------------
      - name: Install Vulkan SDK (lavapipe + validation layers)
        run: |
          sudo apt-get update -q
          sudo apt-get install -y \
            libvulkan-dev \
            vulkan-validationlayers \
            mesa-vulkan-drivers \
            shaderc \
            glslang-tools \
            spirv-tools \
            vulkan-tools
            # Refresh linker cache — LunarG packages may install SOs to a path
            # not yet visible to the dynamic linker.
            sudo ldconfig
            # Smoke-check: lavapipe must enumerate cleanly before we waste build time.
            # VK_INSTANCE_LAYERS is intentionally absent here (moved to test step)
            # to get a clean baseline that reflects Mesa installation, not layer config.
            # 'llvmpipe' is lavapipe's reported device name. Hard failure if absent.
            VK_LOADER_DEBUG=warn VK_ICD_FILENAMES=/usr/share/vulkan/icd.d/lvp_icd.x86_64.json \
              vulkaninfo --summary 2>&1 | tee /dev/stderr | grep -qi "llvmpipe" || {
              echo "::error::lavapipe not enumerated by vulkaninfo — mesa-vulkan-drivers may not be installed or the LunarG loader upgrade broke the ICD path. Check above loader debug output."
              exit 1
            }
            echo "lavapipe smoke-check: OK (llvmpipe enumerated)"

      # -------------------------------------------------------
      # Precondition check: verify the GLSL compiler is actually
      # on PATH before the build starts. A build that fails 40s
      # later inside build.rs with a panic is a much worse
      # diagnostic than this explicit check.
      # -------------------------------------------------------
      - name: Verify GLSL compiler (glslc)
        run: |
          glslc --version || {
            echo "::error::glslc not found on PATH after installing 'shaderc'."
            echo "Package contents:"
            dpkg -L shaderc 2>/dev/null || true
            echo "PATH: $PATH"
            exit 1
          }
          echo "glslc OK: $(glslc --version 2>&1 | head -1)"

      # -------------------------------------------------------
      # ONNX Runtime: download the prebuilt Linux x64 package.
      # The EP crate's build.rs needs ORT headers for FFI bindings.
      # -------------------------------------------------------
      - name: Provision ONNX Runtime ${{ env.ORT_VERSION }}
        run: |
          set -euo pipefail
          curl -fsSL \
            "https://github.com/microsoft/onnxruntime/releases/download/v${ORT_VERSION}/onnxruntime-linux-x64-${ORT_VERSION}.tgz" \
            | tar xz
          echo "ORT_HOME=$PWD/onnxruntime-linux-x64-${ORT_VERSION}" >> "$GITHUB_ENV"

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install Python test dependencies
        run: |
          python -m pip install --upgrade \
            "onnxruntime==${ORT_VERSION}" \
            "onnx>=${ONNX_MIN_VERSION}" \
            numpy onnx_ir pytest onnx-shape-inference

      # -------------------------------------------------------
      # Build the EP crate.
      # -D warnings: warning-clean is a CI requirement.
      #
      # THE ESCAPE THAT USED TO BE HERE. Until 2026-08-03 a missing `rust/Cargo.toml`
      # set `BUILD_SKIPPED=1`, and every subsequent step in this lane carried
      # `if: env.BUILD_SKIPPED != '1'`. So the whole lane -- build, clippy, every cargo
      # test, the pytest suite, all three Criterion 10 gates and both negative controls
      # -- would no-op and the job would report SUCCESS. That was correct while the crate
      # was Tank's work in progress and there was nothing to build. The crate has been
      # tracked in git for a week: `rust/Cargo.toml` is not a state the tree can be in,
      # it is a broken checkout. Leaving the escape in meant one bad path or one
      # sparse-checkout filter could turn both device lanes into green no-ops. It is now
      # a hard failure.
      #
      # THE THIRTY GUARDS ARE ALSO GONE, and leaving them was a mistake I made in the
      # same commit that removed the writer. The stated reason was that the change should
      # "read as one deletion rather than thirty" -- but a guard whose writer does not
      # exist is not inert, it is DORMANT: it reads in review exactly like a live guard,
      # and the next person to add one line writing that name re-arms all thirty at once.
      # ci/check_build_precondition.py rule BP2 fires on exactly that state and found this
      # one on the tree that introduced it. BP1 makes the writer itself unaddable.
      # -------------------------------------------------------
      - name: Build Vulkan EP (cargo build --release)
        run: |
          if [ ! -f rust/Cargo.toml ]; then
            echo "::error::rust/Cargo.toml not found. It is tracked in this repository, so its absence is a broken checkout, not a work-in-progress state. Skipping the lane on this condition would report success for a job that built, tested and gated nothing."
            exit 1
          fi
          ORT_INCLUDE_DIR="$ORT_HOME/include" \
          RUSTFLAGS="-D warnings" \
            cargo build --release --manifest-path rust/Cargo.toml
          # Verify the cdylib was produced
          test -f rust/target/release/libonnxruntime_vulkan_ep.so || {
            echo "::error::cdylib not found after build — check [lib] crate-type in Cargo.toml"
            exit 1
          }
          echo "ONNXRUNTIME_VULKAN_EP_LIB=$PWD/rust/target/release/libonnxruntime_vulkan_ep.so" \
            >> "$GITHUB_ENV"

      # -------------------------------------------------------
      # Compile the test/bench/example targets BEFORE linting them.
      #
      # `cargo build --release` above compiles the lib only. Everything
      # else in the crate — the ~500 unit tests, six integration
      # targets, xtask — was first *compiled* by the clippy step,
      # because clippy is the first `--all-targets` invocation in the
      # lane. So on 2026-08-02 eleven i32/u32 bindgen-signedness errors
      # in rust/src/ep.rs (OrtLoggingLevel is c_int under MSVC and
      # c_uint under GCC) surfaced as a red step named
      # "Clippy (all warnings as errors)", and, because a red step
      # skips the rest of its job, the seven Linux steps behind it —
      # unit tests, portability lint, the remaining integration
      # targets, the loader probe, op-correctness, the gate artifact
      # and its verdict readers — had never executed at all.
      #
      # A step's name is a claim about what its failure means. This one
      # said "someone wrote un-idiomatic code"; it meant "this crate
      # does not compile on Linux". The misrouting cost a day.
      #
      # No `-D warnings` here on purpose: this step must fail on
      # compile errors ONLY, so that a red here and a red in clippy are
      # different findings. Warnings remain clippy's to enforce.
      # -------------------------------------------------------
      - name: Compile all targets (cargo check --all-targets — compile errors, not lints)
        run: |
          ORT_INCLUDE_DIR="$ORT_HOME/include" \
            cargo check --release --manifest-path rust/Cargo.toml --all-targets

      - name: Clippy (all warnings as errors)
        run: |
          ORT_INCLUDE_DIR="$ORT_HOME/include" \
            cargo clippy --release --manifest-path rust/Cargo.toml \
            --all-targets -- -D warnings

      # -------------------------------------------------------
      # Layering lint — Tank's integration test (cargo test --test layering).
      # Tests are in rust/tests/layering.rs and verify that ops code never
      # imports from sys::, ORT C ABI, ash::, vk::, or uses unsafe.
      #
      # No `if [ ! -f rust/Cargo.toml ] ... exit 0` escape: removed 2026-08-03 with the
      # one in the build step it depended on. It had become unreachable -- the build step
      # set BUILD_SKIPPED on exactly that condition and this step was gated on it -- and
      # an unreachable green-for-nothing branch reads in a diff exactly like a live
      # waiver.
      #
      # Captured and floored 2026-08-03: `cargo test --test layering` prints
      # `running 0 tests` / `test result: ok.` and exits ZERO if every test in the target
      # is #[cfg]-ed out or the [[test]] entry is dropped from Cargo.toml. For a LINT
      # that is the worst possible failure mode -- the step keeps its name and its green
      # and stops asserting the thing it is named for.
      # -------------------------------------------------------
      - name: Layering lint (cargo test --test layering)
        run: |
          set -o pipefail
          mkdir -p bench/results/ci-lane
          touch bench/results/ci-lane/.reached-layering-linux
          ORT_INCLUDE_DIR="$ORT_HOME/include" \
            cargo test --test layering --manifest-path rust/Cargo.toml \
            2>&1 | tee bench/results/ci-lane/cargo-test-layering-linux.log

      - name: Layering lint asserted something (productivity floor, libtest)
        # --lane-marker, and a marker written by THIS suite's own step. Without it the
        # floor cannot tell "the suite ran and asserted nothing" from "an earlier step in
        # this job failed and the suite never started", and reports the second as
        # ERROR(instrument=log_not_captured) — exit 4, a red. On 2026-08-04 one clippy
        # lint on Linux produced five red steps that way: one cause and four reporters,
        # which is exactly the mixture this lane's own vocabulary exists to keep apart.
        # The pytest floor in this same job has had the marker since it was written; the
        # four libtest floors did not, and the difference was invisible until a build
        # actually failed.
        if: always()
        run: python ci/check_suite_productivity.py
          --suite "cargo test --test layering" --harness libtest --lane build-test-linux
          --lane-marker=bench/results/ci-lane/.reached-layering-linux
          bench/results/ci-lane/cargo-test-layering-linux.log

      # -------------------------------------------------------
      # Rust unit tests — 440 of them, and until 2026-08-01 NONE of
      # them had ever run in CI. The only cargo test in either lane
      # was --test layering, one integration target out of six.
      #
      # This is not a completeness gesture. lavapipe reports
      # timestampPeriod = 1.0, exactly as NVIDIA does, so a build
      # that drops the period conversion is NUMERICALLY IDENTICAL on
      # every device CI can reach while under-reporting Intel Iris Xe
      # by 52.0833x. No execution on the CI device can see that. The
      # trace.rs unit tests can, because they construct a SYNTHETIC
      # calibration with a non-unit period and never touch a device.
      #
      # Falsified 2026-08-01: dropping both period multiplies in
      # trace.rs turns four of these red, including
      #   treating_intel_ticks_as_nanoseconds_is_wrong_by_fifty_two_times
      #   'period scaling not applied: 100000 ns for 100000 ticks'
      # See ci/lane_inventory.py :: BLIND_SPOTS['timestamp_period_52x'].
      #
      # Default parallelism deliberately. Running with --test-threads=1
      # would hide cross-test state leakage, which is a defect and not
      # a test-runner setting.
      # -------------------------------------------------------
      - name: Rust unit tests (cargo test --lib)
        run: |
          set -o pipefail
          mkdir -p bench/results/ci-lane
          touch bench/results/ci-lane/.reached-lib-linux
          ORT_INCLUDE_DIR="$ORT_HOME/include" \
            cargo test --lib --release --manifest-path rust/Cargo.toml \
            2>&1 | tee bench/results/ci-lane/cargo-test-lib-linux.log

      - name: Rust unit tests asserted something (productivity floor, libtest)
        # libtest has the same hole as pytest and a quieter one: a target with no tests
        # prints `running 0 tests` / `test result: ok.` and exits ZERO, and there is no
        # --strict flag for it. The 510 tests above are the ONLY thing in either lane that
        # can see the timestamp-period 52x blind spot, and they arrived in CI three days
        # ago; a filter typo that left three of them running would look identical here.
        if: always()
        run: python ci/check_suite_productivity.py
          --suite "cargo test --lib" --harness libtest --lane build-test-linux
          --lane-marker=bench/results/ci-lane/.reached-lib-linux
          bench/results/ci-lane/cargo-test-lib-linux.log

      # -------------------------------------------------------
      # Model runner — rust/modelrunner is a workspace member and NOT a
      # default member, so `cargo check --all-targets` and `cargo clippy`
      # above do not touch it. Without this step the crate would compile
      # only on the developer machine that wrote it: a Rust-native runner
      # whose whole reason to exist is "works where PyPI is blocked" would
      # be the one thing CI never built.
      #
      # -p is therefore not an optimisation here, it is the coverage.
      # Its unit tests are pure (SHA-256 vectors, JSON round-trips, the
      # pinned PRNG stream, tolerance policy, comparator NaN asymmetry,
      # library-discovery arbitration) and its integration tests drive the
      # real binary with planted-negative caches. Neither needs a GPU, an
      # ONNX Runtime, or a network. This lane is also the only place the
      # Linux half of the path/loader code is exercised at all.
      #
      # 2026-08-06, issue #39: one integration test did quietly depend on
      # an ambient model cache, passed on every developer machine, and
      # failed on every clean runner. Nothing was added to this step to fix
      # that -- installing a model here would have hidden the defect rather
      # than removed it. Instead each test now builds its own cache and its
      # own stand-in library and scrubs the environment variables discovery
      # reads, so this step needs exactly cargo and nothing else. If a
      # future test needs an install here to pass, the test is wrong.
      # -------------------------------------------------------
      - name: Model runner (cargo test -p ort-model-runner, no device needed)
        run: |
          set -o pipefail
          mkdir -p bench/results/ci-lane
          touch bench/results/ci-lane/.reached-modelrunner-linux
          ORT_INCLUDE_DIR="$ORT_HOME/include" \
            cargo clippy --release --manifest-path rust/Cargo.toml \
            -p ort-model-runner --all-targets -- -D warnings
          ORT_INCLUDE_DIR="$ORT_HOME/include" \
            cargo test --release --manifest-path rust/Cargo.toml -p ort-model-runner \
            2>&1 | tee bench/results/ci-lane/cargo-test-modelrunner-linux.log

      # -------------------------------------------------------
      # Contention gate — the step above runs ONCE, and once is not
      # enough to see an intermittent.
      #
      # 2026-08-03: a merge gate consisting of one `cargo test --lib`
      # went red, was re-run six times green, and was pushed on that
      # basis. A per-run failure rate of 1 in 40 passes 97.5% of
      # merges, and this project merges many times a day. The failing
      # test name was also lost, because the step's output had been
      # truncated to its last two lines — so the incident produced a
      # red with no name, which is a rumour rather than a bug report.
      #
      # This step repeats only the tests that touch a process-global
      # (counters statics, ORT logger pointers, contended env vars),
      # derived from rust/tools/audit_counter_test_lock.py rather than
      # from a list. Narrowing is the amplifier, not a cost saving:
      # `cargo test --lib backend_probe` failed 6 in 40 on Windows
      # while the full suite failed 1 in 40 — a 21-test pool aligns on
      # a contended global far more often than a 505-test pool.
      # 20 repetitions of five pools cost 38 s wall on the reference
      # machine and detect a 0.15 per-run rate with probability 0.961.
      # -------------------------------------------------------
      - name: Contention gate (repeat the process-global families; keep the whole red)
        run: |
          ORT_INCLUDE_DIR="$ORT_HOME/include" \
            python rust/tools/contention_gate.py --release --reps 20

      - name: Upload contention-gate captures
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: contention-gate-linux
          path: bench/results/contention_gate/
          if-no-files-found: ignore

      # -------------------------------------------------------
      # Portability lint — rule P1: a platform-conditional binding
      # named without a cfg gate. In the lint's own words, "the class
      # of bug that only shows up on a CI lane for another OS, hours
      # later, while blocking everything behind it". It has existed in
      # rust/tests/portability.rs and had never been run by CI.
      # -------------------------------------------------------
      - name: Portability lint (cargo test --test portability)
        run: |
          set -o pipefail
          mkdir -p bench/results/ci-lane
          touch bench/results/ci-lane/.reached-portability-linux
          ORT_INCLUDE_DIR="$ORT_HOME/include" \
            cargo test --test portability --release --manifest-path rust/Cargo.toml \
            2>&1 | tee bench/results/ci-lane/cargo-test-portability-linux.log

      - name: Portability lint asserted something (productivity floor, libtest)
        # This lint is the one that catches a defect on the lane it does NOT affect —
        # MSVC types OrtLoggingLevel c_int and GCC c_uint, so a spelled i32 compiles here
        # and dies there, hours later, behind everything else. A silently-empty
        # portability lint restores exactly the state that cost a day on 2026-08-02.
        if: always()
        run: python ci/check_suite_productivity.py
          --suite "cargo test --test portability" --harness libtest --lane build-test-linux
          --lane-marker=bench/results/ci-lane/.reached-portability-linux
          bench/results/ci-lane/cargo-test-portability-linux.log

      # -------------------------------------------------------
      # The remaining integration targets, also never run in CI.
      #
      # FOUR CLAIMS UNDER ONE EXIT CODE. cdylib_load and host_registration are ONE test
      # each; an aggregate floor cannot see either of them go silent behind the other
      # two. So the floor entry for this step sets min_target_blocks = 4 and the checker
      # fails, BY TARGET NAME, if any single `test result:` block executed zero.
      # -------------------------------------------------------
      - name: Remaining integration targets (cdylib_load, dump_capabilities, host_registration, validation_control)
        run: |
          set -o pipefail
          mkdir -p bench/results/ci-lane
          touch bench/results/ci-lane/.reached-integration-linux
          ORT_INCLUDE_DIR="$ORT_HOME/include" \
            cargo test --release --manifest-path rust/Cargo.toml \
            --test cdylib_load --test dump_capabilities \
            --test host_registration --test validation_control \
            2>&1 | tee bench/results/ci-lane/cargo-test-integration-linux.log

      - name: Integration targets asserted something (productivity floor, libtest)
        if: always()
        run: python ci/check_suite_productivity.py
          --suite "cargo test --test cdylib_load,dump_capabilities,host_registration,validation_control"
          --harness libtest --lane build-test-linux
          --lane-marker=bench/results/ci-lane/.reached-integration-linux
          bench/results/ci-lane/cargo-test-integration-linux.log

      # -------------------------------------------------------
      # Probe Vulkan loader — hard failure if no capable device.
      # Switch built epctl for exactly this purpose: a standalone
      # Vulkan probe that needs no ONNX Runtime and exits non-zero
      # when no capable device is found. This turns "tests all
      # skipped for some reason" into a named red step that says why.
      # -------------------------------------------------------
      - name: Probe Vulkan loader (epctl --probe-loader)
        # Captured as well as printed. The capture is what lets the proof-ledger
        # portability screen below distinguish "no Vulkan device" from "a Vulkan device
        # that passed the gate and whose work the EP then declined". Without a loader
        # artifact that condition is UNOBSERVABLE, and the screen says so rather than
        # passing.
        run: |
          mkdir -p bench/results/ci-lane
          rust/target/release/epctl --probe-loader 2>&1 \
            | tee bench/results/ci-lane/loader-probe.log

      # -------------------------------------------------------
      # Op-correctness tests (pytest, Vulkan vs ORT CPU oracle).
      # Includes:
      #   - test_op_table.py    : claim + CPU oracle (124 cases)
      #   - test_barrier_parity.py : sync2/legacy parity (74 claim=True cases, runs twice each)
      #   - test_elementwise.py : edge-case structural tests
      #   - test_fallback.py    : permanent CPU fallback and mixed session
      #   - test_claim_diagnostics.py : M0 claim assertion + no-ICD fallback
      # VK_INSTANCE_LAYERS set here (not job level) so the smoke-check above
      # ran without it and got a clean lavapipe baseline.
      # -------------------------------------------------------
      - name: Op-correctness + barrier-parity tests (pytest, lavapipe)
        env:
          VK_INSTANCE_LAYERS: VK_LAYER_KHRONOS_validation
        run: |
          export LD_LIBRARY_PATH="$ORT_HOME/lib${LD_LIBRARY_PATH:+:$LD_LIBRARY_PATH}"
          mkdir -p bench/results/ci-lane
          # The lane marker: written the moment this lane can produce evidence at all.
          # ci/check_device_state.py and ci/check_fatal_log.py both run under always(),
          # so on a lane that died earlier they would otherwise report an instrument
          # error about a subject that never existed and add a second red on top of a
          # failure they did not cause. The marker can only be written by the step that
          # produces the evidence, so it is never absent on a run that did publish.
          touch bench/results/ci-lane/.lane-reached
          # 2>&1: ORT writes `EP_FAIL ... Falling back` from C++ to fd 2. A tee of
          # stdout alone captures a log in which that line cannot appear, and the
          # grep below would then agree with everything (R12: a counter whose event
          # cannot occur in its frame is UNOBSERVABLE, not 0).
          set -o pipefail
          python -m pytest tests/ops -q --tb=short -p no:randomly --strict-markers \
            2>&1 | tee bench/results/ci-lane/pytest-linux.log

      # -------------------------------------------------------
      # THE STEP ABOVE COULD REPORT SUCCESS FOR DOING NOTHING, AND DID NOT SAY SO.
      #
      # Found 2026-08-02: tests/ops/test_shape_inference_delta.py built its coverage
      # report at MODULE scope, so the import of the optional `onnx-shape-inference`
      # package ran at COLLECTION time. Without the package pytest printed
      # `Interrupted: 1 error during collection` and abandoned all 665 tests in the
      # directory. One optional dependency zeroed the entire op-correctness step.
      #
      # The collection-error form does redden this step by exit code. The quiet form does
      # not: a suite whose tests are all SKIPPED exits zero. `2 passed, 36 skipped` is a
      # real log from this project (bench/results/linux_lavapipe_optests.txt) and it is
      # indistinguishable, to every instrument this repository had before today, from a
      # lane that did its job.
      #
      # This step is the mechanism, not a convention: floors live in ci/suite_floor.json,
      # each with the measurement it came from, and there is deliberately no flag that
      # lowers one. always(), because "the suite failed" and "the suite never ran" are the
      # same red on the step above and are not the same finding.
      # -------------------------------------------------------
      - name: Op-correctness step asserted something (productivity floor)
        if: always()
        run: python ci/check_suite_productivity.py
          --suite tests/ops --lane build-test-linux
          --lane-marker=bench/results/ci-lane/.lane-reached
          --emit-json bench/results/ci-lane/suite-productivity-linux.json
          bench/results/ci-lane/pytest-linux.log

      # -------------------------------------------------------
      # FLAKE WITNESS — the coordinator lost a failing test name to a truncated log on
      # 2026-08-03: one red, six greens, and no subject. Two mechanisms, one step.
      # (a) every failing id is re-emitted as a ::error:: annotation, which GitHub stores
      #     as check-run metadata rather than log bytes and therefore cannot truncate;
      # (b) the ids are appended to a ledger so the SECOND observation of a 1-in-40 is
      #     cheap, since no single run can ever see an intermittent by construction.
      # It runs on both harnesses and reports the WORSE of the two, so a red libtest
      # cannot be hidden behind a green pytest.
      # -------------------------------------------------------
      - name: Flake witness (name the failure where truncation cannot reach it)
        if: always()
        run: |
          rc=0
          python ci/check_flake_witness.py \
            --harness libtest --suite "cargo test --lib" --lane build-test-linux \
            --ledger bench/results/ci-lane/flake-ledger-linux.jsonl \
            --lane-marker bench/results/ci-lane/.lane-reached \
            bench/results/ci-lane/cargo-test-lib-linux.log || rc=$?
          python ci/check_flake_witness.py \
            --harness pytest --suite tests/ops --lane build-test-linux \
            --ledger bench/results/ci-lane/flake-ledger-linux.jsonl \
            --lane-marker bench/results/ci-lane/.lane-reached \
            bench/results/ci-lane/pytest-linux.log || rc=$?
          exit $rc

      # -------------------------------------------------------
      # CRITERION 10 — the gate this lane did not carry until
      # 2026-07-31, and the reason it needed to.
      #
      # Until now this lane's pass condition was "the pytest
      # process exited zero". On 2026-07-30 that condition held
      # for most of a day while the EP executed ZERO nodes: ORT
      # printed `EP_FAIL ... Falling back to CPUExecutionProvider`
      # inside run(), re-ran the whole graph on CPU, and raised
      # nothing. get_providers() still listed VulkanExecutionProvider
      # because the provider list is fixed at session-create time.
      # The suite was green. Nothing ran on Vulkan.
      #
      # Three steps, deliberately three processes, with three
      # different failure modes (DESIGN.md §10.0.1 R13 obligation 3):
      #   1. produce a verdict record from a right-sized artifact
      #   2. read the record back in a separate process
      #   3. grep the captured suite log for the known-fatal line
      # A guard can crash; a grep cannot NameError. Each covers the
      # other's outage.
      #
      # `continue-on-error` appears on NONE of them, and neither does
      # any verdict-bypass flag. Absence of a check is a refusal, not
      # a default green.
      # -------------------------------------------------------
      - name: Verdict vocabulary preflight (which kind of instrument outage?)
        # Runs BEFORE the gate. Every ci/ check imports its vocabulary from
        # tests/ops/_verdict.py and defines none of its own, so if that module cannot
        # be imported EVERY step below reports ERROR(instrument=...) at once. This step
        # exists so that outcome is readable rather than ambient: it separates "this
        # checkout does not carry the vocabulary" (a repository state; token
        # verdict_vocabulary_absent_from_checkout) from "the file is here and this job
        # cannot load it" (a lane defect; token verdict_vocabulary_broken), prints the
        # module's sha256 and git-tracked status so two lanes can be diffed, and writes
        # the answer to the job summary instead of leaving it in a log.
        run: python ci/check_vocabulary.py --github-summary

      - name: Criterion 10 gate artifact (gate_chain_fp32, lavapipe)
        env:
          ONNXRUNTIME_EP_VULKAN_COUNTERS_FILE: ${{ github.workspace }}/bench/results/ci-lane/counters-linux.json
        run: |
          export LD_LIBRARY_PATH="$ORT_HOME/lib${LD_LIBRARY_PATH:+:$LD_LIBRARY_PATH}"
          mkdir -p bench/results/ci-lane
          touch bench/results/ci-lane/.reached-gatechain-linux
          python ci/gate_chain_fp32.py \
            --verdict-out bench/results/ci-lane/verdict-linux.json \
            --workdir bench/results/ci-lane

      - name: Criterion 10 verdict gate (independent reader)
        run: python ci/check_verdict.py bench/results/ci-lane/verdict-linux.json

      - name: Criterion 10 verdict gate (epctl --check-counters)
        run: |
          rust/target/release/epctl --check-counters \
            bench/results/ci-lane/counters-linux.json --require-dispatches 1

      - name: Known-fatal log line is a lane failure (R13 second witness)
        # Runs even when an earlier gate failed: when the EP falls back, the log line
        # is the most legible evidence in the whole lane and it must reach the reader
        # rather than being cancelled by the failure it explains.
        if: always()
        run: |
          python ci/check_fatal_log.py \
            --lane-marker=bench/results/ci-lane/.lane-reached \
            bench/results/ci-lane/pytest-linux.log \
            --lane-marker=bench/results/ci-lane/.reached-gatechain-linux \
            bench/results/ci-lane/gate_chain_fp32_ort_stderr.log

      - name: Device-loss screen on this lane's own evidence (Linux)
        if: always()
        # --run-log NAMES these files as one run's evidence, which turns on the three
        # conditions a tree-wide scan reports UNOBSERVABLE (controls on this project emit
        # those texts deliberately). This step and check_fatal_log above have DIFFERENT
        # extents and must not be read as one guarantee: check_fatal_log reads ORT's
        # fallback announcement in a captured log; this reads the EP's own device-lost
        # text plus a structural rule that needs no text. A device loss the EP reports and
        # ORT never announces is red here and green there — demonstrated, not argued, in
        # ci/negative_control_device_loss.py.
        run: |
          python ci/check_device_loss.py \
            --lane-marker=bench/results/ci-lane/.lane-reached \
            --run-log bench/results/ci-lane/pytest-linux.log \
            --run-log bench/results/ci-lane/gate_chain_fp32_ort_stderr.log \
            bench/results/ci-lane

      - name: Proof-ledger portability screen (Linux device lane)
        if: always()
        # THE LINUX LANE'S OWN CROSS-PLATFORM SCREEN, and the reason it exists is that
        # this lane can pass while asserting nothing. On 2026-08-02, built on Linux at
        # d375a4d and pointed at lavapipe, EVERY ledger entry faulted: shader_digest is a
        # hash of the embedded SPIR-V, Ubuntu's glslc (shaderc 2023.8) emits different
        # bytes than the Vulkan SDK's, so the EP claimed 0/1 nodes, all work went to the
        # CPU EP, and the process EXITED 0. Downstream, tests/ops/conftest.py reports that
        # EP decision as "No Vulkan device available" and skips — 2 passed, 36 skipped —
        # on a box where epctl --probe-loader had just printed gate PASS for llvmpipe.
        #
        # So the op-correctness step below would go GREEN HAVING ASSERTED NOTHING. This
        # step is what makes that state red instead. It does not take the exit status as
        # an input: the defect IS an exit status of 0.
        run: |
          python ci/check_ledger_portability.py \
            --device-lane \
            --lane-marker=bench/results/ci-lane/.lane-reached \
            --loader-artifact bench/results/ci-lane/loader-probe.log \
            --run-log bench/results/ci-lane/pytest-linux.log

      - name: Proof-ledger portability negative control (demand red)
        if: always()
        # 11 arms, counted 3 LIVE / 0 REPLAYED / 8 PLANTED. All three conditions have a
        # LIVE arm, which is unusual here and only because Linux was genuinely broken.
        run: python ci/negative_control_ledger_portability.py

      # -------------------------------------------------------
      # POSITIVE CONTROL FOR THE GATE ITSELF (R9) — see the Windows
      # lane for the full rationale. Evidence scales only with
      # falsifying instruments: a check observed only to pass is not
      # known to be a check. Remove the ICD, keep everything else
      # identical, and require UNATTRIBUTED.
      # -------------------------------------------------------
      - name: Gate negative control — no ICD must produce UNATTRIBUTED
        env:
          VK_ICD_FILENAMES: /nonexistent/lvp_icd.json
          VK_DRIVER_FILES: /nonexistent/lvp_icd.json
          ONNXRUNTIME_EP_VULKAN_COUNTERS_FILE: ""
        run: |
          export LD_LIBRARY_PATH="$ORT_HOME/lib${LD_LIBRARY_PATH:+:$LD_LIBRARY_PATH}"
          mkdir -p bench/results/ci-lane
          # Did the suppression take? Linux runners are not elevated so it should, but
          # "should" is what the Windows lane also believed for weeks (PLATFORMS.md
          # §7.4.1). Observed beats assumed, and the record this writes is the artifact
          # that says which of the two this run was (R10).
          set +e
          rust/target/release/epctl --probe-loader > bench/results/ci-lane/icd-probe-report.txt 2>&1
          probe_exit=$?
          set -e
          cat bench/results/ci-lane/icd-probe-report.txt
          set +e
          python ci/check_icd_suppression.py bench/results/ci-lane/icd-probe-report.txt \
            --record-out bench/results/ci-lane/icd-suppression.json \
            --exit-code "$probe_exit"
          icd_state=$?
          set -e
          if [ "$icd_state" -ne 0 ]; then
            # R13: an instrument error is not a detection. The control did not reach its
            # observation, so it asserts nothing either way and must not be reported as a
            # lane failure. The decline-probe control below is loader-independent, is
            # mandatory in this lane too, and cannot be skipped.
            echo "::warning title=ICD-removal negative control did not fire::ERROR(instrument) — see bench/results/ci-lane/icd-suppression.json. This step asserts NOTHING about the gate."
            exit 0
          fi
          set +e
          out=$(python ci/gate_chain_fp32.py \
            --verdict-out bench/results/ci-lane/negative-control.json \
            --workdir bench/results/ci-lane 2>&1)
          code=$?
          set -e
          echo "$out"
          # Quote the text, never the count (R13 second clause).
          if [ "$code" -eq 0 ]; then
            echo "::error::NEGATIVE CONTROL FAILED: the gate passed with no Vulkan ICD present. It cannot distinguish a run that executed from one that did not, so every green in this lane is uninterpretable."
            exit 1
          fi
          if ! printf '%s' "$out" | grep -q 'FAIL(condition=UNATTRIBUTED)'; then
            echo "::error::NEGATIVE CONTROL FAILED: the gate exited $code but did not report FAIL(condition=UNATTRIBUTED). An instrument error is not a detection (R13); a red that could mean either is not a signal."
            exit 1
          fi
          if printf '%s' "$out" | grep -q 'FAIL(condition=DIVERGENT)'; then
            echo "::error::NEGATIVE CONTROL FAILED: UNATTRIBUTED was reported as DIVERGENT. These have different owners and different fixes and must never be folded together."
            exit 1
          fi
          echo "NEGATIVE CONTROL PASSED: with no ICD the gate reported FAIL(condition=UNATTRIBUTED) and exited $code. The gate can fail, so its passes are readable."

      # -------------------------------------------------------
      # SECOND NEGATIVE CONTROL — LOADER-INDEPENDENT (R9).
      #
      # The control above suppresses the ICD. That is a fine control here and an
      # unreliable one on Windows, where the LunarG loader silently ignores
      # VK_DRIVER_FILES/VK_ICD_FILENAMES in elevated processes (PLATFORMS.md §7.4.1)
      # — so a control that depends on it can fail to suppress anything and then
      # report "the gate cannot fail" when what failed was the suppression.
      #
      # This one never touches the loader. `--artifact decline_probe` is a single
      # `Det` node, an op this EP does not implement: the EP loads, the driver is
      # present, the device is real, and the EP still claims nothing, so the graph
      # runs on CPU and the lane must report FAIL(condition=UNATTRIBUTED).
      #
      # It is also the stronger negative. "The EP could not start" and "the EP
      # started and executed nothing" are different failures, and the second is the
      # one that was live on 2026-07-30.
      # -------------------------------------------------------
      - name: Gate negative control — a declined artifact must produce UNATTRIBUTED
        env:
          ONNXRUNTIME_EP_VULKAN_COUNTERS_FILE: ""
        run: |
          export LD_LIBRARY_PATH="$ORT_HOME/lib${LD_LIBRARY_PATH:+:$LD_LIBRARY_PATH}"
          mkdir -p bench/results/ci-lane
          set +e
          out=$(python ci/gate_chain_fp32.py --artifact decline_probe \
            --verdict-out bench/results/ci-lane/negative-control-decline.json \
            --workdir bench/results/ci-lane 2>&1)
          code=$?
          set -e
          echo "$out"
          if [ "$code" -eq 0 ]; then
            echo "::error::NEGATIVE CONTROL FAILED: the gate passed on an artifact the EP is supposed to decline. Either the EP now claims that op — in which case this control has silently stopped being one and must be re-pointed at an op that is still declined — or the gate cannot tell execution from non-execution with the loader healthy, which is worse."
            exit 1
          fi
          if ! printf '%s' "$out" | grep -q 'FAIL(condition=UNATTRIBUTED)'; then
            echo "::error::NEGATIVE CONTROL FAILED: the gate exited $code on the declined artifact but did not report FAIL(condition=UNATTRIBUTED). An instrument error is not a detection (R13)."
            exit 1
          fi
          echo "NEGATIVE CONTROL PASSED (loader untouched): with a healthy EP that claimed nothing, the gate reported FAIL(condition=UNATTRIBUTED) and exited $code."

      # -------------------------------------------------------
      # OBLIGATION 8 — NO LANE MAY PUBLISH A DURATION WITHOUT A
      # DEVICE-STATE RECORD (DESIGN.md §10.0 obligation 8, R9 amdt 5).
      #
      # `gpu_steady_tail()` is a variance test over a suffix, so it
      # cannot see a bias: a board pinned at its 210 MHz idle clock
      # against a 3105 MHz boost is *perfectly steady about a wrong
      # mean*, and the wrong figure carried the BETTER RSD (0.8035% at
      # 10.99x wrong vs 0.8098% correct). A low clock does not raise
      # RSD; it lowers it.
      #
      # No lane quotes a timing figure today. This step is what makes
      # that true by construction rather than by luck: the first lane
      # step that writes a millisecond into an artifact or into the job
      # summary turns this red on the same run.
      #
      # It runs on always() because the state it reports is most
      # interesting on a lane that already failed — a figure published
      # by a broken run is the one most likely to be quoted later.
      # -------------------------------------------------------
      - name: No duration without a device-state record (§10.0 obligation 8)
        if: always()
        run: |
          python ci/check_device_state.py \
            --scan bench/results/ci-lane \
            --lane-marker bench/results/ci-lane/.lane-reached \
            --summary "$GITHUB_STEP_SUMMARY"

      # -------------------------------------------------------
      # The lane's own classification, published as an artifact.
      # `operational` and `green` are different claims: an operational
      # lane runs, a green lane would go RED if the thing it watches
      # broke. Only the second is worth having, and the only evidence
      # that separates them is two arms that differ.
      #
      # This step also refuses to let the inventory rot: every gate
      # step in this workflow must have an entry, because an
      # unclassified step is exactly the state the ICD negative
      # control lived in for weeks — running, reported, never once
      # falsified.
      # -------------------------------------------------------
      - name: Lane inventory - operational vs green
        if: always()
        run: |
          mkdir -p bench/results/ci-lane
          python ci/check_lane_inventory.py \
            --workflow .github/workflows/ci.yml \
            --out bench/results/ci-lane/lane-inventory.json \
            --summary "$GITHUB_STEP_SUMMARY"

      - name: Upload lane evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: lane-evidence-linux
          path: bench/results/ci-lane/
          if-no-files-found: warn

  # ----------------------------------------------------------------
  # 3. Windows: build + clippy + op tests (lavapipe via mesa-dist-win)
  # ----------------------------------------------------------------
  # FINDING (2026-07-28): lavapipe IS available on Windows CI.
  #   Mesa ships Windows lavapipe builds via mesa-dist-win:
  #   https://github.com/pal1000/mesa-dist-win
  #   The MSVC release (mesa3d-{ver}-release-msvc.7z) ships the
  #   lavapipe ICD DLL + JSON. 7-zip is available on windows-latest.
  #   Validation layers come from the LunarG Vulkan SDK already
  #   installed for glslc/headers; VK_LAYER_PATH = $VULKAN_SDK\Bin.
  #
  #   SwiftShader was also investigated: Google does not publish
  #   prebuilt Windows binaries (only source); building from source
  #   requires cmake + clang (~20 min). mesa-dist-win lavapipe is
  #   the better Windows software-rasterizer for CI.
  #   Reported to docs/PLATFORMS.md §7.4 (Link owns that file).
  # ----------------------------------------------------------------
  build-test-windows:
    name: Build + op tests (Windows, lavapipe via mesa-dist-win)
    runs-on: windows-latest
    env:
      # VK_ICD_FILENAMES set in the lavapipe step (for non-elevated sub-processes).
      # For the elevated runner itself, ICD is registered via HKLM registry —
      # loader 1.3+ ignores VK_ICD_FILENAMES/VK_DRIVER_FILES on elevated processes.
      # Source: docs/PLATFORMS.md §7.4.1 root cause analysis (Link).
      ONNXRUNTIME_EP_VULKAN_VALIDATE: "1"
      # VK_INSTANCE_LAYERS moved to the pytest step (same rationale as Linux).
      VK_LOADER_DEBUG: warn

    steps:
      - uses: actions/checkout@v4

      - name: Install Rust toolchain (rustup, no third-party action)
        run: |
          rustup toolchain install stable --profile minimal --component clippy --no-self-update
          rustup default stable
          rustc --version
          cargo --version

      # -------------------------------------------------------
      # LunarG Vulkan SDK: provides vulkan.h (for ash), glslc,
      # and validation layer headers. The installer sets VULKAN_SDK.
      # -------------------------------------------------------
      - name: Install LunarG Vulkan SDK
        run: |
          $ver = "$env:VULKAN_SDK_VERSION"
          $url = "https://sdk.lunarg.com/sdk/download/$ver/windows/VulkanSDK-$ver-Installer.exe"
          Write-Host "Downloading Vulkan SDK $ver from $url"
          Invoke-WebRequest -Uri $url -OutFile "$env:TEMP\VulkanSDK.exe"
          Start-Process -Wait -FilePath "$env:TEMP\VulkanSDK.exe" `
            -ArgumentList '--accept-licenses', '--default-answer', '--confirm-command', 'install'
          $sdkPath = "C:\VulkanSDK\$ver"
          echo "VULKAN_SDK=$sdkPath" >> $env:GITHUB_ENV
          echo "$sdkPath\Bin" >> $env:GITHUB_PATH
          Write-Host "Vulkan SDK installed at $sdkPath"

      # -------------------------------------------------------
      # ONNX Runtime: download the prebuilt Windows x64 zip.
      # -------------------------------------------------------
      - name: Provision ONNX Runtime ${{ env.ORT_VERSION }}
        run: |
          $ver = "$env:ORT_VERSION"
          $url = "https://github.com/microsoft/onnxruntime/releases/download/v$ver/onnxruntime-win-x64-$ver.zip"
          Invoke-WebRequest -Uri $url -OutFile "$env:TEMP\ort.zip"
          Expand-Archive -Path "$env:TEMP\ort.zip" -DestinationPath "$env:GITHUB_WORKSPACE"
          $ortHome = "$env:GITHUB_WORKSPACE\onnxruntime-win-x64-$ver"
          echo "ORT_HOME=$ortHome" >> $env:GITHUB_ENV

      # -------------------------------------------------------
      # Build. No `crate not yet landed` escape: removed 2026-08-03, see the Linux lane
      # for the full reasoning. rust\Cargo.toml is tracked, so its absence is a broken
      # checkout, and skipping the lane on it reported SUCCESS for a job that built,
      # tested and gated nothing.
      # -------------------------------------------------------
      - name: Build Vulkan EP (cargo build --release)
        run: |
          if (-not (Test-Path "rust\Cargo.toml")) {
            Write-Error "rust\Cargo.toml not found. It is tracked in this repository, so its absence is a broken checkout, not a work-in-progress state."
            exit 1
          }
          $env:ORT_INCLUDE_DIR = "$env:ORT_HOME\include"
          $env:RUSTFLAGS = "-D warnings"
          cargo build --release --manifest-path rust\Cargo.toml
          if (-not (Test-Path "rust\target\release\onnxruntime_vulkan_ep.dll")) {
            Write-Error "cdylib not found after build — check [lib] crate-type in Cargo.toml"
            exit 1
          }

      # -------------------------------------------------------
      # Compile the test/bench/example targets BEFORE linting them —
      # the Windows half of the same split. See the Linux lane's
      # comment for the incident. This lane is the one that was green
      # throughout, which is the point: the defect was invisible from
      # here, and naming the compile separately means a future
      # Windows-only compile error is also reported as a compile error.
      # -------------------------------------------------------
      - name: Compile all targets (cargo check --all-targets — compile errors, not lints)
        run: |
          $env:ORT_INCLUDE_DIR = "$env:ORT_HOME\include"
          cargo check --release --manifest-path rust\Cargo.toml --all-targets
          exit $LASTEXITCODE

      - name: Clippy (all warnings as errors)
        run: |
          $env:ORT_INCLUDE_DIR = "$env:ORT_HOME\include"
          cargo clippy --release --manifest-path rust\Cargo.toml `
            --all-targets -- -D warnings

      # -------------------------------------------------------
      # Rust unit tests and the four integration targets CI never ran.
      # Before 2026-08-01 this lane ran NO cargo test at all — not even
      # the layering lint. The 440 unit tests include the only
      # host-independent falsifier for the timestampPeriod defect
      # class: lavapipe reports period 1.0 exactly as NVIDIA does, so
      # a build that drops the conversion is invisible to every device
      # CI can reach and under-reports Intel Iris Xe by 52.0833x.
      # See ci/lane_inventory.py :: BLIND_SPOTS['timestamp_period_52x'].
      # -------------------------------------------------------
      - name: Rust unit tests (cargo test --lib)
        run: |
          $env:ORT_INCLUDE_DIR = "$env:ORT_HOME\include"
          New-Item -ItemType Directory -Force -Path bench\results\ci-lane | Out-Null
          New-Item -ItemType File -Force -Path bench\results\ci-lane\.reached-lib-windows | Out-Null
          cargo test --lib --release --manifest-path rust\Cargo.toml 2>&1 |
            Tee-Object -FilePath bench\results\ci-lane\cargo-test-lib-windows.log
          exit $LASTEXITCODE

      - name: Rust unit tests asserted something (productivity floor, libtest)
        # See the Linux lane. `test result: ok. 0 passed` exits ZERO, so the exit code
        # above cannot tell a suite that passed from one that ran nothing.
        if: always()
        run: |
          python ci\check_suite_productivity.py `
            --suite "cargo test --lib" --harness libtest --lane build-test-windows `
            --lane-marker=bench\results\ci-lane\.reached-lib-windows `
            bench\results\ci-lane\cargo-test-lib-windows.log
          exit $LASTEXITCODE

      # -------------------------------------------------------
      # Model runner — rust/modelrunner is a workspace member and NOT a
      # default member, so `cargo check --all-targets` and `cargo clippy`
      # above do not touch it. Without this step the crate would compile
      # only on the developer machine that wrote it: a Rust-native runner
      # whose whole reason to exist is "works where PyPI is blocked" would
      # be the one thing CI never built.
      #
      # -p is therefore not an optimisation here, it is the coverage.
      # Its unit tests are pure (SHA-256 vectors, JSON round-trips, the
      # pinned PRNG stream, tolerance policy, comparator NaN asymmetry,
      # library-discovery arbitration) and its integration tests drive the
      # real binary with planted-negative caches. Neither needs a GPU, an
      # ONNX Runtime, or a network.
      #
      # 2026-08-06, issue #39: the clean-runner failure was observed here
      # first -- `C:\Users\runneradmin\.cache\...\mnist-12.onnx` does not
      # exist on a fresh image, and a test that assumed it did reported
      # `model_not_cached` in place of the refusal it was named after. The
      # fix is in the tests, not in this step: they now build their own
      # cache and stand-in library, so this needs exactly cargo. No model
      # is downloaded here and no DLL is placed on PATH for it.
      # -------------------------------------------------------
      - name: Model runner (cargo test -p ort-model-runner, no device needed)
        run: |
          $env:ORT_INCLUDE_DIR = "$env:ORT_HOME\include"
          New-Item -ItemType Directory -Force -Path bench\results\ci-lane | Out-Null
          New-Item -ItemType File -Force -Path bench\results\ci-lane\.reached-modelrunner-windows | Out-Null
          cargo clippy --release --manifest-path rust\Cargo.toml -p ort-model-runner --all-targets -- -D warnings
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
          cargo test --release --manifest-path rust\Cargo.toml -p ort-model-runner 2>&1 |
            Tee-Object -FilePath bench\results\ci-lane\cargo-test-modelrunner-windows.log
          exit $LASTEXITCODE

      # -------------------------------------------------------
      # Contention gate. See the Linux lane for why. This lane is the
      # one that matters most for it: every measured instance of this
      # defect class — Round 36's counters race and Round 37's
      # `backend_probe` env-var race — was reproduced on Windows, and
      # the 6-in-40 rate quoted in the Linux comment is a Windows
      # measurement. It is wired on both lanes anyway, because "it
      # only reproduces on Windows" is a statement about where anyone
      # has looked.
      # -------------------------------------------------------
      - name: Contention gate (repeat the process-global families; keep the whole red)
        run: |
          $env:ORT_INCLUDE_DIR = "$env:ORT_HOME\include"
          python rust\tools\contention_gate.py --release --reps 20
          exit $LASTEXITCODE

      - name: Upload contention-gate captures
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: contention-gate-windows
          path: bench/results/contention_gate/
          if-no-files-found: ignore

      - name: Portability lint (cargo test --test portability)
        run: |
          $env:ORT_INCLUDE_DIR = "$env:ORT_HOME\include"
          New-Item -ItemType Directory -Force -Path bench\results\ci-lane | Out-Null
          New-Item -ItemType File -Force -Path bench\results\ci-lane\.reached-portability-windows | Out-Null
          cargo test --test portability --release --manifest-path rust\Cargo.toml 2>&1 |
            Tee-Object -FilePath bench\results\ci-lane\cargo-test-portability-windows.log
          exit $LASTEXITCODE

      - name: Portability lint asserted something (productivity floor, libtest)
        # See the Linux lane. The floor is the same number on both lanes deliberately:
        # every test in this target is a SOURCE screen needing no device, ICD or ORT, so
        # if the two lanes ever disagree here that disagreement is itself the finding.
        if: always()
        run: |
          python ci\check_suite_productivity.py `
            --suite "cargo test --test portability" --harness libtest --lane build-test-windows `
            --lane-marker=bench\results\ci-lane\.reached-portability-windows `
            bench\results\ci-lane\cargo-test-portability-windows.log
          exit $LASTEXITCODE

      - name: Layering lint (cargo test --test layering)
        run: |
          $env:ORT_INCLUDE_DIR = "$env:ORT_HOME\include"
          New-Item -ItemType Directory -Force -Path bench\results\ci-lane | Out-Null
          New-Item -ItemType File -Force -Path bench\results\ci-lane\.reached-layering-windows | Out-Null
          cargo test --test layering --release --manifest-path rust\Cargo.toml 2>&1 |
            Tee-Object -FilePath bench\results\ci-lane\cargo-test-layering-windows.log
          exit $LASTEXITCODE

      - name: Layering lint asserted something (productivity floor, libtest)
        if: always()
        run: |
          python ci\check_suite_productivity.py `
            --suite "cargo test --test layering" --harness libtest --lane build-test-windows `
            --lane-marker=bench\results\ci-lane\.reached-layering-windows `
            bench\results\ci-lane\cargo-test-layering-windows.log
          exit $LASTEXITCODE

      - name: Remaining integration targets (cdylib_load, dump_capabilities, host_registration, validation_control)
        run: |
          $env:ORT_INCLUDE_DIR = "$env:ORT_HOME\include"
          New-Item -ItemType Directory -Force -Path bench\results\ci-lane | Out-Null
          New-Item -ItemType File -Force -Path bench\results\ci-lane\.reached-integration-windows | Out-Null
          cargo test --release --manifest-path rust\Cargo.toml `
            --test cdylib_load --test dump_capabilities `
            --test host_registration --test validation_control 2>&1 |
            Tee-Object -FilePath bench\results\ci-lane\cargo-test-integration-windows.log
          exit $LASTEXITCODE

      - name: Integration targets asserted something (productivity floor, libtest)
        # min_target_blocks = 4. cdylib_load is ONE test and it is the only thing in
        # either lane that proves the shipped cdylib can be loaded at all; a sum-only
        # floor cannot see it disappear behind dump_capabilities' six.
        if: always()
        run: |
          python ci\check_suite_productivity.py `
            --suite "cargo test --test cdylib_load,dump_capabilities,host_registration,validation_control" `
            --harness libtest --lane build-test-windows `
            --lane-marker=bench\results\ci-lane\.reached-integration-windows `
            bench\results\ci-lane\cargo-test-integration-windows.log
          exit $LASTEXITCODE

      # -------------------------------------------------------
      # Mesa lavapipe ICD (Windows, mesa-dist-win).
      # Installs the CPU-only Vulkan 1.3 software rasterizer so
      # correctness tests can run without a real GPU on windows-latest.
      #
      # SwiftShader was also investigated: Google does not publish
      # prebuilt Windows binaries; building from source requires
      # cmake + clang (~20 min) — too costly for routine PR CI.
      # mesa-dist-win lavapipe (MSVC release, ~69 MB) is the right
      # Windows software ICD. Finding reported to docs/PLATFORMS.md
      # §7.4 (Link owns that file; we do not edit it here).
      #
      # Package contents: x64\ with lavapipe DLL + lvp_icd*.json.
      # 7z is available on windows-latest runners.
      # VK_LAYER_PATH is set to $VULKAN_SDK\Bin so the already-
      # installed LunarG validation layers are found by the loader.
      # -------------------------------------------------------
      - name: Install Mesa lavapipe (mesa-dist-win ${{ env.MESA_VERSION }})
        run: |
          $ver = "$env:MESA_VERSION"
          $url = "https://github.com/pal1000/mesa-dist-win/releases/download/$ver/mesa3d-$ver-release-msvc.7z"
          $archive = "$env:GITHUB_WORKSPACE\mesa3d.7z"
          $dest    = "$env:GITHUB_WORKSPACE\mesa3d"
          Write-Host "Downloading Mesa $ver from $url"
          Invoke-WebRequest -Uri $url -OutFile $archive
          & 7z x $archive "-o$dest" -y | Out-Null
          # Locate the lavapipe ICD JSON — Mesa names it lvp_icd*.json
          $icdJson = Get-ChildItem -Recurse -Path $dest -Filter "lvp_icd*.json" |
                     Sort-Object FullName | Select-Object -First 1
          if (-not $icdJson) {
            # Dump the extracted tree so the maintainer can see the real layout
            Get-ChildItem -Recurse $dest | Select-Object FullName
            Write-Error "lavapipe ICD JSON not found — check mesa-dist-win package structure"
            exit 1
          }
          Write-Host "Found lavapipe ICD: $($icdJson.FullName)"
          echo "VK_ICD_FILENAMES=$($icdJson.FullName)"  >> $env:GITHUB_ENV
          # Add the DLL directory to PATH so vkCreateInstance finds vulkan_lvp.dll
          echo "$($icdJson.DirectoryName)" >> $env:GITHUB_PATH
          # Validation layers live in the LunarG SDK Bin folder
          echo "VK_LAYER_PATH=$env:VULKAN_SDK\Bin" >> $env:GITHUB_ENV

          # -------------------------------------------------------
          # Register ICD in Windows registry.
          # The LunarG loader 1.3+ silently ignores VK_ICD_FILENAMES and
          # VK_DRIVER_FILES on elevated processes (Administrator / runneradmin
          # with UAC disabled). Registry-based ICD discovery under
          # HKLM:\SOFTWARE\Khronos\Vulkan\Drivers works regardless of elevation.
          # Source: docs/PLATFORMS.md §7.4.1 root cause analysis (Link).
          # -------------------------------------------------------
          $icdPath = $icdJson.FullName
          $driverKey = "HKLM:\SOFTWARE\Khronos\Vulkan\Drivers"
          New-Item -Path $driverKey -Force | Out-Null
          New-ItemProperty -Path $driverKey -Name $icdPath -Value 0 -PropertyType DWord -Force | Out-Null
          Write-Host "Registered Mesa lavapipe ICD in registry: $icdPath"

          # -------------------------------------------------------
          # Smoke-check: lavapipe must enumerate before we waste build time.
          # Use vulkaninfoSDK.exe (from LunarG SDK) with VK_LOADER_DEBUG=warn.
          # Hard failure if 'llvmpipe' is not in the output.
          # -------------------------------------------------------
          $env:MESA_LOG_LEVEL = "error"
          $vulkInfo = & "$env:VULKAN_SDK\Bin\vulkaninfoSDK.exe" --summary 2>&1
          Write-Host $vulkInfo
          if (-not ($vulkInfo | Select-String -Quiet -SimpleMatch "llvmpipe")) {
            Write-Error "lavapipe (llvmpipe) not found in vulkaninfo output after registry registration. Check HKLM:\SOFTWARE\Khronos\Vulkan\Drivers and mesa3d\x64\ DLL path."
            exit 1
          }
          Write-Host "lavapipe smoke-check: OK (llvmpipe enumerated)"

      # -------------------------------------------------------
      # Python test environment
      # -------------------------------------------------------
      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install Python test dependencies
        run: pip install -r tests/requirements.txt

      # -------------------------------------------------------
      # Probe Vulkan loader — same as Linux lane.
      # Hard failure if no capable device after registry registration.
      # -------------------------------------------------------
      - name: Probe Vulkan loader (epctl --probe-loader)
        run: rust\target\release\epctl.exe --probe-loader

      # -------------------------------------------------------
      # Op-correctness + barrier-parity tests (pytest, lavapipe).
      # VK_INSTANCE_LAYERS set here (not job level) — same rationale as Linux.
      # ORT DLL on PATH so the EP DLL resolves onnxruntime.dll at load time.
      # -------------------------------------------------------
      - name: Run op-correctness + barrier-parity tests (lavapipe)
        env:
          ONNXRUNTIME_VULKAN_EP_LIB: ${{ github.workspace }}\rust\target\release\onnxruntime_vulkan_ep.dll
          VK_INSTANCE_LAYERS: VK_LAYER_KHRONOS_validation
        run: |
          $env:PATH = "$env:ORT_HOME\lib;$env:PATH"
          New-Item -ItemType Directory -Force -Path bench\results\ci-lane | Out-Null
          # The lane marker: written the moment this lane can produce evidence at all,
          # so the always() checks downstream can tell "the lane published no duration"
          # from "the lane died before it could publish anything". Only the marker's
          # producer can write it, so it is never absent on a run that did publish.
          New-Item -ItemType File -Force -Path bench\results\ci-lane\.lane-reached | Out-Null
          # 2>&1 merges ORT's native stderr, where `EP_FAIL ... Falling back` is
          # written. Without it the grep step scans a log in which its own subject
          # cannot appear.
          python -m pytest tests\ops -q --tb=short `
            -p no:randomly --strict-markers 2>&1 |
            Tee-Object -FilePath bench\results\ci-lane\pytest-windows.log
          exit $LASTEXITCODE

      # -------------------------------------------------------
      # See the Linux lane for the incident. This lane skips fewer tests than Linux
      # today, which is exactly why it needs the floor as much: nobody would notice the
      # skip count climbing on the lane that is always green.
      # -------------------------------------------------------
      - name: Op-correctness step asserted something (productivity floor)
        if: always()
        run: |
          python ci\check_suite_productivity.py `
            --suite tests/ops --lane build-test-windows `
            --lane-marker=bench\results\ci-lane\.lane-reached `
            --emit-json bench\results\ci-lane\suite-productivity-windows.json `
            bench\results\ci-lane\pytest-windows.log
          exit $LASTEXITCODE

      # -------------------------------------------------------
      # FLAKE WITNESS — see the Linux lane for the full rationale. Same two mechanisms:
      # annotate the failing name where truncation cannot reach it, and accumulate a
      # ledger so the second observation of an intermittent is cheap.
      # -------------------------------------------------------
      - name: Flake witness (name the failure where truncation cannot reach it)
        if: always()
        run: |
          $rc = 0
          python ci\check_flake_witness.py `
            --harness libtest --suite "cargo test --lib" --lane build-test-windows `
            --ledger bench\results\ci-lane\flake-ledger-windows.jsonl `
            --lane-marker bench\results\ci-lane\.lane-reached `
            bench\results\ci-lane\cargo-test-lib-windows.log
          if ($LASTEXITCODE -ne 0) { $rc = $LASTEXITCODE }
          python ci\check_flake_witness.py `
            --harness pytest --suite tests/ops --lane build-test-windows `
            --ledger bench\results\ci-lane\flake-ledger-windows.jsonl `
            --lane-marker bench\results\ci-lane\.lane-reached `
            bench\results\ci-lane\pytest-windows.log
          if ($LASTEXITCODE -ne 0) { $rc = $LASTEXITCODE }
          exit $rc

      # -------------------------------------------------------
      # CRITERION 10 — see the Linux lane for the full rationale.
      # Same three steps, same three failure modes, same absence
      # of any verdict-bypass escape.
      # -------------------------------------------------------
      - name: Verdict vocabulary preflight (which kind of instrument outage?)
        # See the Linux lane. Separates "this checkout does not carry
        # tests/ops/_verdict.py" (repository state) from "the file is here and this job
        # cannot import it" (lane defect) — two situations that otherwise produce the
        # identical ERROR(instrument=verdict_vocabulary_unavailable) in every step below.
        run: |
          python ci\check_vocabulary.py --github-summary
          exit $LASTEXITCODE

      - name: Criterion 10 gate artifact (gate_chain_fp32, lavapipe)
        env:
          ONNXRUNTIME_VULKAN_EP_LIB: ${{ github.workspace }}\rust\target\release\onnxruntime_vulkan_ep.dll
          ONNXRUNTIME_EP_VULKAN_COUNTERS_FILE: ${{ github.workspace }}\bench\results\ci-lane\counters-windows.json
        run: |
          $env:PATH = "$env:ORT_HOME\lib;$env:PATH"
          New-Item -ItemType Directory -Force -Path bench\results\ci-lane | Out-Null
          New-Item -ItemType File -Force -Path bench\results\ci-lane\.reached-gatechain-windows | Out-Null
          python ci\gate_chain_fp32.py `
            --verdict-out bench\results\ci-lane\verdict-windows.json `
            --workdir bench\results\ci-lane
          exit $LASTEXITCODE

      - name: Criterion 10 verdict gate (independent reader)
        run: |
          python ci\check_verdict.py bench\results\ci-lane\verdict-windows.json
          exit $LASTEXITCODE

      - name: Criterion 10 verdict gate (epctl --check-counters)
        run: |
          rust\target\release\epctl.exe --check-counters `
            bench\results\ci-lane\counters-windows.json --require-dispatches 1
          exit $LASTEXITCODE

      - name: Known-fatal log line is a lane failure (R13 second witness)
        # One marker per PRODUCER, not one per step. `.lane-reached` is written by the
        # pytest step; `gate_chain_fp32_ort_stderr.log` is written by a later step, so
        # `.lane-reached` says nothing about whether THAT step ran. Passing one marker for
        # both is how this step reported ERROR(instrument=log_not_captured) on 2026-08-04
        # for a gate step that had simply never started.
        if: always()
        run: |
          python ci\check_fatal_log.py `
            --lane-marker=bench\results\ci-lane\.lane-reached `
            bench\results\ci-lane\pytest-windows.log `
            --lane-marker=bench\results\ci-lane\.reached-gatechain-windows `
            bench\results\ci-lane\gate_chain_fp32_ort_stderr.log
          exit $LASTEXITCODE

      - name: Device-loss screen on this lane's own evidence (Windows)
        if: always()
        # See the Linux lane for the rationale. Windows matters more here, not less:
        # VK_ERROR_DEVICE_LOST on Windows is usually a TDR, and both device losses this
        # project has recorded happened on this OS.
        run: |
          python ci\check_device_loss.py `
            --lane-marker=bench\results\ci-lane\.lane-reached `
            --run-log bench\results\ci-lane\pytest-windows.log `
            --run-log bench\results\ci-lane\gate_chain_fp32_ort_stderr.log `
            bench\results\ci-lane
          exit $LASTEXITCODE

      # -------------------------------------------------------
      # No-ICD fallback test (always runs, even without a build).
      # Proves: zero devices advertised → session falls back to CPU
      # gracefully. M0 exit criterion, Windows-specific coverage.
      # Overrides VK_ICD_FILENAMES to a nonexistent path, so the
      # Vulkan loader returns zero devices regardless of lavapipe.
      # -------------------------------------------------------
      - name: Run no-ICD fallback test
        run: |
          $env:VK_ICD_FILENAMES = "C:\nonexistent\lvp_icd.json"
          $env:VK_DRIVER_FILES  = "C:\nonexistent\lvp_icd.json"
          New-Item -ItemType Directory -Force -Path bench\results\ci-lane | Out-Null
          New-Item -ItemType File -Force -Path bench\results\ci-lane\.reached-noicd-windows | Out-Null
          if (Test-Path "rust\target\release\onnxruntime_vulkan_ep.dll") {
            $env:ONNXRUNTIME_VULKAN_EP_LIB = "$env:GITHUB_WORKSPACE\rust\target\release\onnxruntime_vulkan_ep.dll"
          }
          python -m pytest tests\ops\test_claim_diagnostics.py::test_no_vulkan_icd_falls_back_to_cpu `
            -q --tb=short 2>&1 |
            Tee-Object -FilePath bench\results\ci-lane\pytest-no-icd-windows.log
          exit $LASTEXITCODE

      - name: No-ICD fallback step asserted something (productivity floor)
        # A one-test step, and therefore the shape the 2026-08-03 sweep was looking for:
        # this node id is the step's ENTIRE content, so if the test is renamed, moved, or
        # becomes a skip, "the no-ICD fallback works" and "nothing ran" produce the same
        # log. Floor of 1 is not a guess; it is the whole suite.
        if: always()
        run: |
          python ci\check_suite_productivity.py `
            --suite "tests/ops/test_claim_diagnostics.py::test_no_vulkan_icd_falls_back_to_cpu" `
            --lane build-test-windows `
            --lane-marker=bench\results\ci-lane\.reached-noicd-windows `
            bench\results\ci-lane\pytest-no-icd-windows.log
          exit $LASTEXITCODE

      # -------------------------------------------------------
      # POSITIVE CONTROL FOR THE GATE ITSELF (R9).
      #
      # Evidence scales only with falsifying instruments. The three
      # gate steps above have, on a healthy run, only ever been
      # observed to pass — and a check observed only to pass is not
      # known to be a check. This step removes the ICD, so the EP
      # can execute nothing, and requires the gate to say so.
      #
      # It is the cheapest available proof that a green lane above
      # means something: the SAME artifact, the SAME script, one
      # environment variable different, and it must come back
      # UNATTRIBUTED with a non-zero exit. If this step ever passes
      # silently, every green in this lane is uninterpretable.
      #
      # UNATTRIBUTED and not DIVERGENT, asserted by name: the model
      # was not wrong, the subject was absent (§10.0 clause 4).
      # -------------------------------------------------------
      - name: Gate negative control — no ICD must produce UNATTRIBUTED
        env:
          ONNXRUNTIME_VULKAN_EP_LIB: ${{ github.workspace }}\rust\target\release\onnxruntime_vulkan_ep.dll
        run: |
          $env:PATH = "$env:ORT_HOME\lib;$env:PATH"
          $env:VK_ICD_FILENAMES = "C:\nonexistent\lvp_icd.json"
          $env:VK_DRIVER_FILES  = "C:\nonexistent\lvp_icd.json"
          $env:ONNXRUNTIME_EP_VULKAN_COUNTERS_FILE = ""
          New-Item -ItemType Directory -Force -Path bench\results\ci-lane | Out-Null
          # ---------------------------------------------------------------
          # FIRST: did the suppression take? On Windows this is a real
          # question, not a formality. PLATFORMS.md §7.4.1: the LunarG
          # loader SILENTLY IGNORES VK_DRIVER_FILES/VK_ICD_FILENAMES in
          # elevated processes, and GitHub's Windows runners are elevated —
          # which is exactly why this lane registers lavapipe in the
          # registry instead. If the ICD is still there, the gate will
          # execute and pass, and reporting that as "the gate cannot fail"
          # would blame the gate for a control that never fired. That is a
          # real detection's costume on an instrument outage, and it routes
          # the finding to the wrong owner (R13).
          #
          # This used to be an inline `-match 'passed the §7.2 capability
          # gate'`, and that test was ALWAYS TRUE: the report line reads
          # "{n} device(s) passed the §7.2 capability gate." and n is 0 when
          # the suppression DID take. So the short-circuit fired on every
          # run and this control has never once been exercised. A detector
          # that fires on every input is a constant, not a detector. The
          # count is the discriminator, so ci/check_icd_suppression.py
          # parses the count, has two-polarity tests, and writes a record
          # whose content varies with its input (R10) instead of an
          # annotation that leaves no artifact.
          # ---------------------------------------------------------------
          rust\target\release\epctl.exe --probe-loader *> bench\results\ci-lane\icd-probe-report.txt
          $probeExit = $LASTEXITCODE
          Get-Content bench\results\ci-lane\icd-probe-report.txt
          python ci\check_icd_suppression.py bench\results\ci-lane\icd-probe-report.txt `
            --record-out bench\results\ci-lane\icd-suppression.json `
            --exit-code $probeExit
          if ($LASTEXITCODE -ne 0) {
            Write-Host "::warning title=ICD-removal negative control did not fire::ERROR(instrument) — see bench/results/ci-lane/icd-suppression.json. This step asserts NOTHING about the gate: it did not reach its observation. The gate's falsifier on this lane is the loader-independent decline-probe control in the next step, which is mandatory and cannot be skipped."
            exit 0
          }
          $out = python ci\gate_chain_fp32.py `
            --verdict-out bench\results\ci-lane\negative-control.json `
            --workdir bench\results\ci-lane 2>&1 | Out-String
          $code = $LASTEXITCODE
          Write-Host $out
          # Quote the text, never the count (R13 second clause): the assertion is on
          # the token the gate printed, not on how many things went wrong.
          if ($code -eq 0) {
            Write-Error "NEGATIVE CONTROL FAILED: the gate passed with no Vulkan ICD present. It cannot distinguish a run that executed from one that did not, so every green in this lane is uninterpretable."
            exit 1
          }
          if ($out -notmatch 'FAIL\(condition=UNATTRIBUTED\)') {
            Write-Error "NEGATIVE CONTROL FAILED: the gate exited $code but did not report FAIL(condition=UNATTRIBUTED). An instrument error is not a detection (R13); a red that could mean either is not a signal."
            exit 1
          }
          if ($out -match 'FAIL\(condition=DIVERGENT\)') {
            Write-Error "NEGATIVE CONTROL FAILED: UNATTRIBUTED was reported as DIVERGENT. These have different owners and different fixes and must never be folded together."
            exit 1
          }
          Write-Host "NEGATIVE CONTROL PASSED: with no ICD the gate reported FAIL(condition=UNATTRIBUTED) and exited $code. The gate can fail, so its passes are readable."
          # This step's own verdict is PASS at this point, not $code (issue #49): $code is
          # the gate script's INTENTIONALLY non-zero exit, and `Write-Host` never touches
          # $LASTEXITCODE. Without an explicit exit here, GitHub's generated `pwsh -command`
          # wrapper appends `if ((Test-Path variable:\LASTEXITCODE)) { exit $LASTEXITCODE }`,
          # which would still read $code — the STALE exit status of the command this step
          # exists to declare successfully non-zero — and report this passing step as failed.
          exit 0

      # -------------------------------------------------------
      # THE FALSIFIER THIS LANE ACTUALLY RELIES ON.
      #
      # Loader-independent, so it fires on an elevated runner where the
      # step above cannot. `--artifact decline_probe` is a single `Det`
      # node — an op this EP does not implement. The EP loads, lavapipe
      # is present, the device passes the §7.2 gate, and the EP still
      # claims nothing: the graph runs on CPU and the lane must report
      # FAIL(condition=UNATTRIBUTED).
      #
      # This is the stronger negative of the two. "The EP could not
      # start" and "the EP started and executed nothing" are different
      # failures; the second is the one that was live on 2026-07-30 and
      # the one a green lane hid.
      # -------------------------------------------------------
      - name: Gate negative control — a declined artifact must produce UNATTRIBUTED
        env:
          ONNXRUNTIME_VULKAN_EP_LIB: ${{ github.workspace }}\rust\target\release\onnxruntime_vulkan_ep.dll
          ONNXRUNTIME_EP_VULKAN_COUNTERS_FILE: ""
        run: |
          $env:PATH = "$env:ORT_HOME\lib;$env:PATH"
          New-Item -ItemType Directory -Force -Path bench\results\ci-lane | Out-Null
          $out = python ci\gate_chain_fp32.py --artifact decline_probe `
            --verdict-out bench\results\ci-lane\negative-control-decline.json `
            --workdir bench\results\ci-lane 2>&1 | Out-String
          $code = $LASTEXITCODE
          Write-Host $out
          if ($code -eq 0) {
            Write-Error "NEGATIVE CONTROL FAILED: the gate passed on an artifact the EP is supposed to decline. Either the EP now claims that op — in which case this control has silently stopped being one and must be re-pointed at an op that is still declined — or the gate cannot tell execution from non-execution with the loader healthy, which is worse."
            exit 1
          }
          if ($out -notmatch 'FAIL\(condition=UNATTRIBUTED\)') {
            Write-Error "NEGATIVE CONTROL FAILED: the gate exited $code on the declined artifact but did not report FAIL(condition=UNATTRIBUTED). An instrument error is not a detection (R13)."
            exit 1
          }
          Write-Host "NEGATIVE CONTROL PASSED (loader untouched): with a healthy EP that claimed nothing, the gate reported FAIL(condition=UNATTRIBUTED) and exited $code."
          # See the sibling ICD-removal control above (issue #49): this step's verdict is
          # PASS here, not $code, which is the gate's deliberately non-zero exit. Without
          # this explicit exit, GitHub's implicit `exit $LASTEXITCODE` wrapper reads that
          # stale value and reports a step whose own log says PASSED as failed.
          exit 0

      # -------------------------------------------------------
      # OBLIGATION 8 — see the Linux lane for the full rationale.
      # A duration published without a device-state record covering the
      # statistic's own suffix is STEADY_UNCERTIFIED; on a host with no
      # producer at all it is ERROR(instrument), never a pass, because
      # "no telemetry therefore no requirement" makes the cheapest pass
      # a platform that cannot be audited — and a CI runner with no GPU
      # telemetry is that loophole at scale.
      # -------------------------------------------------------
      - name: No duration without a device-state record (§10.0 obligation 8)
        if: always()
        run: |
          python ci\check_device_state.py `
            --scan bench\results\ci-lane `
            --lane-marker bench\results\ci-lane\.lane-reached `
            --summary "$env:GITHUB_STEP_SUMMARY"

      - name: Lane inventory - operational vs green
        if: always()
        run: |
          New-Item -ItemType Directory -Force -Path bench\results\ci-lane | Out-Null
          python ci\check_lane_inventory.py `
            --workflow .github/workflows/ci.yml `
            --out bench\results\ci-lane\lane-inventory.json `
            --summary "$env:GITHUB_STEP_SUMMARY"
          exit $LASTEXITCODE

      - name: Upload lane evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: lane-evidence-windows
          path: bench/results/ci-lane/
          if-no-files-found: warn
