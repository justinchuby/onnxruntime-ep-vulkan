{
  "schema": 2,
  "purpose": "Six-state census of every instrument this EP emits, across two domains. The `uninvoked`/`ambiguous` (Rust) and `harness_uninvoked`/`harness_unfalsified` (Python) arrays are MACHINE-GENERATED by audit_instruments.py --write-baseline and are drift-checked by --check. Everything under `hand` is hand-classified: `unreachable`, `misnamed`, `out-of-frame` and `absent` are not decidable from text.",
  "domains": {
    "rust": "rust/src, INSTRUMENT_FILES. Owner: Tank. Question: has this instrument got a production caller?",
    "harness": "tests/, HARNESS_INSTRUMENT_FILES. Owner: Trinity. Question: has anything in the always-on lane ever watched this guard DISAGREE? Added at schema 2 because Guard D (assert_vulkan_executed_runtime) had four production callers from the day it landed -- the rust-domain question would have answered WIRED and been right -- and raised NameError at its first statement for its entire life, having never read a profiling event. Same census, same drift semantics, same vocabulary; one more state."
  },
  "consumers": [
    "rust/tools/audit_instruments.py --check (CI-shaped drift gate, both domains)",
    "Trinity: M0 criterion 12 wiring census. Consume this file rather than building a second one."
  ],
  "states": {
    "absent": "no listener exists at all",
    "uninvoked": "exists, never called from production code (tests do not count)",
    "unfalsified": "HARNESS DOMAIN ONLY. Called, and possibly called often, but no always-on test has ever observed it in BOTH polarities -- rejecting a bad world AND accepting a good one. A guard that always passes, always crashes, or has inverted polarity is indistinguishable from a working one in this state. Later in the ordering than `uninvoked` because it survives the caller question; earlier than `unreachable` because a paired self-test settles it without hardware. Decided from the test AST: screened iff a non-GPU-gated test calls it inside pytest.raises(...) AND another non-gated test calls it outside one. Both are required -- a reject-only suite certifies a guard that rejects everything.",
    "unreachable": "called, but its output goes somewhere nothing reads",
    "misnamed": "wired, invoked, correct \u2014 and its name misdescribes its content",
    "out-of-frame": "wired, invoked, read, arithmetically correct - and the event it counts CANNOT OCCUR in the frame it observes, so its zero is not a measurement. Reports UNOBSERVABLE, never 0 (DESIGN.md 10.0.1 R12). A FIFTH state, not a split of `unreachable`: unreachable is fixed by wiring a reader, out-of-frame is fixed only by changing the world being measured (here, 6.5's one-VkDevice rule). Different repairs, so they must not share a name."
  },
  "rulings": {
    "R13-is-not-a-seventh-state": "RULED by Tank, 2026-08-01. R13 (an instrument's failure is spelled the same way as its finding) does NOT add a census state. Ask what a state is a property OF: all six above are properties of an instrument's POSITION IN THE SYSTEM -- does it exist, is it called, has it been seen to discriminate, does anything read it, can its event occur, does its name match. Guard D's own position was already covered: invoked and never in a position to report is `unreachable`, whose definition is exactly 'ran, produced nothing observable'. What R13 names is a property of the CHANNEL the verdict travels down -- pytest's summary line has a two-token alphabet and was carrying three states -- and that applies to every row of this census at once. An out-of-frame counter quoted through a two-token gate is misread exactly as badly as a crashed guard. A state that applies to all states is not a state; it is an axis. CONSEQUENCE, which is mechanical rather than taxonomic: audit_instruments.py now reports three terminal tokens (PASS=0, FAIL(drift)=1, ERROR(instrument)=2) and any escape from main() becomes ERROR(instrument), because before this a drift and a traceback left through the same door.",
    "census-timeout-classification": "RULED by Tank, 2026-08-01. `subprocess.TimeoutExpired` in tests/ops/test_wiring_census.py is ERROR(instrument), never FAIL(condition). The census is deterministic and byte-based: its answer does not depend on how loaded the box is, so a timeout is evidence about the box and none about the call graph. Under four-agent contention this box has been measured at 4.4x (708 s loaded vs 161 s quiet on the same suite), and a coordinator already misread the resulting timeouts as a 68-failure regression that did not exist. A lane that records a census timeout has NOT run the census, whatever else it reports, and must not be counted as a detection in either direction. Trinity owns the timeout value; this classification is Tank's and is not negotiable by raising the timeout.",
    "census_frame_is_declared_not_implied": "Tank, 2026-08-02, on Niobe's finding. The census scanned rust/src and tests/ and printed CENSUS VERDICT: PASS, which was true of what it scanned and read as true of the tree; bench/ -- the whole certification apparatus, including the SM-clock record that was the only instrument to refuse a device-clock series reading STEADY at RSD 0.0717% and 20.18x wrong -- had never been in any census's frame. Ruling: the defect was the silence, not the scope. bench/ is now IN FRAME as a third domain; every top-level source directory carries an IN/OUT decision with a reason (FRAME_DIRS); every bench/*.py is screened or held out with a reason (BENCH_HELD_OUT); anything declared neither way is FAIL(drift). R12 applied to the census itself: 'I did not look there' is a different fact from 'I looked and found nothing'.",
    "bench_selection_is_structural_not_lexical": "The bench domain selects every module-public top-level function of a declared module (the same rule the Rust screen uses for `pub fn`), not a name vocabulary. Evidence: bench/phases.py has 37 top-level functions and the harness name regex selects 0 of them, while that file holds gpu_steady_tail, decomposition_identity, phase_containment, trace_matches_counters, valid_bits_applied and red_flags. A lexical extension would have printed 'bench/ scanned' over a module in which it saw nothing -- `absent`, dressed as coverage.",
    "bench_unfalsified_is_a_property_of_the_screen": "85 bench rows read `unfalsified` because polarity is detected from pytest.raises and most bench instruments are TOTAL functions returning a token (STEADY / MARGINAL_TAIL / NO_STEADY_TAIL / SOLE_TENANT). This is the same limit already recorded for `classify_`. The count is not a verdict on Niobe's tests. Crediting a polarity the screen did not observe would be Guard D with the sign flipped. Handed to Niobe: a value-polarity model for total instruments, or a per-row note."
  },
  "uninvoked": [
    "counters.rs::on_arena_insert",
    "counters.rs::on_arena_release",
    "counters.rs::on_arena_remove",
    "counters.rs::to_json",
    "trace.rs::note_thread",
    "trace.rs::observes_gpu_work",
    "trace.rs::op_span",
    "trace.rs::op_timer_start",
    "trace.rs::record_op_meta",
    "trace.rs::record_path",
    "trace.rs::transfer_totals"
  ],
  "ambiguous": [
    "counters.rs::extract_equivalence",
    "counters.rs::extract_equivalence_record"
  ],
  "harness_uninvoked": [
    "tests/ops/_models.py::assert_qdq_reference_oracle_safe"
  ],
  "harness_unfalsified": [
    "tests/ops/_models.py::assert_ep_in_session",
    "tests/ops/_models.py::assert_matches_cpu",
    "tests/ops/_models.py::assert_vulkan_claims",
    "tests/ops/_models.py::assert_vulkan_does_not_claim",
    "tests/ops/_models.py::check",
    "tests/ops/_models.py::count_vulkan_executions_from_profile",
    "tests/ops/_models.py::write_unmeasured_verdict",
    "tests/ops/_verdict.py::classify_plant_run",
    "tests/ops/_verdict.py::classify_validation_probe",
    "tests/ops/conftest.py::_assert_oracle_versions",
    "tests/ops/conftest.py::_classify_failure",
    "tests/ops/conftest.py::require_vulkan"
  ],
  "hand": {
    "harness_notes": [
      {
        "instrument": "bench/devices.py::identify_by_uuid",
        "state": "SCREENED as of 2026-08-06 - and the record of how, because the first answer here was wrong",
        "artifact": "bench/test_devices_identity.py: 16 always-on tests, no GPU. Accept polarity on the issue #18 crux (two cards with the IDENTICAL deviceName, distinct UUIDs, the right one selected, compared by `is` not `==` because those two records are equal on every other field). Reject polarity on all four documented refusals, each asserted to return a DIFFERENT sentence: uuid absent, uuid unmatched, uuid matching two probed devices (a probe() defect, not an ambiguity), and a probed card that reports no uuid at all. Five planted mutants - case-sensitive compare, first-match-wins, absent-uuid-matches-anything, falls-back-to-name (the rejected 11a7c69 shape), and refuses-without-saying-why - are each put through the IDENTICAL protocol and every one must be caught.",
        "why_this_entry_exists": "The honest history, not the tidy version. This instrument was first flagged UNFALSIFIED and the first response was to baseline it here with a note saying the raise-based screen structurally cannot see a total function's refusal. That note was TRUE and it was still the wrong move: it converted an open question into a permanent one, and `unfalsified` is a statement about what has been watched, which a note does not change. The screen was extended instead - see VALUE_REJECT_FN in audit_instruments.py - so that a call wrapped in bench/_polarity.py::refuses(...) counts as reject polarity, because that helper RAISES when the thing inside it did not refuse. It is an assertion, not an annotation; that is the property pytest.raises has and a hand note does not. Kept in this file as a warning about the two rejected alternatives: forcing an exception contract onto a deliberately-total function would have made production worse to make a screen greener, and identify_by_uuid's caller device_identity_check needs the refusal's `why` string to print the reason a device name was withheld.",
        "still_unscreenable_here": "bench/devices.py::identify_by_timestamp is the same total shape and is still baselined under bench_unfalsified. It is now screenable by the same mechanism and is NOT claimed to be screened; whoever gives it the same treatment should delete this sentence.",
        "owner": "tank"
      },
      {
        "instrument": "tests/ops/_watchdog.py::StallGuard.raise_if_stalled and WorkClock.assert_alive",
        "state": "OUT-OF-FRAME for this screen, by construction - falsified elsewhere",
        "artifact": "tests/ops/test_stall_guard.py: 13 always-on tests, no GPU, driven by a fake work clock so they are deterministic. Both polarities of every property: slow-but-progressing never trips / silence trips; at-budget passes / one-over fails; the SAME wall silence trips on a fast clock and does not trip on a slow one (the load-invariance crux); a hang on a loaded machine still trips; a dead clock is ERROR(instrument) and not a pass; MECHANISM and TOOLCHAIN stalls print different verdicts. Four-cell end-to-end demonstration against the real census in tests/ops/probe_stall_guard.py, artifact bench/results/stall_guard_arms-dev{N}.json with arms_must_differ.",
        "why_out_of_frame": "This screen scans module-level functions; both instruments are methods, so adding ops/_watchdog.py to HARNESS_INSTRUMENT_FILES widened the frame without adding a scanned name. Recorded rather than left silent: a file inside the frame that contributes zero instruments looks identical to a file that has none.",
        "misnaming_caught_here": "The method was first called `check`. The screen tallies calls by bare function name, so every guard.check() in the census was credited to tests/ops/_models.py::check and moved it UNFALSIFIED -> SCREENED. That is `misnamed` in this file's own vocabulary and it was caught by running the census, not by reading the diff. Renamed to raise_if_stalled.",
        "owner": "trinity"
      },
      {
        "instrument": "tests/ops/_models.py::assert_vulkan_executed_runtime (Guard D)",
        "state": "SCREENED - the only one, as of 2026-07-31",
        "artifact": "tests/ops/test_guard_d.py: 12 always-on tests, no GPU, no model, synthesised ORT profiling JSON. Negative polarity (zero VulkanEP Node events -> AssertionError), positive polarity (one event -> returns 1), plus four MUTANT guards that must each FAIL the same protocol - including a byte-accurate reconstruction of the NameError defect that shipped.",
        "why_it_needed_this": "It had four production callers from the day it landed and had never read a profiling event. The rust-domain question ('has it got a caller?') answers WIRED and is correct. On merge the suite went 8 passed -> 5 failed and that redness was reported to the team as 'Guard D works'; it was crashing. A guard that raises NameError and a guard that correctly detects a fallback are indistinguishable to every reader we have.",
        "owner": "trinity"
      },
      {
        "instrument": "tests/ops/_models.py::assert_qdq_reference_oracle_safe",
        "state": "UNINVOKED - real finding, surfaced by this screen on its first run",
        "artifact": "none. conftest.py:341 prints a banner instructing readers to call it before any Q/DQ-23 ReferenceEvaluator oracle path, and tests/ops/test_matmulnbits.py:121 cites it in a comment. Both are prose. There is no call.",
        "excludes": "the entire Q/DQ-23 oracle-safety question. The [ORACLE STATUS] line printed on every run is computed by conftest independently; the guard that would FAIL a test using an unsafe oracle has never run.",
        "disposition": "FLAGGED. Trinity's file, Trinity's fix - not done in this pass because wiring it changes which tests fail and that belongs in its own change with its own two-polarity self-test. Recorded here so it cannot age quietly, which is the whole point of the baseline.",
        "owner": "trinity"
      },
      {
        "instrument": "assert_vulkan_claims / assert_vulkan_does_not_claim / assert_matches_cpu / assert_ep_in_session / check",
        "state": "UNFALSIFIED",
        "artifact": "many accept-polarity observations (assert_vulkan_claims: 14, assert_matches_cpu: 9) and ZERO reject-polarity observations across the whole suite.",
        "excludes": "the only question that matters about a guard. Every one of these has been watched agreeing and none has been watched disagreeing, so the suite cannot distinguish any of them from `def guard(*a, **k): return`. This is not a claim that they are broken - it is the statement that we would not know.",
        "disposition": "FLAGGED, tracked by baseline. Falsifying these needs a synthesised bad world per guard, the way test_guard_d.py synthesises a CPU-only profiling trace; some (assert_vulkan_claims) need a real session and are harder. Ordered by blast radius: assert_matches_cpu first - it is the correctness oracle, and a vacuous correctness oracle is the failure this project has hit four times.",
        "owner": "trinity"
      },
      {
        "instrument": "tests/ops/_models.py::write_unmeasured_verdict",
        "state": "UNFALSIFIED, and it must stay that way \u2014 baselined deliberately, not by neglect",
        "artifact": "the model_output_equivalence / model_output_equivalence_record pair in the counters JSON, written from the Guard-A and instrument-outage bail-out paths of test_phi35_vulkan_matches_cpu_logits",
        "excludes": "nothing it claims. It is a WRITER, not a guard, and the census's screening rule (called inside pytest.raises AND outside one) asks a question it cannot answer: it swallows every write failure BY DESIGN. R13 \u2014 an instrument error never counts as a detection, and it must not erase one either. These call sites are already failing for a better-diagnosed reason and a crash in the recorder must not overwrite the finding on its way up the stack.",
        "disposition": "BASELINED WITH A NOTE, which is the sanctioned alternative to a self-test and is used here rather than manufacturing a pytest.raises the function can never satisfy. Its content and its silence ARE falsified in the always-on lane: test_verdict.py::test_unmeasured_writer_records_unmeasured_and_cannot_record_match (accept polarity + proof no argument produces MATCH), ::test_unmeasured_writer_swallows_a_broken_path_but_the_writer_under_it_does_not (the swallow, WITH the control showing write_equivalence_record genuinely raises on the same input \u2014 without it a no-op would pass), and ::test_unmeasured_writer_declines_a_missing_counters_path.",
        "replaces": "tests/ops/_models.py::write_equivalence_verdict, deleted 2026-07-31. That function took the verdict token as a string LITERAL from its caller, so `m.write_equivalence_verdict(path, m.EQUIVALENCE_MATCH)` was a legal sentence at a zero own-provider count \u2014 R10's exact shape, and the specimen \u00a710.0's third metric amendment was written about. Its replacement cannot express that sentence: EquivalenceVerdict.from_comparison() takes a comparison OUTCOME and an ExecutionAttribution instance, and MATCH is a derived property, never an input.",
        "owner": "trinity"
      },
      {
        "instrument": "tests/ops/_models.py::count_vulkan_executions_from_profile",
        "state": "UNFALSIFIED as a guard, and an R11 hazard as a name",
        "artifact": "the integer Guard D returns and every line that prints it",
        "excludes": "nothing arithmetically - it counts exactly what it says. The hazard is the READING: ORT emits one Node event per FUSED ISLAND, and on Phi-3.5 354 of 364 graph nodes execute inside ONE island. A healthy run therefore reports 1, and a reader who takes 'executions' to mean nodes reads the best result this project has produced as a catastrophe.",
        "disposition": "NOT renamed (compatibility outranks elegance, per Tank on Phase::Record). Tagged instead: describe_vulkan_execution_count() is now the single wording, every print site goes through it, and the failure message states what was counted. R11's remedy is available to the writer, and this is it.",
        "owner": "trinity"
      },
      {
        "instrument": "tests/ops/_verdict.py::classify_validation_probe, ::classify_plant_run",
        "state": "UNFALSIFIED by this screen, and the screen is wrong about them - a stated limit, not a pass",
        "artifact": "tests/ops/test_r13_lane.py: 6 parametrised cases for classify_validation_probe (including exit 0 with no ARMED line, which must NOT be armed) and 7 for classify_plant_run, plus test_plant_classification_reaches_all_three_states, which fails unless the SAME function returns three DIFFERENT tokens.",
        "why_the_screen_cannot_see_it": "The polarity model here is raise-based: an instrument is screened iff some non-gated test calls it inside pytest.raises AND another calls it outside one. These two are TOTAL functions - they never raise; they return the token. A total classifier therefore reads UNFALSIFIED however thoroughly it is falsified. The honest generalisation, mechanically decidable and not implemented in this pass: value-polarity - screened iff a non-gated test asserts two DIFFERENT return values for two different inputs. Recorded rather than silently excluded, because a screen that quietly drops the instruments it cannot model reports a number about a world it has not surveyed.",
        "owner": "trinity"
      },
      {
        "instrument": "tests/ops/conftest.py::_classify_failure",
        "state": "UNFALSIFIED - and it is the R13 channel itself",
        "artifact": "the PASS / FAIL(condition) / ERROR(instrument) / XPASS(stale expect) token printed for every test in the suite, plus the quoted failure text under it",
        "excludes": "nothing about its own inputs - tests/ops/test_r13_lane.py drives it through all four tokens. The screen reads it UNFALSIFIED because it is a TOTAL function: it returns the token instead of raising it, so the raise-based polarity model (called inside pytest.raises AND outside one) cannot see the discrimination that is there. This is the value-polarity gap already recorded below, now with the highest-blast-radius specimen in it.",
        "disposition": "BASELINED WITH A NOTE. It entered the screen's frame on 2026-08-01 when ops/conftest.py was added to HARNESS_INSTRUMENT_FILES (Tank's open item) and the leading-underscore names were admitted to HARNESS_FN. Before that the file that decides what every red in this suite MEANS was outside the census that reports on the instruments whose verdicts travel down it. Closing the state needs value-polarity, not a self-test: test_r13_lane.py already supplies the evidence and the screen cannot read it.",
        "owner": "trinity"
      },
      {
        "instrument": "tests/ops/conftest.py::_assert_oracle_versions",
        "state": "UNFALSIFIED",
        "artifact": "a pytest_configure-time refusal when ORT < 1.28 or onnx < 1.22.0",
        "excludes": "its own reject polarity. It is called exactly once, at collection time, and no test has ever watched it refuse - the only machine that could supply the reject polarity is one with a too-old oracle, and this suite refuses to collect on that machine by construction. That is a genuine unscreenable, not a hole somebody forgot to fill.",
        "disposition": "BASELINED WITH A NOTE. Falsifiable only by factoring the version comparison out of the assertion, which is a real improvement and is not done in this pass.",
        "owner": "trinity"
      },
      {
        "instrument": "tests/ops/conftest.py::require_vulkan",
        "state": "UNFALSIFIED - and it read UNINVOKED for one run, which was a screen defect",
        "artifact": "the skip-or-proceed decision for 37 tests",
        "excludes": "its own polarity: a fixture that skips leaves no trace a non-gated test can assert on, and a test that could watch it skip would itself be gated.",
        "disposition": "BASELINED WITH A NOTE. Recorded chiefly for the screen defect it exposed: a pytest fixture is invoked by PARAMETER NAME and never by a call expression, so the call-shaped caller model scored the most-depended-on instrument in the suite as `uninvoked calls=0` on its first run inside the frame. _fixture_instruments() now counts parameter references (calls=37). A false UNINVOKED is worse than a missing row - it is a fabricated detection, which is exactly what R13 exists to keep out of the failure channel.",
        "owner": "trinity"
      },
      {
        "instrument": "bench/real_model.py::classify_logits, ::classify_tensor, ::classify_activation, ::classify_outputs, ::bitwise_identical, ::exceeds_noise_floor, ::is_null_control, ::dispatch_diagnosis, ::fallback_diagnosis, and the arithmetic they read (::latency_stats, ::paired_ratios, ::throughput, ::softmax, ::kv_round_trip_bytes, ::bandwidth_proxy, ::feeds_digest, ::sha256_file, ::external_data_provenance, ::recorded_sha256, ::repo_cache_dir, ::phi35_cases, ::phi35_feeds, ::mobilenet_cases, ::mobilenet_feeds)",
        "state": "UNFALSIFIED by this screen, and the screen is right about the reason and wrong about the conclusion \u2014 a stated limit, not a pass",
        "artifact": "bench/test_real_model.py: 62 always-on tests, no GPU, no model, all inputs synthesised. Both polarities of every gate are watched, but by VALUE and not by raise: `classify_logits` is handed an identical pair (MATCH), a pair one ulp apart (MATCH), a pair with a flipped argmax (MISMATCH) and a pair with a blown relative error (MISMATCH), and the test asserts the token. Planted-defect controls: a gate whose tolerance is widened to infinity must stop rejecting; `bitwise_identical` fed two buffers differing in one byte must say so. Two regression controls fix the defect where `--diagnose` and the timed pass shared one default `--out` and the second run destroyed the first run's artifact.",
        "why_this_screen_cannot_see_it": "polarity here is detected from `pytest.raises`, and every gate in this module is a TOTAL function: it returns MATCH/MISMATCH/UNVERIFIED rather than raising, exactly like `bench/phases.py::gpu_steady_tail` and `tests/ops/_verdict.py::classify_*`. `resolve_model` is the one member of the module that refuses by raising \u2014 it will not guess a model path \u2014 and it is the one row the screen reads as SCREENED (reject_polarity=3). That contrast is the evidence that the UNFALSIFIED rows here are a property of the raise-based model and not of the tests.",
        "excludes": "nothing about the gates' behaviour; only this screen's ability to see it. The limit is already recorded for the harness domain (`_verdict.py::classify_*`, note 6) and for bench (`phases.py`). The standing request in the bench NOTE \u2014 'a value-polarity model for total instruments, or a note per row' \u2014 is answered here in its second form; the first form is still open and is not in issue #56's scope.",
        "owner": "niobe"
      }
    ],
    "uninvoked_notes": [
      {
        "instrument": "trace.rs::Tracer::record_path",
        "artifact": "trace summary line `compute: first-record=N replay=N rerecord=N`; now prints `record-path breakdown NOT WIRED` when all three are zero",
        "excludes": "everything. Zero production callers. It is the ONLY instrument that could falsify Phase::Record's former caveat that recording is 'amortised across replays'; that caveat shipped in every ph:\"X\" span for weeks and was false.",
        "disposition": "FLAGGED \u2014 wiring it requires a vk/session.rs edit (Switch's file). Artifact now says NOT WIRED instead of printing zeros.",
        "owner": "switch"
      },
      {
        "instrument": "trace.rs::record_op_meta / op_span / op_timer_start / note_thread",
        "artifact": "log_slowest_ops() table, now wired from ep.rs Drop and printing `per-op host time NOT WIRED` on an empty map",
        "excludes": "all per-op host attribution. The producer has no caller, so the table was empty; before this session the reporter ALSO returned silently, so the absence had two layers and printed nothing at all.",
        "disposition": "PARTIAL FIX \u2014 reporter wired + NOT WIRED message. Producer wiring belongs to op translation.",
        "owner": "mouse"
      },
      {
        "instrument": "transfer.rs::device_buffer_for",
        "artifact": "alloc_device_buffer_binds in the counters JSON (added this session; reads 0)",
        "excludes": "the 95% transfer ceiling. Zero production callers is the structural reason alloc_device_authoritative_spans is 0: nothing has ever bound one of our device buffers.",
        "disposition": "FLAGGED (feature, not defect) \u2014 the bind seam is Switch's persistent-residency work. Counter wired ahead of it deliberately.",
        "owner": "switch"
      },
      {
        "instrument": "allocator.rs::tally::on_device_authoritative",
        "artifact": "alloc_device_authoritative_spans + alloc_device_authoritative_ceiling + the audit sentence in every staging_verdict branch",
        "excludes": "nothing yet \u2014 it is the single documented increment point for the counter that will be quoted when residency lands. Uninvoked BY DESIGN until then.",
        "disposition": "INTENTIONAL \u2014 audited by ceiling (device_backed - staged) and by bind traffic so it cannot be over-claimed.",
        "owner": "tank"
      },
      {
        "instrument": "counters.rs::Counters::to_json",
        "artifact": "none in production; to_json_with_equiv is the wired path (counters.rs:355)",
        "excludes": "the equivalence verdict \u2014 it hardcodes UNMEASURED. This is the residue of Defect C (two writers, one artifact) and is now the only remaining caller-less half.",
        "disposition": "FLAGGED \u2014 harmless while unused; delete or fold if it stays uninvoked.",
        "owner": "tank"
      },
      {
        "instrument": "trace.rs::Phase::observes_gpu_work",
        "artifact": "none",
        "excludes": "n/a \u2014 predicate, tests only.",
        "disposition": "FLAGGED, low value.",
        "owner": "tank"
      },
      {
        "instrument": "vk/host_device_memory.rs::offer_shared_device",
        "artifact": "none yet, by construction - and that is the point. The observable proxy is alloc_device_frame in the counters JSON, which reads SPLIT-DEVICE on every run until this function acquires a caller and SHARED afterwards. That key is the artifact whose content varies with this function's input (R10).",
        "excludes": "n/a - it is the 6.5 seam, not a measurement. Listed so its wiring status is machine-checked rather than asserted.",
        "disposition": "FLAGGED for Switch, who owns the seam (6.5: the side that owns the lifetime owns the seam). vk/host_device_memory.rs added to INSTRUMENT_FILES so the baseline goes red the day it is called and somebody must look at the frame.",
        "owner": "switch"
      }
    ],
    "unreachable": [
      {
        "instrument": "allocator.rs GetStats KVPs (Limit, InUse, NumAllocs, MaxInUse, NumFrees, LiveSpans, QuarantinedSpans, QuarantineRetired, FailedLookups)",
        "artifact": "NONE. Nothing in this repository calls ORT's GetStats, so no run has ever produced these nine numbers.",
        "excludes": "everything they measure. failed_lookups is the costly one: its own doc says 'non-zero always indicates a bug somewhere', and it was only observable through an API nothing calls. Same shape as quarantine_retired, which we asserted had never fired while nothing had ever written it down.",
        "disposition": "FIXED for failed_lookups \u2014 plumbed process-wide (tally::on_failed_lookup at all three increment sites) and emitted as alloc_failed_lookups in the counters JSON, which the harness DOES read. The other eight remain unreachable and are partly duplicated by process-wide keys.",
        "owner": "tank"
      },
      {
        "instrument": "ops/claim_log.rs::record",
        "artifact": "the append-only claim log \u2014 but record_audit (registry.rs:1639) is the wired writer and record writes a THINNER line to the SAME file",
        "excludes": "the audit fields. Two writers, one artifact, different schemas: the exact shape of counters Defect C, where a run ending in a dispatch silently lost 27 keys. It has not bitten because record has no caller.",
        "disposition": "FLAGGED \u2014 Mouse's file, do not fix. Screen reports it as AMBIGUOUS, not uninvoked, because logging.rs has a parameter named `record`.",
        "owner": "mouse"
      }
    ],
    "misnamed": [
      {
        "instrument": "trace.rs::Phase::Record",
        "artifact": "every ph:\"X\" span in the GPU trace JSON, plus the host-time table in the tracer summary",
        "excludes": "nothing \u2014 it INCLUDES what nobody expected. It brackets vkBeginCommandBuffer..vkEndCommandBuffer, and the staging memcpy runs inside it while record_transfer deliberately emits no ph:\"X\" span. 96% of `record` is upload. Any aggregation over ph:\"X\" spans structurally cannot see upload as a separate term, so '68% recording, transfers bounded by the 30.4% fence-wait' was wrong in both halves.",
        "disposition": "FIXED in the artifact, not by rename (compatibility outranks elegance): caveat() rewritten to say it CONTAINS upload/readback and cite the 96%; a machine-readable `nested_in` arg is now emitted on every span; the summary table indents children, derives the sibling set from Phase::nested_in(), and prints a SIBLING TOTAL so nobody sums the column by hand.",
        "owner": "tank"
      },
      {
        "instrument": "epctl --require-device-memory",
        "artifact": "epctl exit code + PASS/FAIL line",
        "excludes": "vk::session's ~1997.6 MiB-per-inference host staging. It verifies ALLOCATOR SPANS only. Its own help text said 'fail unless nothing was host-staged', which is the misnaming.",
        "disposition": "FIXED \u2014 help text corrected and the PASS path now prints an explicit exclusion note naming the staging it is blind to. Exit-code semantics unchanged (it already fails the MIRRORED state).",
        "owner": "tank"
      }
    ],
    "absent": [
      {
        "instrument": "per-span transfer attribution inside Phase::Record",
        "artifact": "none \u2014 record_transfer feeds totals only, and emits no ph:\"X\" span by design",
        "excludes": "any timeline view of upload. The 96% figure is derivable only from the summary totals, not from the trace JSON.",
        "disposition": "FLAGGED \u2014 emitting a nested span is a vk/session.rs change (Switch).",
        "owner": "switch"
      }
    ],
    "out_of_frame": [
      {
        "instrument": "allocator.rs::tally::device_authoritative_spans (alloc_device_authoritative_spans)",
        "artifact": "the counters JSON, and the authoritative-audit sentence in every staging_verdict() branch",
        "excludes": "everything, in the SPLIT-DEVICE and OFF frames. The counter can only move when the engine binds one of the provider's buffers, and while the provider holds its own VkDevice (6.5) no dispatch on the session's device can bind them at all. The zero was being read as 'residency has not landed yet'; it actually meant 'this question has no answer in this run'.",
        "disposition": "FIXED. The artifact emits the JSON string \"UNOBSERVABLE\" instead of the number 0 whenever alloc_device_frame != SHARED, so a reader doing arithmetic on it fails loudly. New keys alloc_device_frame / alloc_device_frame_device carry frame provenance; epctl prints the frame on the --require-device-memory PASS path. Falsifiers: counters::a_pinned_authoritative_counter_reports_unobservable_and_never_zero, allocator::a_zero_authoritative_count_in_a_foreign_frame_says_unobservable_not_unwired, allocator::a_nonzero_authoritative_count_in_a_split_frame_is_not_credible.",
        "owner": "tank"
      },
      {
        "instrument": "allocator.rs::tally::device_upload_bytes (alloc_device_upload_bytes)",
        "artifact": "the counters JSON",
        "excludes": "vk::session's staging copies, ALL of them. It counts only copies made by the allocator's own provider, on the provider's own VkDevice, and only with ONNXRUNTIME_EP_VULKAN_DEVICE_MEMORY=1. It read 0 on a run whose cmd_upload phase was 15.2 s. Both numbers correct; different worlds - R12's specimen.",
        "disposition": "NOT renamed: it is correct in its own frame, and compatibility outranks elegance. Tagged instead, by alloc_device_frame. The session-side accounting is the separate, unconditional session_staging_upload_bytes. The two must NEVER be added.",
        "owner": "tank"
      }
    ]
  },
  "ledger": {
    "quarantine": "UNOBSERVED, not clean: peak depth 423 against a window of 4096, retired 0, use_after_free 0. The detector was NOT exhausted (peak is 10% of the window), so use_after_free == 0 is a genuine negative for this pattern \u2014 'we checked', not 'we could not have seen it'. Distinct from quarantine_retired, which was 'we could not have seen it' until it was plumbed off the GetStats path.",
    "alloc_device_authoritative_spans": "UNOBSERVABLE, not 0, as of 6.5's ruling: structurally pinned while the memory provider holds a second VkDevice, and R12 forbids any criterion naming a pinned instrument - so M1's weight-residency criterion is read against device_upload_bytes on the session's device instead. It passes through THREE distinct states and each gets its own sentence: UNOBSERVABLE (no frame in which a result could exist) -> UNWIRED zero (frame SHARED, but allocator::tally::on_device_authoritative still has no production caller) -> a measurement (transfer::device_buffer_for called). The word 'zero' never appears alone."
  },
  "states_ordering": "absent -> uninvoked -> unfalsified (harness) -> unreachable -> out-of-frame -> misnamed, by how late the failure is discoverable. R10's screen catches the first two mechanically. `unfalsified` is the third mechanical one and is the harness domain's whole contribution: it is decidable from the test AST, it needs no hardware, and it is the only state that would have caught Guard D -- which passed the caller question cleanly while crashing on its first statement. Nothing but a second, independent instrument catches the last two.",
  "bench_uninvoked": [
    "bench/devices.py::by_index"
  ],
  "bench_unfalsified": [
    "bench/admissible.py::baseline_comparability",
    "bench/admissible.py::grade",
    "bench/admissible.py::phase_share_admissibility",
    "bench/admissible.py::report",
    "bench/clock_log.py::record",
    "bench/clock_log.py::summary",
    "bench/clock_log.py::window",
    "bench/contention.py::baseline_path",
    "bench/contention.py::gate",
    "bench/contention.py::load_baseline",
    "bench/contention.py::machine_key",
    "bench/contention.py::occupancy_check",
    "bench/contention.py::occupancy_probe",
    "bench/contention.py::quiescence",
    "bench/contention.py::sample_now",
    "bench/contention.py::save_baseline",
    "bench/device_companion.py::certify",
    "bench/device_companion.py::compose",
    "bench/device_companion.py::empty_axis",
    "bench/device_companion.py::from_nvidia_record",
    "bench/devices.py::by_ep_index",
    "bench/devices.py::capture",
    "bench/devices.py::device_identity_check",
    "bench/devices.py::ep_index_of",
    "bench/devices.py::ep_selection_order",
    "bench/devices.py::find_vulkaninfo",
    "bench/devices.py::identify_by_timestamp",
    "bench/devices.py::probe",
    "bench/phases.py::analyse",
    "bench/phases.py::anchor_quality",
    "bench/phases.py::attribute",
    "bench/phases.py::attribute_gpu_ordinally",
    "bench/phases.py::contention_signature",
    "bench/phases.py::decomposition_identity",
    "bench/phases.py::device_fingerprint",
    "bench/phases.py::gpu_containment",
    "bench/phases.py::gpu_span_accounting",
    "bench/phases.py::gpu_spans",
    "bench/phases.py::gpu_steady_tail",
    "bench/phases.py::gpu_totals",
    "bench/phases.py::host_phase_totals",
    "bench/phases.py::is_leaf_phase",
    "bench/phases.py::nested_phase_names",
    "bench/phases.py::partition_stats",
    "bench/phases.py::phase_containment",
    "bench/phases.py::phase_leaf_accounting",
    "bench/phases.py::phase_nesting",
    "bench/phases.py::phase_spans",
    "bench/phases.py::record_scaling",
    "bench/phases.py::red_flags",
    "bench/phases.py::sibling_phases",
    "bench/phases.py::steady_state_split",
    "bench/phases.py::subgraph_spans",
    "bench/phases.py::timestamp_conversion_integrality",
    "bench/phases.py::trace_matches_counters",
    "bench/phases.py::transfer_events",
    "bench/phases.py::transfer_totals",
    "bench/phases.py::upload_accounting",
    "bench/phases.py::valid_bits_applied",
    "bench/portability.py::describe",
    "bench/portability.py::evaluate",
    "bench/portability.py::fits_device",
    "bench/portability.py::transfer_model_merge_refusal",
    "bench/real_model.py::arm_order",
    "bench/real_model.py::bandwidth_proxy",
    "bench/real_model.py::bitwise_identical",
    "bench/real_model.py::classify_activation",
    "bench/real_model.py::classify_logits",
    "bench/real_model.py::classify_outputs",
    "bench/real_model.py::classify_tensor",
    "bench/real_model.py::dispatch_diagnosis",
    "bench/real_model.py::exceeds_noise_floor",
    "bench/real_model.py::external_data_provenance",
    "bench/real_model.py::fallback_diagnosis",
    "bench/real_model.py::feeds_digest",
    "bench/real_model.py::is_null_control",
    "bench/real_model.py::kv_round_trip_bytes",
    "bench/real_model.py::latency_stats",
    "bench/real_model.py::mobilenet_cases",
    "bench/real_model.py::mobilenet_feeds",
    "bench/real_model.py::paired_ratios",
    "bench/real_model.py::phi35_cases",
    "bench/real_model.py::phi35_feeds",
    "bench/real_model.py::recorded_sha256",
    "bench/real_model.py::repo_cache_dir",
    "bench/real_model.py::sha256_file",
    "bench/real_model.py::softmax",
    "bench/real_model.py::throughput",
    "bench/run_disturbance.py::classify",
    "bench/run_disturbance.py::localise",
    "bench/run_disturbance.py::synthetic_jittered",
    "bench/run_disturbance.py::synthetic_uniform_slowdown",
    "bench/stats.py::comparable",
    "bench/stats.py::drift",
    "bench/stats.py::relative_delta",
    "bench/stats.py::significant",
    "bench/timestamp_audit.py::audit",
    "bench/timestamp_audit.py::conversion_self_check",
    "bench/timestamp_audit.py::cross_check",
    "bench/timestamp_audit.py::end_to_end",
    "bench/timestamp_audit.py::find_epctl",
    "bench/timestamp_audit.py::mask_ticks",
    "bench/timestamp_audit.py::probe_ep",
    "bench/timestamp_audit.py::span_ns",
    "bench/timestamp_audit.py::wrap_period_seconds",
    "bench/win_gpu_counters.py::adapters",
    "bench/win_gpu_counters.py::available",
    "bench/win_gpu_counters.py::expand_engine_paths",
    "bench/win_gpu_counters.py::live_luids",
    "bench/win_gpu_counters.py::observe",
    "bench/win_gpu_counters.py::parse_instance",
    "bench/win_gpu_counters.py::summarise"
  ],
  "not_baselined_on_purpose": {
    "tests/ops/_models.py::assert_ep_owns_whole_graph": "Trinity, 0f9a4e9. Correctly flagged unfalsified; two-polarity self-tests are owed. Baselining it would turn her open red green and delete the ask. Do NOT --write-baseline this away.",
    "tests/ops/_models.py::assert_no_cpu_fallback_is_live": "Trinity, 0f9a4e9. Same. The census is red on these two by intent, and that red is the record of an open item, not drift to be absorbed."
  }
}
