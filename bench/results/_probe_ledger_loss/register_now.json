{"retired": [{"key": "ai.onnx::SimplifiedLayerNormalization/1+/f32,f32>f32/metadata/runtime-extent/n2", "owner": "Mouse", "date": "2026-08-04", "reason": "key-schema change 2026-08-04 (Mouse, per Morpheus \u00a78.9.23): the variant component now names the dispatched module instead of the literal `metadata`, and the `#form` suffix is withdrawn because group/strides/dilations/pads and transA/transB are push-constant expressions in one uniform code path, not separate code paths. The form is not withdrawn \u2014 it is re-proven in the same session under the new key, or it collapses onto a key that is, which is what the ruling says it always was."}, {"key": "com.microsoft::SkipSimplifiedLayerNormalization/1+/f32,f32,f32>f32/metadata/runtime-extent/none", "owner": "Mouse", "date": "2026-08-04", "reason": "key-schema change 2026-08-04 (Mouse, per Morpheus \u00a78.9.23): the variant component now names the dispatched module instead of the literal `metadata`, and the `#form` suffix is withdrawn because group/strides/dilations/pads and transA/transB are push-constant expressions in one uniform code path, not separate code paths. The form is not withdrawn \u2014 it is re-proven in the same session under the new key, or it collapses onto a key that is, which is what the ruling says it always was."}, {"key": "com.microsoft::SkipSimplifiedLayerNormalization/1+/f32,f32,f32>f32,-,-,f32/metadata/runtime-extent/none", "owner": "Mouse", "date": "2026-08-04", "reason": "key-schema change 2026-08-04 (Mouse, per Morpheus \u00a78.9.23): the variant component now names the dispatched module instead of the literal `metadata`, and the `#form` suffix is withdrawn because group/strides/dilations/pads and transA/transB are push-constant expressions in one uniform code path, not separate code paths. The form is not withdrawn \u2014 it is re-proven in the same session under the new key, or it collapses onto a key that is, which is what the ruling says it always was."}, {"key": "com.microsoft::GroupQueryAttention/1+/f16,f16,f16,i32,i32,f16,f16>f16,f16,f16/metadata/runtime-extent/past_key+past_value+cos_cache+sin_cache", "owner": "Mouse", "date": "2026-08-04", "reason": "key-schema change 2026-08-04 (Mouse, per Morpheus \u00a78.9.23): the variant component now names the dispatched module instead of the literal `metadata`, and the `#form` suffix is withdrawn because group/strides/dilations/pads and transA/transB are push-constant expressions in one uniform code path, not separate code paths. The form is not withdrawn \u2014 it is re-proven in the same session under the new key, or it collapses onto a key that is, which is what the ruling says it always was."}, {"key": "ai.onnx::Conv/1+/f32,f32,f32>f32/metadata/static/n3", "owner": "Mouse", "date": "2026-08-04", "reason": "key-schema change 2026-08-04 (Mouse, per Morpheus \u00a78.9.23): the variant component now names the dispatched module instead of the literal `metadata`, and the `#form` suffix is withdrawn because group/strides/dilations/pads and transA/transB are push-constant expressions in one uniform code path, not separate code paths. The form is not withdrawn \u2014 it is re-proven in the same session under the new key, or it collapses onto a key that is, which is what the ruling says it always was."}, {"key": "ai.onnx::Conv/1+/f32,f32>f32/metadata/static/n2", "owner": "Mouse", "date": "2026-08-04", "reason": "key-schema change 2026-08-04 (Mouse, per Morpheus \u00a78.9.23): the variant component now names the dispatched module instead of the literal `metadata`, and the `#form` suffix is withdrawn because group/strides/dilations/pads and transA/transB are push-constant expressions in one uniform code path, not separate code paths. The form is not withdrawn \u2014 it is re-proven in the same session under the new key, or it collapses onto a key that is, which is what the ruling says it always was."}, {"key": "ai.onnx::Conv/1+/f32,f32,f32>f32/metadata/runtime-extent/n3", "owner": "Mouse", "date": "2026-08-04", "reason": "key-schema change 2026-08-04 (Mouse, per Morpheus \u00a78.9.23): the variant component now names the dispatched module instead of the literal `metadata`, and the `#form` suffix is withdrawn because group/strides/dilations/pads and transA/transB are push-constant expressions in one uniform code path, not separate code paths. The form is not withdrawn \u2014 it is re-proven in the same session under the new key, or it collapses onto a key that is, which is what the ruling says it always was."}, {"key": "ai.onnx::Conv/1+/f32,f32>f32/metadata/runtime-extent/n2", "owner": "Mouse", "date": "2026-08-04", "reason": "key-schema change 2026-08-04 (Mouse, per Morpheus \u00a78.9.23): the variant component now names the dispatched module instead of the literal `metadata`, and the `#form` suffix is withdrawn because group/strides/dilations/pads and transA/transB are push-constant expressions in one uniform code path, not separate code paths. The form is not withdrawn \u2014 it is re-proven in the same session under the new key, or it collapses onto a key that is, which is what the ruling says it always was."}, {"key": "ai.onnx::Conv/1+/f32,f32,f32>f32/metadata#padded/static/n3", "owner": "Mouse", "date": "2026-08-04", "reason": "key-schema change 2026-08-04 (Mouse, per Morpheus \u00a78.9.23): the variant component now names the dispatched module instead of the literal `metadata`, and the `#form` suffix is withdrawn because group/strides/dilations/pads and transA/transB are push-constant expressions in one uniform code path, not separate code paths. The form is not withdrawn \u2014 it is re-proven in the same session under the new key, or it collapses onto a key that is, which is what the ruling says it always was."}, {"key": "ai.onnx::Conv/1+/f32,f32>f32/metadata#padded/static/n2", "owner": "Mouse", "date": "2026-08-04", "reason": "key-schema change 2026-08-04 (Mouse, per Morpheus \u00a78.9.23): the variant component now names the dispatched module instead of the literal `metadata`, and the `#form` suffix is withdrawn because group/strides/dilations/pads and transA/transB are push-constant expressions in one uniform code path, not separate code paths. The form is not withdrawn \u2014 it is re-proven in the same session under the new key, or it collapses onto a key that is, which is what the ruling says it always was."}, {"key": "ai.onnx::Conv/1+/f32,f32,f32>f32/metadata#padded/runtime-extent/n3", "owner": "Mouse", "date": "2026-08-04", "reason": "key-schema change 2026-08-04 (Mouse, per Morpheus \u00a78.9.23): the variant component now names the dispatched module instead of the literal `metadata`, and the `#form` suffix is withdrawn because group/strides/dilations/pads and transA/transB are push-constant expressions in one uniform code path, not separate code paths. The form is not withdrawn \u2014 it is re-proven in the same session under the new key, or it collapses onto a key that is, which is what the ruling says it always was."}, {"key": "ai.onnx::Conv/1+/f32,f32>f32/metadata#padded/runtime-extent/n2", "owner": "Mouse", "date": "2026-08-04", "reason": "key-schema change 2026-08-04 (Mouse, per Morpheus \u00a78.9.23): the variant component now names the dispatched module instead of the literal `metadata`, and the `#form` suffix is withdrawn because group/strides/dilations/pads and transA/transB are push-constant expressions in one uniform code path, not separate code paths. The form is not withdrawn \u2014 it is re-proven in the same session under the new key, or it collapses onto a key that is, which is what the ruling says it always was."}, {"key": "ai.onnx::Conv/1+/f32,f32,f32>f32/metadata#base/runtime-extent/n3", "owner": "Mouse", "date": "2026-08-04", "reason": "key-schema change 2026-08-04 (Mouse, per Morpheus \u00a78.9.23): the variant component now names the dispatched module instead of the literal `metadata`, and the `#form` suffix is withdrawn because group/strides/dilations/pads and transA/transB are push-constant expressions in one uniform code path, not separate code paths. The form is not withdrawn \u2014 it is re-proven in the same session under the new key, or it collapses onto a key that is, which is what the ruling says it always was."}, {"key": "ai.onnx::Conv/1+/f32,f32,f32>f32/metadata#strided+padded/runtime-extent/n3", "owner": "Mouse", "date": "2026-08-04", "reason": "key-schema change 2026-08-04 (Mouse, per Morpheus \u00a78.9.23): the variant component now names the dispatched module instead of the literal `metadata`, and the `#form` suffix is withdrawn because group/strides/dilations/pads and transA/transB are push-constant expressions in one uniform code path, not separate code paths. The form is not withdrawn \u2014 it is re-proven in the same session under the new key, or it collapses onto a key that is, which is what the ruling says it always was."}, {"key": "ai.onnx::Conv/1+/f32,f32,f32>f32/metadata#grouped+padded/runtime-extent/n3", "owner": "Mouse", "date": "2026-08-04", "reason": "key-schema change 2026-08-04 (Mouse, per Morpheus \u00a78.9.23): the variant component now names the dispatched module instead of the literal `metadata`, and the `#form` suffix is withdrawn because group/strides/dilations/pads and transA/transB are push-constant expressions in one uniform code path, not separate code paths. The form is not withdrawn \u2014 it is re-proven in the same session under the new key, or it collapses onto a key that is, which is what the ruling says it always was."}, {"key": "ai.onnx::Conv/1+/f32,f32,f32>f32/metadata#grouped+strided+padded/runtime-extent/n3", "owner": "Mouse", "date": "2026-08-04", "reason": "key-schema change 2026-08-04 (Mouse, per Morpheus \u00a78.9.23): the variant component now names the dispatched module instead of the literal `metadata`, and the `#form` suffix is withdrawn because group/strides/dilations/pads and transA/transB are push-constant expressions in one uniform code path, not separate code paths. The form is not withdrawn \u2014 it is re-proven in the same session under the new key, or it collapses onto a key that is, which is what the ruling says it always was."}, {"key": "ai.onnx::Conv/1+/f32,f32,f32>f32/metadata#dilated/static/n3", "owner": "Mouse", "date": "2026-08-04", "reason": "key-schema change 2026-08-04 (Mouse, per Morpheus \u00a78.9.23): the variant component now names the dispatched module instead of the literal `metadata`, and the `#form` suffix is withdrawn because group/strides/dilations/pads and transA/transB are push-constant expressions in one uniform code path, not separate code paths. The form is not withdrawn \u2014 it is re-proven in the same session under the new key, or it collapses onto a key that is, which is what the ruling says it always was."}, {"key": "ai.onnx::GlobalAveragePool/1+/f32>f32/metadata/static/n1", "owner": "Mouse", "date": "2026-08-04", "reason": "key-schema change 2026-08-04 (Mouse, per Morpheus \u00a78.9.23): the variant component now names the dispatched module instead of the literal `metadata`, and the `#form` suffix is withdrawn because group/strides/dilations/pads and transA/transB are push-constant expressions in one uniform code path, not separate code paths. The form is not withdrawn \u2014 it is re-proven in the same session under the new key, or it collapses onto a key that is, which is what the ruling says it always was."}, {"key": "ai.onnx::GlobalAveragePool/1+/f32>f32/metadata/runtime-extent/n1", "owner": "Mouse", "date": "2026-08-04", "reason": "key-schema change 2026-08-04 (Mouse, per Morpheus \u00a78.9.23): the variant component now names the dispatched module instead of the literal `metadata`, and the `#form` suffix is withdrawn because group/strides/dilations/pads and transA/transB are push-constant expressions in one uniform code path, not separate code paths. The form is not withdrawn \u2014 it is re-proven in the same session under the new key, or it collapses onto a key that is, which is what the ruling says it always was."}, {"key": "ai.onnx::Gemm/7+/f32,f32,f32>f32/metadata#base/static/n3", "owner": "Mouse", "date": "2026-08-04", "reason": "key-schema change 2026-08-04 (Mouse, per Morpheus \u00a78.9.23): the variant component now names the dispatched module instead of the literal `metadata`, and the `#form` suffix is withdrawn because group/strides/dilations/pads and transA/transB are push-constant expressions in one uniform code path, not separate code paths. The form is not withdrawn \u2014 it is re-proven in the same session under the new key, or it collapses onto a key that is, which is what the ruling says it always was."}, {"key": "ai.onnx::Gemm/7+/f32,f32>f32/metadata#base/static/n2", "owner": "Mouse", "date": "2026-08-04", "reason": "key-schema change 2026-08-04 (Mouse, per Morpheus \u00a78.9.23): the variant component now names the dispatched module instead of the literal `metadata`, and the `#form` suffix is withdrawn because group/strides/dilations/pads and transA/transB are push-constant expressions in one uniform code path, not separate code paths. The form is not withdrawn \u2014 it is re-proven in the same session under the new key, or it collapses onto a key that is, which is what the ruling says it always was."}, {"key": "ai.onnx::Gemm/7+/f32,f32,f32>f32/metadata#transA/static/n3", "owner": "Mouse", "date": "2026-08-04", "reason": "key-schema change 2026-08-04 (Mouse, per Morpheus \u00a78.9.23): the variant component now names the dispatched module instead of the literal `metadata`, and the `#form` suffix is withdrawn because group/strides/dilations/pads and transA/transB are push-constant expressions in one uniform code path, not separate code paths. The form is not withdrawn \u2014 it is re-proven in the same session under the new key, or it collapses onto a key that is, which is what the ruling says it always was."}, {"key": "ai.onnx::Gemm/7+/f32,f32,f32>f32/metadata#transB/static/n3", "owner": "Mouse", "date": "2026-08-04", "reason": "key-schema change 2026-08-04 (Mouse, per Morpheus \u00a78.9.23): the variant component now names the dispatched module instead of the literal `metadata`, and the `#form` suffix is withdrawn because group/strides/dilations/pads and transA/transB are push-constant expressions in one uniform code path, not separate code paths. The form is not withdrawn \u2014 it is re-proven in the same session under the new key, or it collapses onto a key that is, which is what the ruling says it always was."}, {"key": "ai.onnx::Gemm/7+/f32,f32,f32>f32/metadata#transA+transB/static/n3", "owner": "Mouse", "date": "2026-08-04", "reason": "key-schema change 2026-08-04 (Mouse, per Morpheus \u00a78.9.23): the variant component now names the dispatched module instead of the literal `metadata`, and the `#form` suffix is withdrawn because group/strides/dilations/pads and transA/transB are push-constant expressions in one uniform code path, not separate code paths. The form is not withdrawn \u2014 it is re-proven in the same session under the new key, or it collapses onto a key that is, which is what the ruling says it always was."}, {"key": "ai.onnx::Gemm/7+/f32,f32,f32>f32/metadata#transB/runtime-extent/n3", "owner": "Mouse", "date": "2026-08-04", "reason": "key-schema change 2026-08-04 (Mouse, per Morpheus \u00a78.9.23): the variant component now names the dispatched module instead of the literal `metadata`, and the `#form` suffix is withdrawn because group/strides/dilations/pads and transA/transB are push-constant expressions in one uniform code path, not separate code paths. The form is not withdrawn \u2014 it is re-proven in the same session under the new key, or it collapses onto a key that is, which is what the ruling says it always was."}, {"key": "ai.onnx::Gemm/7+/f32,f32,f32>f32/metadata#base/runtime-extent/n3", "owner": "Mouse", "date": "2026-08-04", "reason": "key-schema change 2026-08-04 (Mouse, per Morpheus \u00a78.9.23): the variant component now names the dispatched module instead of the literal `metadata`, and the `#form` suffix is withdrawn because group/strides/dilations/pads and transA/transB are push-constant expressions in one uniform code path, not separate code paths. The form is not withdrawn \u2014 it is re-proven in the same session under the new key, or it collapses onto a key that is, which is what the ruling says it always was."}, {"key": "ai.onnx::Gather/1+/f32,i64>f32/metadata/runtime-extent/n2", "owner": "Mouse", "date": "2026-08-04", "reason": "key-schema change 2026-08-04 (Mouse, per Morpheus \u00a78.9.23): the variant component now names the dispatched module instead of the literal `metadata`, and the `#form` suffix is withdrawn because group/strides/dilations/pads and transA/transB are push-constant expressions in one uniform code path, not separate code paths. The form is not withdrawn \u2014 it is re-proven in the same session under the new key, or it collapses onto a key that is, which is what the ruling says it always was."}, {"key": "ai.onnx::Gemm/7+/f32,f32>f32/metadata#transA/static/n2", "owner": "Mouse", "date": "2026-08-04", "reason": "key-schema change 2026-08-04 (Mouse, per Morpheus \u00a78.9.23): the variant component now names the dispatched module instead of the literal `metadata`, and the `#form` suffix is withdrawn because group/strides/dilations/pads and transA/transB are push-constant expressions in one uniform code path, not separate code paths. The form is not withdrawn \u2014 it is re-proven in the same session under the new key, or it collapses onto a key that is, which is what the ruling says it always was."}, {"key": "ai.onnx::Gemm/7+/f32,f32>f32/metadata#transB/static/n2", "owner": "Mouse", "date": "2026-08-04", "reason": "key-schema change 2026-08-04 (Mouse, per Morpheus \u00a78.9.23): the variant component now names the dispatched module instead of the literal `metadata`, and the `#form` suffix is withdrawn because group/strides/dilations/pads and transA/transB are push-constant expressions in one uniform code path, not separate code paths. The form is not withdrawn \u2014 it is re-proven in the same session under the new key, or it collapses onto a key that is, which is what the ruling says it always was."}, {"key": "ai.onnx::Gemm/7+/f32,f32>f32/metadata#transA+transB/static/n2", "owner": "Mouse", "date": "2026-08-04", "reason": "key-schema change 2026-08-04 (Mouse, per Morpheus \u00a78.9.23): the variant component now names the dispatched module instead of the literal `metadata`, and the `#form` suffix is withdrawn because group/strides/dilations/pads and transA/transB are push-constant expressions in one uniform code path, not separate code paths. The form is not withdrawn \u2014 it is re-proven in the same session under the new key, or it collapses onto a key that is, which is what the ruling says it always was."}, {"key": "ai.onnx::Conv/1+/f32,f32,f32>f32/metadata#base/static/n3", "owner": "Mouse", "date": "2026-08-04", "reason": "key-schema change 2026-08-04 (Mouse, per Morpheus \u00a78.9.23): the variant component now names the dispatched module instead of the literal `metadata`, and the `#form` suffix is withdrawn because group/strides/dilations/pads and transA/transB are push-constant expressions in one uniform code path, not separate code paths. The form is not withdrawn \u2014 it is re-proven in the same session under the new key, or it collapses onto a key that is, which is what the ruling says it always was."}, {"key": "ai.onnx::Conv/1+/f32,f32,f32>f32/metadata#strided+padded/static/n3", "owner": "Mouse", "date": "2026-08-04", "reason": "key-schema change 2026-08-04 (Mouse, per Morpheus \u00a78.9.23): the variant component now names the dispatched module instead of the literal `metadata`, and the `#form` suffix is withdrawn because group/strides/dilations/pads and transA/transB are push-constant expressions in one uniform code path, not separate code paths. The form is not withdrawn \u2014 it is re-proven in the same session under the new key, or it collapses onto a key that is, which is what the ruling says it always was."}, {"key": "ai.onnx::Conv/1+/f32,f32,f32>f32/metadata#grouped+padded/static/n3", "owner": "Mouse", "date": "2026-08-04", "reason": "key-schema change 2026-08-04 (Mouse, per Morpheus \u00a78.9.23): the variant component now names the dispatched module instead of the literal `metadata`, and the `#form` suffix is withdrawn because group/strides/dilations/pads and transA/transB are push-constant expressions in one uniform code path, not separate code paths. The form is not withdrawn \u2014 it is re-proven in the same session under the new key, or it collapses onto a key that is, which is what the ruling says it always was."}, {"key": "ai.onnx::Conv/1+/f32,f32,f32>f32/metadata#grouped+strided+padded/static/n3", "owner": "Mouse", "date": "2026-08-04", "reason": "key-schema change 2026-08-04 (Mouse, per Morpheus \u00a78.9.23): the variant component now names the dispatched module instead of the literal `metadata`, and the `#form` suffix is withdrawn because group/strides/dilations/pads and transA/transB are push-constant expressions in one uniform code path, not separate code paths. The form is not withdrawn \u2014 it is re-proven in the same session under the new key, or it collapses onto a key that is, which is what the ruling says it always was."}, {"key": "ai.onnx::Gather/1+/f16,i64>f16/metadata/runtime-extent/n2", "owner": "Mouse", "date": "2026-08-04", "reason": "key-schema change 2026-08-04 (Mouse, per Morpheus \u00a78.9.23): the variant component now names the dispatched module instead of the literal `metadata`, and the `#form` suffix is withdrawn because group/strides/dilations/pads and transA/transB are push-constant expressions in one uniform code path, not separate code paths. The form is not withdrawn \u2014 it is re-proven in the same session under the new key, or it collapses onto a key that is, which is what the ruling says it always was."}, {"key": "ai.onnx::RMSNormalization/23-23/f32,f32>f32/metadata/static/n2", "owner": "Mouse", "date": "2026-08-04", "reason": "key-schema change 2026-08-04 (Mouse, per Morpheus \u00a78.9.23): the variant component now names the dispatched module instead of the literal `metadata`, and the `#form` suffix is withdrawn because group/strides/dilations/pads and transA/transB are push-constant expressions in one uniform code path, not separate code paths. The form is not withdrawn \u2014 it is re-proven in the same session under the new key, or it collapses onto a key that is, which is what the ruling says it always was."}, {"key": "ai.onnx::SimplifiedLayerNormalization/1+/f16,f16>f16/metadata/runtime-extent/n2", "owner": "Mouse", "date": "2026-08-04", "reason": "key-schema change 2026-08-04 (Mouse, per Morpheus \u00a78.9.23): the variant component now names the dispatched module instead of the literal `metadata`, and the `#form` suffix is withdrawn because group/strides/dilations/pads and transA/transB are push-constant expressions in one uniform code path, not separate code paths. The form is not withdrawn \u2014 it is re-proven in the same session under the new key, or it collapses onto a key that is, which is what the ruling says it always was."}, {"key": "com.microsoft::SkipSimplifiedLayerNormalization/1+/f16,f16,f16>f16/metadata/runtime-extent/none", "owner": "Mouse", "date": "2026-08-04", "reason": "key-schema change 2026-08-04 (Mouse, per Morpheus \u00a78.9.23): the variant component now names the dispatched module instead of the literal `metadata`, and the `#form` suffix is withdrawn because group/strides/dilations/pads and transA/transB are push-constant expressions in one uniform code path, not separate code paths. The form is not withdrawn \u2014 it is re-proven in the same session under the new key, or it collapses onto a key that is, which is what the ruling says it always was."}, {"key": "com.microsoft::SkipSimplifiedLayerNormalization/1+/f16,f16,f16>f16,-,-,f16/metadata/runtime-extent/none", "owner": "Mouse", "date": "2026-08-04", "reason": "key-schema change 2026-08-04 (Mouse, per Morpheus \u00a78.9.23): the variant component now names the dispatched module instead of the literal `metadata`, and the `#form` suffix is withdrawn because group/strides/dilations/pads and transA/transB are push-constant expressions in one uniform code path, not separate code paths. The form is not withdrawn \u2014 it is re-proven in the same session under the new key, or it collapses onto a key that is, which is what the ruling says it always was."}, {"key": "com.microsoft::SkipSimplifiedLayerNormalization/1+/f16,f16,f16>f16/metadata/static/none", "owner": "Mouse", "date": "2026-08-04", "reason": "key-schema change 2026-08-04 (Mouse, per Morpheus \u00a78.9.23): the variant component now names the dispatched module instead of the literal `metadata`, and the `#form` suffix is withdrawn because group/strides/dilations/pads and transA/transB are push-constant expressions in one uniform code path, not separate code paths. The form is not withdrawn \u2014 it is re-proven in the same session under the new key, or it collapses onto a key that is, which is what the ruling says it always was."}, {"key": "com.microsoft::SkipSimplifiedLayerNormalization/1+/f16,f16,f16>f16,-,-,f16/metadata/static/none", "owner": "Mouse", "date": "2026-08-04", "reason": "key-schema change 2026-08-04 (Mouse, per Morpheus \u00a78.9.23): the variant component now names the dispatched module instead of the literal `metadata`, and the `#form` suffix is withdrawn because group/strides/dilations/pads and transA/transB are push-constant expressions in one uniform code path, not separate code paths. The form is not withdrawn \u2014 it is re-proven in the same session under the new key, or it collapses onto a key that is, which is what the ruling says it always was."}, {"key": "com.microsoft::SkipSimplifiedLayerNormalization/1+/f32,f32,f32>f32/metadata/static/none", "owner": "Mouse", "date": "2026-08-04", "reason": "key-schema change 2026-08-04 (Mouse, per Morpheus \u00a78.9.23): the variant component now names the dispatched module instead of the literal `metadata`, and the `#form` suffix is withdrawn because group/strides/dilations/pads and transA/transB are push-constant expressions in one uniform code path, not separate code paths. The form is not withdrawn \u2014 it is re-proven in the same session under the new key, or it collapses onto a key that is, which is what the ruling says it always was."}, {"key": "com.microsoft::SkipSimplifiedLayerNormalization/1+/f32,f32,f32>f32,-,-,f32/metadata/static/none", "owner": "Mouse", "date": "2026-08-04", "reason": "key-schema change 2026-08-04 (Mouse, per Morpheus \u00a78.9.23): the variant component now names the dispatched module instead of the literal `metadata`, and the `#form` suffix is withdrawn because group/strides/dilations/pads and transA/transB are push-constant expressions in one uniform code path, not separate code paths. The form is not withdrawn \u2014 it is re-proven in the same session under the new key, or it collapses onto a key that is, which is what the ruling says it always was."}]}